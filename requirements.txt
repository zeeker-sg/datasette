# Kept aligned with pyproject.toml [project.dependencies] — used only as the
# Dockerfile pip fallback when uv.lock is absent.
boto3>=1.28.0
click>=8.1.3
datasette==0.65.2
datasette-search-all>=1.1.4
python-dotenv>=1.0.0
pyyaml>=6.0.2
