---
phase: 06-port-auxiliary-pages
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/zeeker-frontend/pyproject.toml
  - packages/zeeker-frontend/src/zeeker_frontend/data/changelog.yaml
  - packages/zeeker-frontend/tests/fixtures/searchable_databases.json
  - packages/zeeker-frontend/tests/fixtures/headlines_search_results.json
  - packages/zeeker-frontend/tests/fixtures/metadata_with_canned_queries.json
  - packages/zeeker-frontend/tests/fixtures/sql_error_400.json
  - packages/zeeker-frontend/tests/test_routes_aux.py
  - packages/zeeker-frontend/tests/test_routes_search.py
  - packages/zeeker-frontend/tests/test_routes_sql.py
  - packages/zeeker-frontend/tests/test_datasette_client_phase06.py
  - packages/zeeker-frontend/tests/test_changelog.py
autonomous: true
requirements:
  - REQ-frontend-route-set
  - REQ-frontend-data-via-http

threat_model:
  trust_boundaries:
    - "fixture files committed to git (synthetic-only, no production data)"
    - "pyyaml dependency boundary (declared, not system-fallback)"
  threats:
    - id: T-06-01-01
      category: "V14 Configuration (ASVS L1)"
      severity: medium
      surface: "PyYAML version drift causes ModuleNotFoundError or arbitrary YAML deserialization"
      mitigation: "pin pyyaml>=6.0,<7.0 in pyproject.toml; loader uses yaml.safe_load only (never yaml.load); RESEARCH §Pitfall 12"
    - id: T-06-01-02
      category: "V8 Data Protection"
      severity: low
      surface: "test fixtures committed to git could leak production data"
      mitigation: "all fixtures are synthetic shapes captured from a sanitised local datasette; no PII in fixture files; sql_error_400.json contains only the canonical 'no such table: nope' error body shape"
    - id: T-06-01-03
      category: "V5 Input Validation"
      severity: low
      surface: "Wave-0 test stubs reference functions that do not yet exist (Plan 02-05)"
      mitigation: "stubs use pytest.skip or import-time guards so collect-only succeeds; full assertions land in Plans 02-05; this plan asserts only that all test files are collectable"

must_haves:
  truths:
    - "pyyaml>=6.0,<7.0 is declared in packages/zeeker-frontend/pyproject.toml [project] dependencies"
    - "packages/zeeker-frontend/src/zeeker_frontend/data/changelog.yaml exists with the 8 recent_updates entries from M1 plugins/strings.yaml verbatim (date, type, title, description fields)"
    - "tests/fixtures/searchable_databases.json maps {db_name: [table_names]} with at least 2 databases x 1 table each"
    - "tests/fixtures/headlines_search_results.json captures /sglawwatch/headlines.json?_search=DBS&_size=10&_shape=objects shape with rows + columns + primary_keys + filtered_table_rows_count"
    - "tests/fixtures/metadata_with_canned_queries.json contains at least one databases.{db}.queries.{name} entry with sql + title + params"
    - "tests/fixtures/sql_error_400.json has {ok: false, error: 'no such table: nope', rows: [], columns: [], truncated: false}"
    - "test_routes_aux.py, test_routes_search.py, test_routes_sql.py, test_datasette_client_phase06.py, test_changelog.py exist and are pytest-collectable (`uv run pytest --collect-only` exits 0)"
    - "uv lock includes pyyaml after `uv sync` runs"
  artifacts:
    - path: "packages/zeeker-frontend/pyproject.toml"
      provides: "pyyaml dep declaration"
      contains: "pyyaml>=6.0"
    - path: "packages/zeeker-frontend/src/zeeker_frontend/data/changelog.yaml"
      provides: "Recent updates timeline data (D-12)"
      contains: "recent_updates:"
    - path: "packages/zeeker-frontend/tests/fixtures/searchable_databases.json"
      provides: "FTS-discovery cache fixture"
    - path: "packages/zeeker-frontend/tests/fixtures/headlines_search_results.json"
      provides: "FTS row results fixture"
    - path: "packages/zeeker-frontend/tests/fixtures/metadata_with_canned_queries.json"
      provides: "Canned-queries metadata fixture"
    - path: "packages/zeeker-frontend/tests/fixtures/sql_error_400.json"
      provides: "Datasette SQL 400 error body shape"
    - path: "packages/zeeker-frontend/tests/test_routes_aux.py"
      provides: "Stub tests for /developers, /status, /sources, /about, /how-to-use, /llms.txt, /robots.txt"
    - path: "packages/zeeker-frontend/tests/test_routes_search.py"
      provides: "Stub tests for /search State A, State B, partial-failure, 503"
    - path: "packages/zeeker-frontend/tests/test_routes_sql.py"
      provides: "Stub tests for /sql, GET /sql/{db}, POST /sql/{db}, _detect_params"
    - path: "packages/zeeker-frontend/tests/test_datasette_client_phase06.py"
      provides: "Stub tests for discover_searchable_tables, search_table, execute_sql"
    - path: "packages/zeeker-frontend/tests/test_changelog.py"
      provides: "Stub tests for load_changelog YAML loader"
  key_links:
    - from: "packages/zeeker-frontend/pyproject.toml"
      to: "uv lockfile"
      via: "uv sync"
      pattern: "pyyaml"
    - from: "packages/zeeker-frontend/src/zeeker_frontend/data/changelog.yaml"
      to: "plugins/strings.yaml recent_updates block"
      via: "verbatim port (D-12)"
      pattern: "recent_updates"
---

<objective>
Wave-0 scaffolding for Phase 6: declare the new pyyaml dependency, port the M1 changelog YAML
into a frontend-owned data file, capture all test fixtures, and create empty (collectable)
test stub files for every test surface that subsequent Plans 02-05 will fill in. Establishes
test infrastructure BEFORE handlers land, so the Wave-1+2 plans can wire real assertions
against pre-existing files.

Purpose: Plans 02-05 must not author new test files alongside production code (it conflates
TDD signal). Plan 01 lands all fixtures + stubs in one wave-0 commit, then Plans 02-05 fill
in test bodies as they ship handler code. Also lands the pyyaml dep so the lifespan boot
in Plan 02 will not ModuleNotFoundError inside the Docker container (RESEARCH §Pitfall 12).

Output: pyproject.toml extended; data/changelog.yaml created; 4 fixture files; 5 test stub
files; uv lockfile regenerated to include pyyaml.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-port-auxiliary-pages/06-CONTEXT.md
@.planning/phases/06-port-auxiliary-pages/06-RESEARCH.md
@.planning/phases/06-port-auxiliary-pages/06-PATTERNS.md
@.planning/phases/06-port-auxiliary-pages/06-VALIDATION.md

<interfaces>
<!-- Existing datasette_client exports executor must reference (do NOT redefine). -->
From packages/zeeker-frontend/src/zeeker_frontend/datasette_client.py:
```python
async def fetch_databases(client: httpx.AsyncClient) -> list[dict]
async def fetch_database(client: httpx.AsyncClient, db: str) -> dict | None
async def fetch_site_metadata(client: httpx.AsyncClient) -> dict
async def fetch_table(client, db, table, params=None) -> dict | None
async def fetch_row(client, db, table, pk) -> dict | None
_TABLE_ALLOWED_PARAMS  # frozenset
def reset_metadata_cache() -> None
```

From packages/zeeker-frontend/tests/conftest.py (Phase 5):
```python
# Fixtures already present and reusable: sglawwatch_fixture, metadata_fixture,
# headlines_table_fixture, about_singapore_law_table_fixture, headlines_row_fixture,
# judgments_row_fixture
```

Source for the YAML port — plugins/strings.yaml `recent_updates:` block (8 entries):
```yaml
recent_updates:
  - date: "2025-06-09"
    type: "feature"
    title: "data.zeeker.sg launches!"
    description: "Everything is new and shiny here!"
  - date: "2025-05-16"
    type: "data"
    title: "Sglawwatch -- headlines starts"
    description: "Legal News in Singapore selected by Editors starts compiling and automatic updates"
  # ... 6 more entries; copy verbatim from plugins/strings.yaml lines 78-126
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Declare pyyaml dependency + run uv sync</name>
  <read_first>
    - packages/zeeker-frontend/pyproject.toml (full file — see existing [project] dependencies block)
    - .planning/phases/06-port-auxiliary-pages/06-RESEARCH.md "Standard Stack" + "Pitfall 12" sections
    - .planning/phases/06-port-auxiliary-pages/06-PATTERNS.md "pyproject.toml (modified — add dep)" section
  </read_first>
  <files>packages/zeeker-frontend/pyproject.toml</files>
  <action>
    Open `packages/zeeker-frontend/pyproject.toml`. Locate the `[project]` table and its
    `dependencies = [...]` list (currently has fastapi[standard]==0.136.0, httpx==0.28.1,
    jinja2==3.1.6, uvicorn[standard]==0.44.0). Append a new entry at the END of the list,
    after the existing `uvicorn[standard]` line:

    ```
    "pyyaml>=6.0,<7.0",
    ```

    Preserve TOML formatting (matching indentation + trailing comma if list uses one).
    Do NOT add to `[dependency-groups]` or `[tool.uv]`. The dep is a runtime requirement
    for `changelog.py` lifespan loader (RESEARCH §Pattern 3 + Pitfall 12).

    After editing, run from the package directory:
    ```bash
    cd packages/zeeker-frontend && uv sync
    ```

    This regenerates the uv lockfile to include pyyaml. Verify the install with:
    ```bash
    cd packages/zeeker-frontend && uv run python -c "import yaml; print(yaml.__version__)"
    ```

    Expected: prints a 6.0.x version (e.g. `6.0.3`). The Docker frontend image will pick
    up the dep on next `docker compose build frontend` — Plan 06 verifier runs this.
  </action>
  <verify>
    <automated>cd packages/zeeker-frontend &amp;&amp; grep -E '^\s*"pyyaml&gt;=6\.0,&lt;7\.0"' pyproject.toml &amp;&amp; uv run python -c "import yaml; assert yaml.__version__.startswith('6.'), yaml.__version__"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E '"pyyaml&gt;=6\.0,&lt;7\.0"' packages/zeeker-frontend/pyproject.toml` exits 0
    - `cd packages/zeeker-frontend && uv run python -c "import yaml"` exits 0 (no ModuleNotFoundError)
    - `yaml.__version__` starts with `6.`
    - No other deps in pyproject.toml were modified (diff shows only the +1 pyyaml line in [project] dependencies)
  </acceptance_criteria>
  <done>pyyaml is declared, lockfile updated, container build will install it. Plan 02 can `import yaml` safely.</done>
</task>

<task type="auto">
  <name>Task 2: Port M1 changelog YAML + create test fixtures</name>
  <read_first>
    - plugins/strings.yaml (full file — focus on `recent_updates:` block)
    - .planning/phases/06-port-auxiliary-pages/06-PATTERNS.md "data/changelog.yaml" section
    - .planning/phases/06-port-auxiliary-pages/06-RESEARCH.md "Pattern 3" + "Code Examples"
    - packages/zeeker-frontend/tests/fixtures/sglawwatch.json (existing — fixture shape reference)
    - packages/zeeker-frontend/tests/fixtures/headlines_table.json (existing — Phase 5 row+columns shape reference)
    - packages/zeeker-frontend/tests/fixtures/metadata.json (existing — for canned-queries variant base)
  </read_first>
  <files>
    packages/zeeker-frontend/src/zeeker_frontend/data/changelog.yaml,
    packages/zeeker-frontend/tests/fixtures/searchable_databases.json,
    packages/zeeker-frontend/tests/fixtures/headlines_search_results.json,
    packages/zeeker-frontend/tests/fixtures/metadata_with_canned_queries.json,
    packages/zeeker-frontend/tests/fixtures/sql_error_400.json
  </files>
  <action>
    **Step 1 — Create the changelog YAML.** First create the data directory:
    `mkdir -p packages/zeeker-frontend/src/zeeker_frontend/data`. Then write
    `packages/zeeker-frontend/src/zeeker_frontend/data/changelog.yaml` with the 8 entries
    copied **verbatim** from `plugins/strings.yaml` `recent_updates:` block (M1 lines 78-126
    approximately — read the M1 file to get the exact content). Top-level YAML key MUST be
    `recent_updates:` (singular nesting key). Each entry has fields: `date` (ISO string,
    quoted), `type` (one of feature|data|technical|bugfix), `title`, `description`.
    Do NOT add new entries; do NOT edit copy. This is a verbatim port per D-12.

    **Step 2 — Create searchable_databases.json fixture.** Synthetic shape mirroring the
    return value of `discover_searchable_tables` (just the post-discovery dict, NOT the
    intermediate /-/databases.json shape):
    ```json
    {
      "sglawwatch": ["headlines", "about_singapore_law"],
      "Zeeker-Judgements": ["judgments"]
    }
    ```

    **Step 3 — Create headlines_search_results.json fixture.** Capture the shape of
    `/sglawwatch/headlines.json?_search=DBS&_size=10&_shape=objects`. If the live datasette
    is reachable, run:
    ```bash
    curl -s "http://localhost/sglawwatch/headlines.json?_search=DBS&_size=10&_shape=objects" \
      > packages/zeeker-frontend/tests/fixtures/headlines_search_results.json
    ```
    If not reachable (offline), construct synthetic JSON with all REQUIRED keys:
    `{"ok": true, "rows": [{"id": 1, "title": "Bank of DBS news", "excerpt": "...", "published_at": "2025-01-01"}, {"id": 2, "title": "...", ...}], "columns": ["id","title","excerpt","published_at"], "primary_keys": ["id"], "filtered_table_rows_count": 12, "query_ms": 4.2, "truncated": false, "next_url": null}`.
    At least 2 rows. Sanitise any production text to obviously-fake content.

    **Step 4 — Create metadata_with_canned_queries.json fixture.** Variant of existing
    `metadata.json` with at least one canned-query populated. Minimal shape:
    ```json
    {
      "title": "Zeeker",
      "databases": {
        "sglawwatch": {
          "title": "SG Law Watch",
          "description": "Legal news",
          "queries": {
            "headline_by_id": {
              "sql": "SELECT * FROM headlines WHERE id = :id",
              "title": "Headline by ID",
              "params": ["id"]
            }
          },
          "tables": {}
        }
      }
    }
    ```

    **Step 5 — Create sql_error_400.json fixture.** Captured from a real Datasette 400
    response (RESEARCH §Pitfall 1):
    ```json
    {
      "ok": false,
      "error": "no such table: nope",
      "rows": [],
      "columns": [],
      "truncated": false,
      "query_ms": 0.67,
      "query": {"sql": "SELECT * FROM nope", "params": {}}
    }
    ```

    All fixtures must be valid JSON (`python -m json.tool &lt; fixture.json` exits 0).
  </action>
  <verify>
    <automated>test -f packages/zeeker-frontend/src/zeeker_frontend/data/changelog.yaml &amp;&amp; cd packages/zeeker-frontend &amp;&amp; uv run python -c "import yaml; d=yaml.safe_load(open('src/zeeker_frontend/data/changelog.yaml').read()); assert 'recent_updates' in d; assert len(d['recent_updates']) &gt;= 8; assert all('date' in i and 'type' in i and 'title' in i for i in d['recent_updates'])" &amp;&amp; for f in tests/fixtures/searchable_databases.json tests/fixtures/headlines_search_results.json tests/fixtures/metadata_with_canned_queries.json tests/fixtures/sql_error_400.json; do uv run python -m json.tool "$f" &gt; /dev/null || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/zeeker-frontend/src/zeeker_frontend/data/changelog.yaml` exists
    - `yaml.safe_load` returns a dict with `recent_updates` key, list of >= 8 entries
    - Every entry dict has keys: `date`, `type`, `title`, `description`
    - 4 fixture files exist under `packages/zeeker-frontend/tests/fixtures/`
    - All 4 fixtures are valid JSON (json.tool succeeds)
    - `searchable_databases.json` has >= 2 keys, each value is a list with >= 1 string
    - `headlines_search_results.json` has keys: `rows` (list), `columns` (list), `filtered_table_rows_count` (int), `primary_keys` (list)
    - `metadata_with_canned_queries.json` `databases.sglawwatch.queries` has at least 1 entry with `sql` + `title` + `params`
    - `sql_error_400.json` has `ok: false`, `error` (non-empty string), `rows: []`, `columns: []`
  </acceptance_criteria>
  <done>YAML changelog + 4 test fixtures committed. Plan 02-05 unit tests can load these from the standard path without authoring more fixtures.</done>
</task>

<task type="auto">
  <name>Task 3: Author 5 collectable test stub files</name>
  <read_first>
    - packages/zeeker-frontend/tests/test_database.py:1-48 (Phase 4 integration test pattern — fixture factory + ASGITransport + reset_metadata_cache)
    - packages/zeeker-frontend/tests/test_routes_table.py:1-86 (Phase 5 multi-fixture handler factory, mock factory pattern)
    - packages/zeeker-frontend/tests/test_datasette_client_table_row.py:1-104 (unit test pattern — `_mock` helper + per-helper assertions)
    - packages/zeeker-frontend/tests/test_filters.py (Python module test pattern — for test_changelog reference)
    - packages/zeeker-frontend/tests/conftest.py (existing fixtures: sglawwatch_fixture, metadata_fixture, headlines_table_fixture, etc.)
    - .planning/phases/06-port-auxiliary-pages/06-PATTERNS.md "tests/test_routes_aux.py", "tests/test_routes_search.py", "tests/test_routes_sql.py", "tests/test_datasette_client_phase06.py", "tests/test_changelog.py" sections
    - .planning/phases/06-port-auxiliary-pages/06-VALIDATION.md "Wave 0 Requirements" + "Phase Requirements → Test Map"
  </read_first>
  <files>
    packages/zeeker-frontend/tests/test_routes_aux.py,
    packages/zeeker-frontend/tests/test_routes_search.py,
    packages/zeeker-frontend/tests/test_routes_sql.py,
    packages/zeeker-frontend/tests/test_datasette_client_phase06.py,
    packages/zeeker-frontend/tests/test_changelog.py
  </files>
  <action>
    Author 5 test stub files. Each MUST be pytest-collectable (`uv run pytest --collect-only`
    exits 0) but the test bodies are placeholders — Plans 02-05 will fill them in.

    Each stub file structure:
    1. Module docstring noting which Plan(s) own the real implementation
    2. Standard imports (`pytest`, `httpx`, `from zeeker_frontend.main import app`, etc.)
    3. Conftest-style fixture stubs IF needed (re-use existing conftest fixtures where possible)
    4. One `def test_collectable():` placeholder per planned test name. Body:
       `pytest.skip("Implementation pending — Plan 06-XX")` (cite the actual plan number).

    **packages/zeeker-frontend/tests/test_routes_aux.py** — placeholder tests:
    - test_developers_renders, test_status_renders, test_sources_hides_internal,
      test_about_renders, test_how_to_use_re_pointed, test_llms_txt_format, test_robots_txt
    - Each body: `pytest.skip("Implementation pending — Plan 06-03")`

    **packages/zeeker-frontend/tests/test_routes_search.py** — placeholder tests:
    - test_search_empty_query, test_search_groups_results, test_search_partial_failure,
      test_search_503_empty_cache, test_search_xss_q_echoed
    - Each body: `pytest.skip("Implementation pending — Plan 06-04")`

    **packages/zeeker-frontend/tests/test_routes_sql.py** — placeholder tests:
    - test_sql_landing, test_sql_db_get, test_sql_db_post_success,
      test_sql_db_post_400_error, test_sql_db_truncation_banner,
      test_sql_db_export_links, test_detect_params_regex
    - Each body: `pytest.skip("Implementation pending — Plan 06-05")`

    **packages/zeeker-frontend/tests/test_datasette_client_phase06.py** — placeholder tests:
    - test_discover_searchable_extracts_fts_table,
      test_discover_searchable_filters_zeeker_prefix,
      test_discover_searchable_filters_hidden,
      test_search_table_passes_q_and_size,
      test_search_table_404_returns_none,
      test_execute_sql_builds_param_url,
      test_execute_sql_400_returns_friendly_error,
      test_execute_sql_404_returns_db_not_found,
      test_execute_sql_shape_objects_always_set
    - Each body: `pytest.skip("Implementation pending — Plan 06-02")`

    **packages/zeeker-frontend/tests/test_changelog.py** — placeholder tests:
    - test_load_changelog_returns_list_of_dicts,
      test_load_changelog_returns_empty_when_file_missing,
      test_load_changelog_returns_empty_on_invalid_yaml,
      test_load_changelog_filters_entries_without_date
    - Each body: `pytest.skip("Implementation pending — Plan 06-02")`

    All files start with `from __future__ import annotations` and `import pytest`. No imports
    of `zeeker_frontend.changelog` or `zeeker_frontend.routes_*` (those modules don't exist
    yet — Plans 02-05 add them). The skip-only stubs avoid import errors at collect time.

    **Important:** Do NOT use the existing `client_with_mocked_datasette` fixture at module
    level in these files (it would trigger imports that don't yet exist). Define stubs as
    plain functions; Plans 02-05 add fixtures + real assertions.
  </action>
  <verify>
    <automated>cd packages/zeeker-frontend &amp;&amp; uv run pytest --collect-only tests/test_routes_aux.py tests/test_routes_search.py tests/test_routes_sql.py tests/test_datasette_client_phase06.py tests/test_changelog.py 2&gt;&amp;1 | grep -E "(error|ERROR)" &amp;&amp; exit 1 || true &amp;&amp; uv run pytest tests/test_routes_aux.py tests/test_routes_search.py tests/test_routes_sql.py tests/test_datasette_client_phase06.py tests/test_changelog.py -q 2&gt;&amp;1 | grep -E "skipped"</automated>
  </verify>
  <acceptance_criteria>
    - 5 test files exist at the specified paths
    - `cd packages/zeeker-frontend && uv run pytest --collect-only tests/test_routes_aux.py tests/test_routes_search.py tests/test_routes_sql.py tests/test_datasette_client_phase06.py tests/test_changelog.py` exits 0
    - Running just these 5 files via pytest shows ALL tests as `skipped` (none pass with real assertions, none fail/error)
    - test_routes_aux.py has >= 7 stub tests; test_routes_search.py has >= 5; test_routes_sql.py has >= 7; test_datasette_client_phase06.py has >= 9; test_changelog.py has >= 4
    - No test file imports `zeeker_frontend.changelog`, `zeeker_frontend.routes_aux`, `zeeker_frontend.routes_search`, or `zeeker_frontend.routes_sql` (those modules land in Plan 02/03)
    - Each `pytest.skip(...)` message cites the plan number that will fill in the body
  </acceptance_criteria>
  <done>5 test stub files exist and collect cleanly. Plans 02-05 will replace skip bodies with real assertions as they land production code.</done>
</task>

</tasks>

<verification>
1. `cd packages/zeeker-frontend && grep -E '"pyyaml>=6\.0,<7\.0"' pyproject.toml` → match
2. `cd packages/zeeker-frontend && uv run python -c "import yaml; print(yaml.__version__)"` → 6.x.x
3. `test -f packages/zeeker-frontend/src/zeeker_frontend/data/changelog.yaml` → exists
4. `cd packages/zeeker-frontend && uv run python -c "import yaml; d=yaml.safe_load(open('src/zeeker_frontend/data/changelog.yaml').read()); print(len(d['recent_updates']))"` → ≥ 8
5. All 4 fixtures pass `python -m json.tool`
6. `cd packages/zeeker-frontend && uv run pytest --collect-only` exits 0 across all test files
7. The 5 new test files report all skips, zero errors
</verification>

<success_criteria>
- pyyaml dep declared and installed (uv sync clean; `import yaml` works)
- Changelog YAML ported verbatim from M1 with 8+ entries
- 4 fixture files exist as valid JSON with documented shapes
- 5 test stub files exist, pytest-collectable, all tests skip cleanly
- Wave 0 unblocks Plans 02-05 — they can author handlers + fill in test bodies without authoring fixtures or pytest scaffolding inline
</success_criteria>

<output>
After completion, create `.planning/phases/06-port-auxiliary-pages/06-01-SUMMARY.md` documenting:
- pyproject.toml diff (+1 pyyaml line)
- changelog.yaml entry count + source citation (M1 plugins/strings.yaml lines)
- 4 fixture file shapes (capture method: live curl vs synthetic)
- 5 test stub file structure + skip-message convention
- Any blockers encountered (e.g., live datasette unreachable for fixture capture)
</output>
