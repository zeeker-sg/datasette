---
phase: 06-port-auxiliary-pages
plan: 04
type: execute
wave: 2
depends_on: [06-01, 06-02]
files_modified:
  - packages/zeeker-frontend/src/zeeker_frontend/routes_search.py
  - packages/zeeker-frontend/src/zeeker_frontend/templates/pages/search.html
  - packages/zeeker-frontend/src/zeeker_frontend/templates/_partials/search_result.html
  - packages/zeeker-frontend/src/zeeker_frontend/main.py
  - packages/zeeker-frontend/tests/test_routes_search.py
autonomous: true
requirements:
  - REQ-frontend-route-set
  - REQ-frontend-data-via-http
  - REQ-eliminate-template-drift

threat_model:
  trust_boundaries:
    - "user query string `q` echoed in HTML response"
    - "frontend → datasette per-table FTS fan-out (1 → N HTTP calls)"
    - "FTS-discovery cache (boot-time state on app.state.searchable_tables)"
  threats:
    - id: T-06-04-01
      category: "V5 Input Validation (ASVS L1)"
      severity: high
      surface: "Reflected XSS via `q` echoed in `<h1>Results for <em>{{ q }}</em></h1>` and form input value"
      mitigation: "Jinja autoescape ON for .html (Phase 4 wired); template uses `{{ q }}` and `{{ q|e }}` (NEVER `|safe`); verifier curls `/search?q=<script>alert(1)</script>` and asserts `<script>alert(1)</script>` does NOT appear raw in response body; the only `<mark>` markup comes from Datasette's `_search_highlight` field, NOT from frontend regex; RESEARCH §Security Domain"
    - id: T-06-04-02
      category: "V14 Configuration"
      severity: high
      surface: "asyncio.TaskGroup cancels siblings on first failure → empty /search on one slow table"
      mitigation: "use asyncio.gather(*tasks, return_exceptions=True) — NEVER TaskGroup; per-table coro `_safe_search_one` converts ALL httpx errors + ValueError to None sentinel; RESEARCH §Pitfall 2"
    - id: T-06-04-03
      category: "V14 Configuration"
      severity: medium
      surface: "Empty FTS-discovery cache after Datasette boot blip → /search renders 0 groups silently"
      mitigation: "when `app.state.searchable_tables == {}` AND `q` non-empty, raise HTTPException(503, 'Search temporarily unavailable. Try again in a minute.'); State A (empty q) always renders; RESEARCH §Pitfall 10"
    - id: T-06-04-04
      category: "V8 Data Protection"
      severity: low
      surface: "Internal hostname `zeeker-datasette:8001` leaks in error responses or `next_url`-style fields"
      mitigation: "/search does NOT pass next_url through to template (per-table top-10 only, no pagination); response body never embeds full datasette URL; verifier negative-asserts `! grep zeeker-datasette:8001`"
    - id: T-06-04-05
      category: "V5 Input Validation"
      severity: low
      surface: "FTS5 query operator injection (e.g., q='--' or unbalanced quotes) crashing datasette's FTS"
      mitigation: "datasette's FTS5 binding handles operator escaping server-side; if a bad query produces 5xx, `_safe_search_one` converts to None and that group is dropped; user sees their query echoed safely + a 'no results' or partial-failure notice"

must_haves:
  truths:
    - "GET /search returns 200 with rendered HTML in both states (empty `q` and non-empty `q`)"
    - "State A (empty `q`): renders hero + search-tips card, no fan-out, no datasette calls"
    - "State B (non-empty `q`): fans out via asyncio.gather(*tasks, return_exceptions=True) over app.state.searchable_tables; NEVER uses TaskGroup"
    - "_safe_search_one converts httpx + ValueError to None; one failing table does not abort the whole response"
    - "Results render grouped by (db, table) in alphabetical order; per-group: count + top-10 rows + 'see all' link to /{db}/{table}?_search=q when count > 10"
    - "Empty cache + non-empty q raises HTTPException(503, 'Search temporarily unavailable...')"
    - "GET /search Cache-Control: public, max-age=60, stale-while-revalidate=300"
    - "Reflected XSS: `q=<script>alert(1)</script>` is HTML-escaped in response body (no raw <script> tag)"
    - "search_router registered in main.py BEFORE database_router (after aux_router from Plan 03)"
    - "test_routes_search.py has REAL assertions (no skips); partial-failure test confirms one failing table does not zero-out results"
  artifacts:
    - path: "packages/zeeker-frontend/src/zeeker_frontend/routes_search.py"
      provides: "GET /search handler with State A + State B + 503 + partial-failure tolerance"
      contains: "router = APIRouter()\n@router.get(\"/search\"\nasyncio.gather(\nreturn_exceptions=True"
    - path: "packages/zeeker-frontend/src/zeeker_frontend/templates/pages/search.html"
      provides: "Two-state Jinja template (empty q vs results)"
      contains: "extends \"base.html\""
    - path: "packages/zeeker-frontend/src/zeeker_frontend/templates/_partials/search_result.html"
      provides: "Per-row search-result item card partial"
    - path: "packages/zeeker-frontend/src/zeeker_frontend/main.py"
      provides: "search_router registered before database_router"
      contains: "from zeeker_frontend.routes_search import router as search_router\napp.include_router(search_router)"
    - path: "packages/zeeker-frontend/tests/test_routes_search.py"
      provides: "Filled-in tests for /search State A, State B, partial-failure, 503, XSS escape"
  key_links:
    - from: "packages/zeeker-frontend/src/zeeker_frontend/routes_search.py"
      to: "app.state.searchable_tables"
      via: "request.app.state.searchable_tables"
      pattern: "request\\.app\\.state\\.searchable_tables"
    - from: "packages/zeeker-frontend/src/zeeker_frontend/routes_search.py"
      to: "datasette_client.search_table"
      via: "_safe_search_one wraps client.get directly OR calls search_table"
      pattern: "asyncio\\.gather"
    - from: "packages/zeeker-frontend/src/zeeker_frontend/main.py"
      to: "search_router"
      via: "include_router before database_router"
      pattern: "app\\.include_router\\(search_router\\)"
---

<objective>
Author the `/search` route — the cross-database FTS UI replacing M1's `/-/search`. Handler
fans out per-table FTS calls via `asyncio.gather(return_exceptions=True)`, groups results
by (db, table), and renders the civic-broadsheet two-state template (empty `q` vs results).
Registers `search_router` in `main.py` before `database_router`.

Purpose: This is one of two NEW user-facing surfaces in Phase 6 (the other is `/sql`,
Plan 05). The two run parallel in Wave 2 because they touch disjoint files (Plan 04 owns
routes_search.py + search.html, Plan 05 owns routes_sql.py + sql_*.html). Both depend on
Plan 02 (datasette_client extensions + lifespan).

Output: 1 router file, 1 page template + 1 partial, main.py router registration, filled-in
unit tests including the load-bearing partial-failure + XSS-escape assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-port-auxiliary-pages/06-CONTEXT.md
@.planning/phases/06-port-auxiliary-pages/06-RESEARCH.md
@.planning/phases/06-port-auxiliary-pages/06-PATTERNS.md
@.planning/phases/06-port-auxiliary-pages/06-UI-SPEC.md
@.planning/phases/06-port-auxiliary-pages/06-02-SUMMARY.md

<interfaces>
From packages/zeeker-frontend/src/zeeker_frontend/datasette_client.py (Plan 02):
async def search_table(client, db, table, q, size=10) -> dict | None
  # GET /{db}/{table}.json?_search=q&_size=size&_shape=objects
  # 404 → None; 5xx → raise httpx.HTTPError

main.py post Plan 02:
- app.state.http: httpx.AsyncClient
- app.state.searchable_tables: dict[str, list[str]]  # populated at lifespan boot
- app.state.changelog: list[dict]
- app.state.templates: Jinja2Templates

base.html provides nav/breadcrumb/footer; aux pages bind page_class="page-search".

Phase 5 partial referenced for visual patterning (do NOT redefine; reference only):
- templates/_partials/table_feed.html  # .va-item card pattern (Phase 5)
- templates/_partials/applied_facets.html  # .filter-chip pattern (Phase 5)

Datasette FTS response shape (live-verified):
{
  "ok": true,
  "rows": [{"id": 1, "title": "...", "excerpt": "...", "_search_highlight": {...}}],
  "columns": ["id","title","excerpt","..."],
  "primary_keys": ["id"],
  "filtered_table_rows_count": 12,
  "query_ms": 4.2,
  "truncated": false
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author routes_search.py + register search_router in main.py</name>
  <read_first>
    - packages/zeeker-frontend/src/zeeker_frontend/routes_table.py:1-150 (handler shape — 503 on httpx error, Cache-Control)
    - packages/zeeker-frontend/src/zeeker_frontend/routes_aux.py (Plan 03 — aux router registration pattern)
    - packages/zeeker-frontend/src/zeeker_frontend/main.py (post-Plan 03 — has aux_router import + include_router; insertion point for search_router is between aux_router and database_router)
    - packages/zeeker-frontend/src/zeeker_frontend/datasette_client.py (Plan 02 — `search_table` helper signature)
    - .planning/phases/06-port-auxiliary-pages/06-RESEARCH.md "Pattern 1" (full /search fan-out implementation) + "Pitfall 2" (TaskGroup vs gather) + "Pitfall 10" (empty cache 503)
    - .planning/phases/06-port-auxiliary-pages/06-PATTERNS.md "routes_search.py" section (lines ~152-208)
    - .planning/phases/06-port-auxiliary-pages/06-UI-SPEC.md "/search" section + "Interaction Contracts"
  </read_first>
  <files>
    packages/zeeker-frontend/src/zeeker_frontend/routes_search.py,
    packages/zeeker-frontend/src/zeeker_frontend/main.py
  </files>
  <action>
**Step 1 — Author packages/zeeker-frontend/src/zeeker_frontend/routes_search.py.**

Full file content (RESEARCH §Pattern 1):

```
"""Phase 6 — GET /search: cross-database FTS UI.

Fans out per-table FTS calls via asyncio.gather(return_exceptions=True) over
app.state.searchable_tables, groups results by (db, table), renders two states.
"""
from __future__ import annotations

import asyncio
from datetime import datetime

import httpx
from fastapi import APIRouter, HTTPException, Request
from fastapi.responses import HTMLResponse


router = APIRouter()

_CACHE_HEADER = "public, max-age=60, stale-while-revalidate=300"


async def _safe_search_one(
    client: httpx.AsyncClient, db: str, table: str, q: str, size: int
) -> dict | None:
    """Single per-table FTS call. Errors converted to None (caller drops the group).

    NEVER raises — RESEARCH Pitfall 2 (one bad table must not abort the whole search).
    """
    try:
        r = await client.get(
            f"/{db}/{table}.json",
            params={"_search": q, "_size": size, "_shape": "objects"},
            timeout=httpx.Timeout(3.0, connect=1.0),
        )
        r.raise_for_status()
        return r.json()
    except (httpx.HTTPError, ValueError):
        return None


def _derive_pk_value(row: dict, primary_keys: list[str]) -> str | None:
    """Return the comma-joined PK value(s) for row_url; None if PK can't be derived."""
    if not primary_keys:
        return None
    parts = []
    for k in primary_keys:
        v = row.get(k)
        if v is None:
            return None
        parts.append(str(v))
    return ",".join(parts)


@router.get("/search", response_class=HTMLResponse)
async def search(request: Request, q: str = "", _retry: int = 0):
    client: httpx.AsyncClient = request.app.state.http
    searchable: dict[str, list[str]] = getattr(
        request.app.state, "searchable_tables", {}
    )

    q_stripped = q.strip()

    # State A — empty query: hero + tips, no fan-out
    if not q_stripped:
        response = request.app.state.templates.TemplateResponse(
            request=request,
            name="pages/search.html",
            context={
                "q": "",
                "groups": [],
                "total_count": 0,
                "n_databases": 0,
                "failures": 0,
                "page_class": "page-search",
                "breadcrumbs": [{"label": "Search"}],
                "current_year": datetime.now().year,
            },
        )
        response.headers["Cache-Control"] = _CACHE_HEADER
        return response

    # Pitfall 10 — non-empty q but no searchable tables (boot blip): friendly 503
    if not searchable:
        raise HTTPException(
            status_code=503,
            detail="Search temporarily unavailable. Try again in a minute.",
        )

    # State B — fan out via gather(return_exceptions=True). NEVER TaskGroup (Pitfall 2).
    pairs: list[tuple[str, str]] = [
        (db, t) for db, ts in searchable.items() for t in ts
    ]
    tasks = [_safe_search_one(client, db, t, q_stripped, 10) for db, t in pairs]
    results = await asyncio.gather(*tasks, return_exceptions=True)

    groups: list[dict] = []
    failures = 0
    for (db, t), r in zip(pairs, results):
        if r is None or isinstance(r, BaseException):
            failures += 1
            continue
        rows = r.get("rows") or []
        if not rows:
            continue  # skip 0-result groups per UI-SPEC
        primary_keys = r.get("primary_keys") or []
        # Annotate each row with a derived PK string for row_url construction in the template
        for row in rows:
            row["_pk_str"] = _derive_pk_value(row, primary_keys)
        groups.append(
            {
                "db": db,
                "table": t,
                "count": r.get("filtered_table_rows_count") or len(rows),
                "rows": rows[:10],
                "primary_keys": primary_keys,
            }
        )
    groups.sort(key=lambda g: (g["db"], g["table"]))

    response = request.app.state.templates.TemplateResponse(
        request=request,
        name="pages/search.html",
        context={
            "q": q_stripped,
            "groups": groups,
            "total_count": sum(g["count"] for g in groups),
            "n_databases": len({g["db"] for g in groups}),
            "failures": failures,
            "page_class": "page-search",
            "breadcrumbs": [{"label": "Search"}],
            "current_year": datetime.now().year,
        },
    )
    response.headers["Cache-Control"] = _CACHE_HEADER
    return response
```

Critical:
- `asyncio.gather(*tasks, return_exceptions=True)` — NEVER `asyncio.TaskGroup`.
- `_safe_search_one` catches `httpx.HTTPError` AND `ValueError` (covers JSON-decode errors).
- Empty-cache 503 only fires when q is non-empty.
- The handler does NOT touch `_search_highlight` — the template renders Datasette's
  highlight markup directly via Jinja autoescape rules (template uses `{{ row.field }}`
  for the column name displayed; if Datasette returns an `_search_highlight` map, the
  template uses `{% autoescape false %}{{ row._search_highlight.field|default(row.field) }}{% endautoescape %}` ONLY if we explicitly want highlights — see UI-SPEC for the simpler path: just render `{{ row[col] }}` plain and rely on a separate `<mark>` rule that Datasette already provides as part of the highlight HTML).

**Step 2 — Register search_router in main.py.** Add ONE import + ONE include_router call.

After the existing `from zeeker_frontend.routes_aux import router as aux_router` line
(landed in Plan 03), add:
```
from zeeker_frontend.routes_search import router as search_router
```

In the `app.include_router(...)` block, insert AFTER `aux_router` and BEFORE
`database_router`:
```
app.include_router(home_router)
app.include_router(aux_router)        # Phase 6
app.include_router(search_router)     # Phase 6 — must precede database_router (RESEARCH Pitfall 3)
app.include_router(database_router)
app.include_router(table_router)
app.include_router(row_router)
```

**Step 3 — Smoke test.** Run the existing suite to confirm no regressions:
```
cd packages/zeeker-frontend && uv run pytest -x
```
All Plan 03 + earlier tests must remain green. The new `/search` route doesn't have its
own template yet (Task 2) so it will 500 if hit, but no existing tests hit it.
  </action>
  <verify>
    <automated>test -f packages/zeeker-frontend/src/zeeker_frontend/routes_search.py &amp;&amp; grep -qE "@router\.get\(\"/search\"" packages/zeeker-frontend/src/zeeker_frontend/routes_search.py &amp;&amp; grep -qE "asyncio\.gather\(.*return_exceptions=True" packages/zeeker-frontend/src/zeeker_frontend/routes_search.py &amp;&amp; ! grep -qE "TaskGroup" packages/zeeker-frontend/src/zeeker_frontend/routes_search.py &amp;&amp; grep -qE "from zeeker_frontend\.routes_search import router as search_router" packages/zeeker-frontend/src/zeeker_frontend/main.py &amp;&amp; grep -qE "app\.include_router\(search_router\)" packages/zeeker-frontend/src/zeeker_frontend/main.py</automated>
  </verify>
  <acceptance_criteria>
    - `routes_search.py` exists and contains `@router.get("/search"`
    - `asyncio.gather(*tasks, return_exceptions=True)` appears literally
    - `! grep TaskGroup packages/zeeker-frontend/src/zeeker_frontend/routes_search.py` (no TaskGroup usage)
    - `_safe_search_one` exists and catches both `httpx.HTTPError` and `ValueError`
    - Empty-cache 503 path: `if not searchable: raise HTTPException(503, ...)` present
    - `from zeeker_frontend.routes_search import router as search_router` in main.py
    - `app.include_router(search_router)` line in main.py appears AFTER `aux_router` and BEFORE `database_router`
    - `Cache-Control: public, max-age=60, stale-while-revalidate=300` set on both response paths
    - Existing test suite still green (`uv run pytest -x` exits 0)
  </acceptance_criteria>
  <done>routes_search.py with handler + main.py registration. Template + tests in subsequent tasks.</done>
</task>

<task type="auto">
  <name>Task 2: Author search.html (two-state) + search_result.html partial</name>
  <read_first>
    - packages/zeeker-frontend/src/zeeker_frontend/templates/base.html (the shell)
    - packages/zeeker-frontend/src/zeeker_frontend/templates/database.html (italic-accent H1 pattern)
    - packages/zeeker-frontend/src/zeeker_frontend/templates/_partials/table_feed.html (.va-item shape — pattern reference)
    - packages/zeeker-frontend/src/zeeker_frontend/templates/_partials/applied_facets.html (filter-chip pattern reference)
    - .planning/phases/06-port-auxiliary-pages/06-UI-SPEC.md "/search" section (State A + State B + result card anatomy)
    - .planning/phases/06-port-auxiliary-pages/06-PATTERNS.md "templates/pages/search.html" section
    - .planning/phases/06-port-auxiliary-pages/06-RESEARCH.md "Pattern 1" (handler context dict shape: q, groups, total_count, n_databases, failures)
  </read_first>
  <files>
    packages/zeeker-frontend/src/zeeker_frontend/templates/pages/search.html,
    packages/zeeker-frontend/src/zeeker_frontend/templates/_partials/search_result.html
  </files>
  <action>
**Step 1 — Author packages/zeeker-frontend/src/zeeker_frontend/templates/pages/search.html.**

Two-state template (empty `q` → State A; non-empty `q` → State B):

```
{% extends "base.html" %}

{% block content %}
<div class="container">
  <header class="search-hero guide-hero">
    <div class="kicker">№ 01 · SEARCH</div>
    {% if q %}
      <h1>Results for <em>{{ q }}</em></h1>
    {% else %}
      <h1>Search across <em>everything</em></h1>
    {% endif %}
    <form class="search-form" method="get" action="/search" role="search" autocomplete="off">
      <input type="search" name="q" value="{{ q }}" placeholder="Search FTS-indexed tables…">
      <button type="submit">Search</button>
      {% if q %}<a class="search-clear" href="/search">× Clear</a>{% endif %}
    </form>
    {% if q %}
      <p class="lede">{{ total_count }} {{ total_count|pluralize('result') }} across {{ n_databases }} {{ n_databases|pluralize('database') }}</p>
    {% else %}
      <p class="lede">Look across every searchable table on data.zeeker.sg. Results group by database and table, ranked by relevance.</p>
    {% endif %}
  </header>

  {% if not q %}
    {# State A — search tips #}
    <section class="aux-card search-tips">
      <h2>Search tips</h2>
      <ul>
        <li>Use quotes for exact phrases: <code>"data centre"</code></li>
        <li>Use AND / OR / NOT (FTS5 operators)</li>
        <li>Wildcard suffixes: <code>invest*</code> matches <code>investment</code> / <code>investor</code></li>
      </ul>
    </section>
  {% elif total_count == 0 %}
    {# State B — no results #}
    <section class="aux-card search-empty">
      <h2>No results for "{{ q }}"</h2>
      <p class="lede">Try broadening your search, removing operators, or checking for typos.</p>
      <ul>
        <li>Use quotes for exact phrases</li>
        <li>Try synonyms or shorter phrases</li>
        <li>Check spelling</li>
      </ul>
    </section>
  {% else %}
    {# State B — results region #}
    <section class="search-results-region">
      {% for group in groups %}
        <section class="search-group" id="group-{{ group.db }}-{{ group.table }}">
          <header class="search-group-head">
            <h2><a href="/{{ group.db }}/{{ group.table }}?_search={{ q|urlencode }}">{{ group.db }} › {{ group.table }}</a></h2>
            <span class="result-count">{{ group.count }} {{ group.count|pluralize('result') }}</span>
          </header>
          <ol class="search-results">
            {% for row in group.rows %}
              {% include "_partials/search_result.html" %}
            {% endfor %}
          </ol>
          {% if group.count > 10 %}
            <a class="see-all" href="/{{ group.db }}/{{ group.table }}?_search={{ q|urlencode }}">See all {{ group.count }} results in {{ group.table }} →</a>
          {% endif %}
        </section>
      {% endfor %}

      {% if failures %}
        <p class="search-failures-notice">Search timed out for {{ failures }} {{ failures|pluralize('table') }}. <a href="?q={{ q|urlencode }}&_retry=1">Retry →</a></p>
      {% endif %}
    </section>
  {% endif %}
</div>
{% endblock %}
```

Critical:
- `{{ q }}` and `value="{{ q }}"` rely on Jinja autoescape ON for `.html` (Phase 4 wired
  `select_autoescape(['html'])`). NEVER use `|safe` on `q`.
- `q|urlencode` for URL embedding (used in `_search=` querystring + retry link).
- The `_partials/search_result.html` include is a Jinja `{% include %}` (no macro).

**Step 2 — Author packages/zeeker-frontend/src/zeeker_frontend/templates/_partials/search_result.html.**

```
{# Single FTS result row. Reads from parent scope: row, group, q.

   row may have:
     - row[col] for any column in group.primary_keys (used for title/excerpt heuristic)
     - row._pk_str — comma-joined primary key string (set by handler), or None
   group provides:
     - group.db, group.table, group.primary_keys
   q is the raw search term (autoescaped).
#}
<li class="search-result">
  <div class="search-result-meta">
    <span class="kicker">{{ group.db }} · {{ group.table }}</span>
  </div>
  <h3 class="search-result-title">
    {% if row._pk_str %}
      <a href="/{{ group.db }}/{{ group.table }}/{{ row._pk_str|urlencode }}">
        {# Use the first non-PK string column as title heuristic; fall back to PK value #}
        {%- for col, val in row.items() if col not in group.primary_keys and col != "_pk_str" and val is string and val %}
          {%- if loop.first %}{{ val[:120] }}{% endif %}
        {%- else %}
          {{ row._pk_str }}
        {%- endfor %}
      </a>
    {% else %}
      <a href="/{{ group.db }}/{{ group.table }}?_search={{ q|urlencode }}">
        {%- for col, val in row.items() if val is string and val %}
          {%- if loop.first %}{{ val[:120] }}{% endif %}
        {%- endfor %}
      </a>
    {% endif %}
  </h3>
  <div class="search-result-foot">
    {%- for col, val in row.items() if col != "_pk_str" %}
      {%- if loop.first %}{# skipped — used as title #}
      {%- elif loop.index <= 3 and val is string and val|length < 200 %}
        <span class="meta-cell"><span class="meta-key">{{ col }}</span> {{ val }}</span>
      {%- endif %}
    {%- endfor %}
  </div>
</li>
```

This partial is intentionally simple — UI-SPEC §Result card anatomy describes a richer
shape (mark highlights, dateline, etc.), but for v1 the partial renders title +
db/table kicker + first 3 short string columns. Visual polish lands when CSS
appends in Plan 06.

**Step 3 — Smoke test the template renders without Jinja syntax errors.**
```
cd packages/zeeker-frontend && uv run python -c "
from jinja2 import Environment, FileSystemLoader
env = Environment(loader=FileSystemLoader('src/zeeker_frontend/templates'), autoescape=True)
env.get_template('pages/search.html')
env.get_template('_partials/search_result.html')
print('OK')
"
```
Expected: prints `OK`.
  </action>
  <verify>
    <automated>test -f packages/zeeker-frontend/src/zeeker_frontend/templates/pages/search.html &amp;&amp; test -f packages/zeeker-frontend/src/zeeker_frontend/templates/_partials/search_result.html &amp;&amp; grep -qE 'extends "base\.html"' packages/zeeker-frontend/src/zeeker_frontend/templates/pages/search.html &amp;&amp; grep -qE '&lt;h1&gt;.*&lt;em&gt;' packages/zeeker-frontend/src/zeeker_frontend/templates/pages/search.html &amp;&amp; ! grep -qE '\|safe' packages/zeeker-frontend/src/zeeker_frontend/templates/pages/search.html &amp;&amp; cd packages/zeeker-frontend &amp;&amp; uv run python -c "from jinja2 import Environment, FileSystemLoader; env=Environment(loader=FileSystemLoader('src/zeeker_frontend/templates'), autoescape=True); env.get_template('pages/search.html'); env.get_template('_partials/search_result.html'); print('OK')" | grep -q OK</automated>
  </verify>
  <acceptance_criteria>
    - `templates/pages/search.html` exists and `extends "base.html"`
    - Both italic-accent H1s present: `Results for <em>...` (State B) and `Search across <em>everything</em>` (State A)
    - `templates/_partials/search_result.html` exists
    - search.html does NOT use `|safe` filter on `q`
    - Smoke test succeeds (Jinja parses both templates)
    - search.html uses `{% include "_partials/search_result.html" %}` for per-row rendering
  </acceptance_criteria>
  <done>Two-state /search template + result-card partial parse cleanly. Plan 06 CSS append will visually polish; functional shape is in place.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fill in test_routes_search.py with REAL assertions</name>
  <read_first>
    - packages/zeeker-frontend/tests/test_routes_table.py:1-200 (multi-fixture handler factory pattern)
    - packages/zeeker-frontend/tests/test_routes_aux.py (Plan 03 — fixture pattern reference)
    - packages/zeeker-frontend/tests/fixtures/headlines_search_results.json (Plan 01 fixture)
    - packages/zeeker-frontend/src/zeeker_frontend/routes_search.py (post-Task-1)
    - .planning/phases/06-port-auxiliary-pages/06-PATTERNS.md "tests/test_routes_search.py" section (lines 921-960)
    - .planning/phases/06-port-auxiliary-pages/06-VALIDATION.md "Phase Requirements → Test Map" — test_search_* rows
    - .planning/phases/06-port-auxiliary-pages/06-RESEARCH.md "Pitfall 2" (partial-failure assertion)
  </read_first>
  <behavior>
    Required tests (replace stubs):
    - test_search_empty_query: GET /search → 200, body contains "Search across" + "<em>", form input present, NO datasette calls made (assert mock handler was not invoked)
    - test_search_groups_results: GET /search?q=DBS with 2-table searchable cache → 200, body contains group headings for both tables, top-N rows
    - test_search_partial_failure: GET /search?q=foo with one fixture-success table + one mocked-fail table (`raise_on=` matched) → 200, body still contains the successful group; failures notice present
    - test_search_503_empty_cache: GET /search?q=foo with `app.state.searchable_tables = {}` → 503
    - test_search_xss_q_echoed: GET /search?q=<script>alert(1)</script> → 200, raw `<script>alert(1)</script>` does NOT appear in response body (autoescape on)
    - test_search_cache_control: 200 sets max-age=60 + stale-while-revalidate=300
  </behavior>
  <files>
    packages/zeeker-frontend/tests/test_routes_search.py
  </files>
  <action>
Replace EVERY `pytest.skip(...)` body with real assertions. Pattern from
`test_routes_table.py:54-86` adapted for searchable_tables fan-out:

```
"""Phase 6 — /search route integration tests (Plan 04)."""
from __future__ import annotations

import json
import re
from pathlib import Path

import httpx
import pytest

from zeeker_frontend.main import app


_FIXTURES = Path(__file__).parent / "fixtures"
_ITALIC_H1 = re.compile(r"<h1>.*?<em[^>]*>[^<]+</em>.*?</h1>", re.DOTALL)


def _load(name: str):
    return json.loads((_FIXTURES / name).read_text())


def _mock_factory(*, hits: dict[str, dict], raise_on: str | None = None):
    """Return an AsyncClient that returns `hits[path]` for each .json call.
    Paths missing from `hits` return 404. If `raise_on` substring matches the path,
    a ConnectError is raised."""
    def handler(request: httpx.Request) -> httpx.Response:
        path = request.url.path
        if raise_on and raise_on in path:
            raise httpx.ConnectError("simulated upstream failure")
        if path in hits:
            return httpx.Response(200, json=hits[path])
        return httpx.Response(404, json={"ok": False, "error": "not found"})
    return httpx.AsyncClient(
        base_url="http://zeeker-datasette:8001",
        transport=httpx.MockTransport(handler),
    )


@pytest.fixture
async def client_search():
    """Two-table searchable cache. Default fixture for State B tests."""
    search_fixture = _load("headlines_search_results.json")
    app.state.http = _mock_factory(hits={
        "/sglawwatch/headlines.json": search_fixture,
        "/Zeeker-Judgements/judgments.json": search_fixture,
    })
    app.state.searchable_tables = {
        "sglawwatch": ["headlines"],
        "Zeeker-Judgements": ["judgments"],
    }
    app.state.changelog = []
    try:
        async with httpx.AsyncClient(
            transport=httpx.ASGITransport(app=app), base_url="http://testserver",
        ) as ac:
            yield ac
    finally:
        await app.state.http.aclose()


@pytest.fixture
async def client_search_partial():
    """Two-table cache; one table errors out (Pitfall 2 partial-failure assertion)."""
    search_fixture = _load("headlines_search_results.json")
    app.state.http = _mock_factory(
        hits={"/sglawwatch/headlines.json": search_fixture},
        raise_on="/Zeeker-Judgements",
    )
    app.state.searchable_tables = {
        "sglawwatch": ["headlines"],
        "Zeeker-Judgements": ["judgments"],
    }
    app.state.changelog = []
    try:
        async with httpx.AsyncClient(
            transport=httpx.ASGITransport(app=app), base_url="http://testserver",
        ) as ac:
            yield ac
    finally:
        await app.state.http.aclose()


@pytest.fixture
async def client_search_empty_cache():
    """No FTS-discovery cache (boot-blip simulation)."""
    app.state.http = _mock_factory(hits={})
    app.state.searchable_tables = {}
    app.state.changelog = []
    try:
        async with httpx.AsyncClient(
            transport=httpx.ASGITransport(app=app), base_url="http://testserver",
        ) as ac:
            yield ac
    finally:
        await app.state.http.aclose()


@pytest.mark.asyncio
async def test_search_empty_query(client_search):
    r = await client_search.get("/search")
    assert r.status_code == 200
    assert _ITALIC_H1.search(r.text)
    assert "Search across" in r.text
    assert 'name="q"' in r.text  # form input present
    cc = r.headers.get("cache-control", "")
    assert "max-age=60" in cc and "stale-while-revalidate=300" in cc


@pytest.mark.asyncio
async def test_search_groups_results(client_search):
    r = await client_search.get("/search?q=DBS")
    assert r.status_code == 200
    assert _ITALIC_H1.search(r.text)
    # Both groups present (alphabetical by db name)
    assert "sglawwatch" in r.text
    assert "Zeeker-Judgements" in r.text
    # Top-N rows render — at least one row from the fixture leaks into the body
    fixture = _load("headlines_search_results.json")
    if fixture.get("rows"):
        first_row = fixture["rows"][0]
        for v in first_row.values():
            if isinstance(v, str) and v:
                assert v[:30] in r.text or any(v[:30] in line for line in r.text.split("\n"))
                break


@pytest.mark.asyncio
async def test_search_partial_failure(client_search_partial):
    """Pitfall 2 — one failing table must NOT empty /search."""
    r = await client_search_partial.get("/search?q=foo")
    assert r.status_code == 200
    # Successful group still rendered
    assert "sglawwatch" in r.text
    # Failure notice present
    assert "timed out" in r.text or "failed" in r.text.lower() or "retry" in r.text.lower()


@pytest.mark.asyncio
async def test_search_503_empty_cache(client_search_empty_cache):
    """Pitfall 10 — empty FTS-discovery cache + non-empty q → 503."""
    r = await client_search_empty_cache.get("/search?q=foo")
    assert r.status_code == 503


@pytest.mark.asyncio
async def test_search_empty_cache_state_a_still_renders(client_search_empty_cache):
    """State A renders even when cache is empty (Pitfall 10 — q empty bypasses fan-out)."""
    r = await client_search_empty_cache.get("/search")
    assert r.status_code == 200
    assert _ITALIC_H1.search(r.text)


@pytest.mark.asyncio
async def test_search_xss_q_echoed(client_search):
    """T-06-04-01 — Reflected XSS prevented by Jinja autoescape."""
    r = await client_search.get("/search?q=%3Cscript%3Ealert(1)%3C%2Fscript%3E")
    assert r.status_code == 200
    # Raw <script> tag MUST NOT appear in response body
    assert "<script>alert(1)</script>" not in r.text
    # The escaped form may appear (e.g., &lt;script&gt;) — that's expected
```

Note: The `test_search_groups_results` row-content assertion is best-effort — the
fixture rows may not have predictable text. The hard guarantees are:
- Both group names present in body
- Top-N rows section rendered (the row count chip "{N} results" etc.)

Run the tests:
```
cd packages/zeeker-frontend && uv run pytest tests/test_routes_search.py -v
```
All 6 tests must pass.

Then full suite:
```
cd packages/zeeker-frontend && uv run pytest -x
```
  </action>
  <verify>
    <automated>cd packages/zeeker-frontend &amp;&amp; uv run pytest tests/test_routes_search.py -v 2&gt;&amp;1 | tee /tmp/p06-04-t3.log &amp;&amp; ! grep -q "skipped" /tmp/p06-04-t3.log &amp;&amp; grep -E "passed" /tmp/p06-04-t3.log &amp;&amp; uv run pytest -x 2&gt;&amp;1 | tail -3 | grep -E "passed"</automated>
  </verify>
  <acceptance_criteria>
    - `cd packages/zeeker-frontend && uv run pytest tests/test_routes_search.py -v` reports >= 6 PASSED, 0 skipped, 0 failed
    - `cd packages/zeeker-frontend && uv run pytest -x` (full suite) exits 0
    - test_search_partial_failure passes (proves gather(return_exceptions=True) tolerates 1 failure)
    - test_search_503_empty_cache passes (proves Pitfall 10 mitigation)
    - test_search_xss_q_echoed passes (proves autoescape mitigates Reflected XSS)
    - test_search_empty_cache_state_a_still_renders passes (State A unaffected by cache)
  </acceptance_criteria>
  <done>6 search tests pass; partial-failure tolerance + XSS escape + empty-cache 503 all verified.</done>
</task>

</tasks>

<verification>
1. `cd packages/zeeker-frontend && uv run pytest tests/test_routes_search.py -v` → 6 passed, 0 skipped
2. `cd packages/zeeker-frontend && uv run pytest -x` → full suite green
3. `routes_search.py` uses `asyncio.gather(*tasks, return_exceptions=True)` and NOT TaskGroup
4. main.py registers `search_router` AFTER `aux_router` and BEFORE `database_router`
5. search.html has italic-accent H1 in both states
6. Reflected XSS test: `q=<script>alert(1)</script>` does NOT echo raw `<script>` tag
</verification>

<success_criteria>
- /search renders State A (empty q) + State B (with results) + 503 (empty cache)
- Fan-out tolerates per-table failures via gather(return_exceptions=True)
- search_router registered before database_router
- 6 integration tests pass; XSS + partial-failure + empty-cache invariants verified
- File-disjoint with Plan 05 — both can run parallel in Wave 2
</success_criteria>

<output>
After completion, create `.planning/phases/06-port-auxiliary-pages/06-04-SUMMARY.md` documenting:
- Handler shape: State A (no fan-out), State B (gather-based fan-out), 503 (empty cache)
- Critical invariant: gather(return_exceptions=True), NEVER TaskGroup
- Template structure: 2 states in one .html + 1 partial for result rows
- main.py router registration order: home_router → aux_router → search_router → database_router → table_router → row_router
- 6 integration tests + key XSS / partial-failure / 503 assertions
</output>
