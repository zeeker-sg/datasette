---
phase: 06-port-auxiliary-pages
plan: 05
type: execute
wave: 2
depends_on: [06-01, 06-02]
files_modified:
  - packages/zeeker-frontend/src/zeeker_frontend/routes_sql.py
  - packages/zeeker-frontend/src/zeeker_frontend/templates/pages/sql_landing.html
  - packages/zeeker-frontend/src/zeeker_frontend/templates/pages/sql_db.html
  - packages/zeeker-frontend/src/zeeker_frontend/main.py
  - packages/zeeker-frontend/tests/test_routes_sql.py
autonomous: true
requirements:
  - REQ-frontend-route-set
  - REQ-frontend-data-via-http
  - REQ-eliminate-template-drift

threat_model:
  trust_boundaries:
    - "user-submitted SQL text + form params (POST body) → datasette read-only execution"
    - "Datasette SQL response: HTTP 400 = validated client error WITH body (NOT terminal failure)"
    - "frontend → datasette internal HTTP (1 GET per execute_sql call)"
  threats:
    - id: T-06-05-01
      category: "V5 Input Validation (ASVS L1)"
      severity: critical
      surface: "SQL injection via canned-query param substitution"
      mitigation: "param values are bound via Datasette `_param_<name>=<value>` URL params, NEVER concatenated into the SQL string; Datasette's prepared-statement-style placeholders enforce safe binding; `_detect_params` regex restricts param names to `[a-zA-Z_][a-zA-Z0-9_]*`; RESEARCH §Don't Hand-Roll + §Pitfall 7"
    - id: T-06-05-02
      category: "V5 Input Validation"
      severity: high
      surface: "SSRF via querystring smuggling — form fields other than `sql`/`_sql_param_*` forwarded to Datasette"
      mitigation: "explicit allowlist when building `ds_params`: only `sql`, `_shape=objects`, and `_param_<name>` keys for names that appear in `_detect_params(sql)`; everything else in `request.form()` is silently dropped; NEVER `params=request.form()`; RESEARCH §Pitfall 7"
    - id: T-06-05-03
      category: "V14 Configuration"
      severity: high
      surface: "Naive `r.raise_for_status()` on Datasette 400 response loses the friendly `error` field"
      mitigation: "execute_sql + handler check `r.status_code == 400` BEFORE `raise_for_status()`; render `body.error` as inline error block; user sees the actual SQL error message, not 503; RESEARCH §Pitfall 1"
    - id: T-06-05-04
      category: "V11 Business Logic"
      severity: medium
      surface: "Long-running SQL → DoS or timeout"
      mitigation: "trust Datasette's built-in `ms_limit=3000` + 1000-row cap (D-08); frontend adds NO timeout layering; truncated=true response renders banner + CSV download link routed direct via Caddy suffix"
    - id: T-06-05-05
      category: "V5 Input Validation"
      severity: medium
      surface: "Unbounded result-set size → memory pressure"
      mitigation: "Datasette enforces 1000-row cap; frontend renders `truncated=true` banner with CSV link; Cache-Control: no-store on POST so results are not cached"
    - id: T-06-05-06
      category: "V14 Configuration"
      severity: low
      surface: "POST cache poisoning"
      mitigation: "Cache-Control: no-store on every POST /sql/{db} response (D-14)"

must_haves:
  truths:
    - "GET /sql returns 200 with rendered HTML listing all visible databases (hidden filter applied at DB level)"
    - "GET /sql/{db} returns 200 with empty editor (when no `sql` query param) OR pre-filled editor (when `?sql=...` provided); does NOT execute on GET"
    - "GET /sql/{db} for unknown db returns 404 with 'Database not found' message"
    - "POST /sql/{db} executes via execute_sql, renders results table on success"
    - "POST /sql/{db} with invalid SQL renders the friendly error message inline (status 200 with .sql-error block); does NOT 503 (RESEARCH §Pitfall 1)"
    - "POST /sql/{db} with truncated=true renders truncation banner with CSV download link (suffix-routed to /{db}.csv?sql=...)"
    - "POST /sql/{db} export anchors are URL-encoded: `<a href=\"/{db}.csv?sql={urlencoded}\">CSV</a>`"
    - "POST /sql/{db} Cache-Control: no-store"
    - "GET /sql, GET /sql/{db} Cache-Control: public, max-age=60, stale-while-revalidate=300"
    - "_detect_params regex: `r':([a-zA-Z_][a-zA-Z0-9_]*)'` returns deduped param names in encounter order"
    - "Querystring allowlist: only `sql` + `_shape=objects` + `_param_<name>` keys reach upstream; everything else in form is dropped"
    - "sql_router registered in main.py BEFORE database_router (after aux_router + search_router)"
    - "test_routes_sql.py has REAL assertions (no skips); 400-error rendering test confirms body inspected before raise_for_status"
  artifacts:
    - path: "packages/zeeker-frontend/src/zeeker_frontend/routes_sql.py"
      provides: "GET /sql, GET /sql/{db}, POST /sql/{db} handlers + _detect_params helper"
      contains: "router = APIRouter()\n@router.get(\"/sql\"\n@router.get(\"/sql/{db}\"\n@router.post(\"/sql/{db}\"\n_PARAM_RE\n_detect_params"
    - path: "packages/zeeker-frontend/src/zeeker_frontend/templates/pages/sql_landing.html"
      provides: "GET /sql landing — database list (editorial-row pattern)"
    - path: "packages/zeeker-frontend/src/zeeker_frontend/templates/pages/sql_db.html"
      provides: "GET + POST /sql/{db} — editor + canned queries + results/error/truncation"
    - path: "packages/zeeker-frontend/src/zeeker_frontend/main.py"
      provides: "sql_router registered before database_router"
      contains: "from zeeker_frontend.routes_sql import router as sql_router\napp.include_router(sql_router)"
    - path: "packages/zeeker-frontend/tests/test_routes_sql.py"
      provides: "Filled-in tests for /sql landing + GET /sql/{db} + POST success/400/truncation/export-links + _detect_params unit tests"
  key_links:
    - from: "packages/zeeker-frontend/src/zeeker_frontend/routes_sql.py POST handler"
      to: "datasette_client.execute_sql"
      via: "execute_sql(client, db, sql, param_values)"
      pattern: "execute_sql"
    - from: "packages/zeeker-frontend/src/zeeker_frontend/main.py"
      to: "sql_router"
      via: "include_router before database_router"
      pattern: "app\\.include_router\\(sql_router\\)"
    - from: "packages/zeeker-frontend/src/zeeker_frontend/templates/pages/sql_db.html"
      to: "/{db}.csv?sql=...  (Caddy suffix route)"
      via: "<a href> with urlencoded sql"
      pattern: "\\.csv\\?sql="
---

<objective>
Author the `/sql` route family — landing page (`GET /sql`), per-database editor (`GET
/sql/{db}` + `POST /sql/{db}`), and the SQL results/error/truncation rendering. Uses
Plan 02's `execute_sql` helper for the POST. Registers `sql_router` in `main.py` before
`database_router`.

Purpose: Second of two NEW user-facing surfaces in Phase 6 (paired with /search in
Plan 04). Plans 04 and 05 are file-disjoint and run parallel in Wave 2. /sql implements
PRD R2 v1: a thin SQL editor that delegates safety to Datasette's read-only mode +
ms_limit + 1000-row cap (D-08).

Output: 1 router file, 2 page templates (landing + per-db editor), main.py registration,
filled-in unit tests including the load-bearing 400-error-rendering + querystring
allowlist + truncation-banner assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-port-auxiliary-pages/06-CONTEXT.md
@.planning/phases/06-port-auxiliary-pages/06-RESEARCH.md
@.planning/phases/06-port-auxiliary-pages/06-PATTERNS.md
@.planning/phases/06-port-auxiliary-pages/06-UI-SPEC.md
@.planning/phases/06-port-auxiliary-pages/06-02-SUMMARY.md

<interfaces>
From packages/zeeker-frontend/src/zeeker_frontend/datasette_client.py (Plan 02):
async def execute_sql(client, db, sql, params=None) -> tuple[dict | None, str | None]
  # Returns (body, error). Body is None on 400/404; body has rows/columns/truncated on 200.
  # Error is None on success; populated on 400 with Datasette's friendly message.
async def fetch_databases(client) -> list[dict]
async def fetch_database(client, db) -> dict | None
async def fetch_site_metadata(client) -> dict

main.py post Plan 04:
- app.state.http: httpx.AsyncClient
- app.state.searchable_tables: dict (used by /search)
- app.state.changelog: list (used by /status)
- app.state.templates: Jinja2Templates
Routers registered (so far): home_router, aux_router (Plan 03), search_router (Plan 04).
Plan 05 inserts sql_router AFTER search_router and BEFORE database_router.

Datasette canned-queries metadata path:
metadata.json["databases"][db]["queries"][name] = {"sql": "...", "title": "...", "params": [...]}

Datasette SQL execution response shapes (live-verified):
HTTP 200 success (with _shape=objects):
  {"ok": true, "rows": [{"col": val, ...}], "columns": ["col"], "truncated": false,
   "query_ms": 1.0, "error": null, "query": {...}}

HTTP 400 client error (typo, ms_limit hit):
  {"ok": false, "error": "no such table: nope", "rows": [], "columns": [],
   "truncated": false, "query_ms": 0.67}

Truncated success (1000+ rows):
  {"ok": true, "rows": [...1000 items...], "columns": [...],
   "truncated": true, "query_ms": 28.4, "error": null}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author routes_sql.py with /sql, GET /sql/{db}, POST /sql/{db} handlers</name>
  <read_first>
    - packages/zeeker-frontend/src/zeeker_frontend/routes_database.py:1-72 (GET handler shape — landing list iterates databases like /sql)
    - packages/zeeker-frontend/src/zeeker_frontend/routes_table.py:1-150 (querystring allowlist pattern — applies to /sql via _detect_params allowlist)
    - packages/zeeker-frontend/src/zeeker_frontend/datasette_client.py (Plan 02 — `execute_sql` signature + `_TABLE_ALLOWED_PARAMS`)
    - packages/zeeker-frontend/src/zeeker_frontend/routes_aux.py (Plan 03 — _hidden helper pattern)
    - packages/zeeker-frontend/src/zeeker_frontend/main.py (post Plan 04 — has aux_router + search_router; insertion point for sql_router is between search_router and database_router)
    - .planning/phases/06-port-auxiliary-pages/06-RESEARCH.md "Pattern 4" (POST + 400-handling) + "Pitfall 1" + "Pitfall 7" (allowlist) + "Code Examples"
    - .planning/phases/06-port-auxiliary-pages/06-PATTERNS.md "routes_sql.py" section (lines ~210-307)
    - .planning/phases/06-port-auxiliary-pages/06-UI-SPEC.md "/sql" + "/sql/{db}" sections + "Interaction Contracts"
  </read_first>
  <files>
    packages/zeeker-frontend/src/zeeker_frontend/routes_sql.py,
    packages/zeeker-frontend/src/zeeker_frontend/main.py
  </files>
  <action>
**Step 1 — Author packages/zeeker-frontend/src/zeeker_frontend/routes_sql.py.**

Full file content (RESEARCH §Pattern 4 + 06-PATTERNS.md routes_sql.py):

```
"""Phase 6 — /sql route family: landing list + per-database editor.

GET /sql        → list of databases (link-out to /sql/{db})
GET /sql/{db}   → empty editor (or pre-filled if ?sql=… provided); does NOT execute
POST /sql/{db}  → execute SQL via datasette_client.execute_sql; render results/error
"""
from __future__ import annotations

import re
from datetime import datetime

import httpx
from fastapi import APIRouter, Form, HTTPException, Request
from fastapi.responses import HTMLResponse

from zeeker_frontend.datasette_client import (
    execute_sql,
    fetch_database,
    fetch_databases,
    fetch_site_metadata,
)


router = APIRouter()

_CACHE_HEADER = "public, max-age=60, stale-while-revalidate=300"

# RESEARCH §Pattern 4 — :param_name detection in SQL string
_PARAM_RE = re.compile(r":([a-zA-Z_][a-zA-Z0-9_]*)")


def _detect_params(sql: str) -> list[str]:
    """Return param names in :name encounter order, deduped."""
    seen: set[str] = set()
    out: list[str] = []
    for m in _PARAM_RE.finditer(sql or ""):
        name = m.group(1)
        if name not in seen:
            seen.add(name)
            out.append(name)
    return out


def _hidden_db(entry: dict) -> bool:
    """Hide _zeeker_* dbs and any flagged hidden:true at db level."""
    if entry.get("hidden"):
        return True
    name = entry.get("name", "")
    return name.startswith("_zeeker")


def _get_canned_queries(site_metadata: dict, db: str) -> dict[str, dict]:
    """Defensive accessor for metadata.databases.{db}.queries (RESEARCH §Code Examples)."""
    return ((site_metadata.get("databases") or {}).get(db) or {}).get("queries") or {}


@router.get("/sql", response_class=HTMLResponse)
async def sql_landing(request: Request):
    client: httpx.AsyncClient = request.app.state.http
    try:
        dbs = await fetch_databases(client)
    except httpx.HTTPError:
        raise HTTPException(503, "Data API unavailable")

    visible = [d for d in dbs if not _hidden_db(d)]
    site_metadata = await fetch_site_metadata(client)

    # Decorate with title + table count for editorial-row rendering
    decorated: list[dict] = []
    for entry in visible:
        name = entry["name"]
        db_meta = (site_metadata.get("databases") or {}).get(name) or {}
        # tables_count: read /{db}.json once per db (cheap, cached upstream by 60s TTL on metadata only;
        # /{db}.json itself is not TTL-cached, but Caddy + browser are short-circuit caches).
        try:
            payload = await fetch_database(client, name)
        except httpx.HTTPError:
            payload = None
        tables_count = 0
        if payload:
            tables_count = sum(
                1 for t in (payload.get("tables") or [])
                if not (t.get("hidden") or t.get("name", "").startswith("_zeeker"))
            )
        decorated.append({
            "name": name,
            "title": db_meta.get("title") or name,
            "size": entry.get("size"),
            "tables_count": tables_count,
        })

    response = request.app.state.templates.TemplateResponse(
        request=request,
        name="pages/sql_landing.html",
        context={
            "databases": decorated,
            "page_class": "page-sql",
            "breadcrumbs": [{"label": "SQL"}],
            "current_year": datetime.now().year,
        },
    )
    response.headers["Cache-Control"] = _CACHE_HEADER
    return response


@router.get("/sql/{db}", response_class=HTMLResponse)
async def sql_db_get(request: Request, db: str):
    """Render the editor with optional ?sql=… pre-fill. Does NOT execute on GET."""
    client: httpx.AsyncClient = request.app.state.http
    try:
        payload = await fetch_database(client, db)
    except httpx.HTTPError:
        raise HTTPException(503, "Data API unavailable")
    if payload is None:
        raise HTTPException(404, "Database not found")

    site_metadata = await fetch_site_metadata(client)
    canned = _get_canned_queries(site_metadata, db)
    db_meta = (site_metadata.get("databases") or {}).get(db) or {}

    # Optional pre-fill from ?sql=…  (UI-SPEC §Open Questions Q1)
    sql = request.query_params.get("sql") or ""

    # First visible table for default placeholder text
    first_table = ""
    for t in payload.get("tables") or []:
        if t.get("hidden") or t.get("name", "").startswith("_zeeker"):
            continue
        first_table = t.get("name") or ""
        break

    response = request.app.state.templates.TemplateResponse(
        request=request,
        name="pages/sql_db.html",
        context={
            "database": db,
            "db_title": db_meta.get("title") or db,
            "sql": sql,
            "results": None,
            "error": None,
            "canned": canned,
            "detected_params": _detect_params(sql),
            "first_table": first_table,
            "page_class": "page-sql-db",
            "breadcrumbs": [{"label": "SQL", "url": "/sql"}, {"label": db}],
            "current_year": datetime.now().year,
        },
    )
    response.headers["Cache-Control"] = _CACHE_HEADER
    return response


@router.post("/sql/{db}", response_class=HTMLResponse)
async def sql_db_post(request: Request, db: str, sql: str = Form(...)):
    """Execute the submitted SQL via execute_sql. Render results, error, or truncation."""
    client: httpx.AsyncClient = request.app.state.http

    # Pitfall 7 — querystring allowlist. Read form, extract ONLY _sql_param_<name> keys
    # whose <name> matches the regex AND appears in _detect_params(sql).
    form = await request.form()
    raw_param_values: dict[str, str] = {
        k.removeprefix("_sql_param_"): str(v)
        for k, v in form.items()
        if isinstance(k, str) and k.startswith("_sql_param_")
        and _PARAM_RE.fullmatch(":" + k.removeprefix("_sql_param_"))
    }
    detected = _detect_params(sql)
    bound: dict[str, str] = {n: raw_param_values[n] for n in detected if n in raw_param_values}

    # execute_sql handles 400/404; returns (body, error)
    try:
        body, error = await execute_sql(client, db, sql, bound)
    except httpx.HTTPError:
        raise HTTPException(503, "Data API unavailable")

    if body is None and error == "Database not found":
        raise HTTPException(404, "Database not found")

    # Site metadata for canned-queries listing (re-render the form area)
    site_metadata = await fetch_site_metadata(client)
    canned = _get_canned_queries(site_metadata, db)
    db_meta = (site_metadata.get("databases") or {}).get(db) or {}

    response = request.app.state.templates.TemplateResponse(
        request=request,
        name="pages/sql_db.html",
        context={
            "database": db,
            "db_title": db_meta.get("title") or db,
            "sql": sql,
            "results": body,        # dict {rows, columns, truncated, query_ms} on success; else None
            "error": error,         # str on 400-error path; None on success
            "canned": canned,
            "detected_params": detected,
            "first_table": "",
            "page_class": "page-sql-db",
            "breadcrumbs": [{"label": "SQL", "url": "/sql"}, {"label": db}],
            "current_year": datetime.now().year,
        },
    )
    response.headers["Cache-Control"] = "no-store"  # D-14
    return response
```

Critical:
- The querystring allowlist on POST: ONLY `_sql_param_<name>` keys whose `<name>` matches
  `[a-zA-Z_][a-zA-Z0-9_]*` AND whose `<name>` appears in the parsed SQL. Everything else
  in `request.form()` is silently dropped. NEVER `params=request.form()`.
- 400-error path: `execute_sql` returns `(None, error_string)` — handler renders this
  as the inline `.sql-error` block with HTTP **200** (not 503).
- Cache-Control: POST = `no-store`; GET = standard cacheable header.

**Step 2 — Register sql_router in main.py.**

Add ONE import:
```
from zeeker_frontend.routes_sql import router as sql_router
```

Insert `app.include_router(sql_router)` AFTER `search_router` and BEFORE `database_router`:
```
app.include_router(home_router)
app.include_router(aux_router)        # Phase 6
app.include_router(search_router)     # Phase 6
app.include_router(sql_router)        # Phase 6 — must precede database_router (Pitfall 3)
app.include_router(database_router)
app.include_router(table_router)
app.include_router(row_router)
```

**Step 3 — Smoke run** (templates land in Task 2; this just confirms no syntax errors):
```
cd packages/zeeker-frontend && uv run python -c "from zeeker_frontend.main import app; print('OK')"
```
  </action>
  <verify>
    <automated>test -f packages/zeeker-frontend/src/zeeker_frontend/routes_sql.py &amp;&amp; grep -qE "@router\.get\(\"/sql\"" packages/zeeker-frontend/src/zeeker_frontend/routes_sql.py &amp;&amp; grep -qE "@router\.get\(\"/sql/\{db\}\"" packages/zeeker-frontend/src/zeeker_frontend/routes_sql.py &amp;&amp; grep -qE "@router\.post\(\"/sql/\{db\}\"" packages/zeeker-frontend/src/zeeker_frontend/routes_sql.py &amp;&amp; grep -qE '_PARAM_RE = re\.compile' packages/zeeker-frontend/src/zeeker_frontend/routes_sql.py &amp;&amp; grep -qE 'no-store' packages/zeeker-frontend/src/zeeker_frontend/routes_sql.py &amp;&amp; grep -qE "from zeeker_frontend\.routes_sql import router as sql_router" packages/zeeker-frontend/src/zeeker_frontend/main.py &amp;&amp; grep -qE "app\.include_router\(sql_router\)" packages/zeeker-frontend/src/zeeker_frontend/main.py &amp;&amp; cd packages/zeeker-frontend &amp;&amp; uv run python -c "from zeeker_frontend.main import app; print('OK')" | grep -q OK</automated>
  </verify>
  <acceptance_criteria>
    - `routes_sql.py` exists with 3 route decorators: `@router.get("/sql")`, `@router.get("/sql/{db}")`, `@router.post("/sql/{db}")`
    - `_PARAM_RE = re.compile(r":([a-zA-Z_][a-zA-Z0-9_]*)")` literal present
    - `_detect_params` function dedupes and preserves encounter order
    - POST handler builds `ds_params` via allowlist (NO `params=request.form()` literal)
    - POST handler sets `Cache-Control: no-store`
    - GET handlers set `Cache-Control: public, max-age=60, stale-while-revalidate=300`
    - `from zeeker_frontend.routes_sql import router as sql_router` in main.py
    - In main.py, `app.include_router(sql_router)` appears AFTER `search_router` and BEFORE `database_router`
    - `from zeeker_frontend.main import app` succeeds (no import errors)
  </acceptance_criteria>
  <done>routes_sql.py + main.py registration land. Templates + tests in subsequent tasks.</done>
</task>

<task type="auto">
  <name>Task 2: Author sql_landing.html + sql_db.html templates</name>
  <read_first>
    - packages/zeeker-frontend/src/zeeker_frontend/templates/base.html (the shell)
    - packages/zeeker-frontend/src/zeeker_frontend/templates/database.html:114-158 (editorial-row .list pattern — sql_landing reuses this)
    - packages/zeeker-frontend/src/zeeker_frontend/templates/pages/about.html (per Plan 03 — italic-accent H1 reference)
    - .planning/phases/06-port-auxiliary-pages/06-UI-SPEC.md "/sql" + "/sql/{db}" sections (full)
    - .planning/phases/06-port-auxiliary-pages/06-PATTERNS.md "templates/pages/sql_index.html and sql_db.html" + "Code Examples" (canned-query <details>)
    - .planning/phases/06-port-auxiliary-pages/06-RESEARCH.md "Pattern 4" (template-side context dict)
  </read_first>
  <files>
    packages/zeeker-frontend/src/zeeker_frontend/templates/pages/sql_landing.html,
    packages/zeeker-frontend/src/zeeker_frontend/templates/pages/sql_db.html
  </files>
  <action>
**Step 1 — Author packages/zeeker-frontend/src/zeeker_frontend/templates/pages/sql_landing.html.**

```
{% extends "base.html" %}

{% block content %}
<div class="container">
  <header class="guide-hero">
    <div class="kicker">№ 01 · SQL</div>
    <h1>Run <em>SQL</em></h1>
    <p class="lede">Pick a database to write a query against.</p>
  </header>

  <section class="aux-card">
    <h2>Databases</h2>
    {% if databases %}
      <div class="list sql-db-list">
        {% for db in databases %}
        <div class="row">
          <div class="idx">{{ "{:02d}".format(loop.index) }}</div>
          <div class="name-col">
            <a href="/sql/{{ db.name }}" class="name">{{ db.title }}</a>
          </div>
          <div class="cols"></div>
          <div class="count-col">
            <span class="count">{{ db.tables_count }}</span>
            <span class="label">tables</span>
          </div>
        </div>
        {% endfor %}
      </div>
    {% else %}
      <p class="empty-state">No databases available.</p>
    {% endif %}
  </section>

  <p class="footer-note">Need API docs instead? See <a href="/developers">/developers</a>.</p>
</div>
{% endblock %}
```

**Step 2 — Author packages/zeeker-frontend/src/zeeker_frontend/templates/pages/sql_db.html.**

```
{% extends "base.html" %}

{% block content %}
<div class="container">
  <header class="guide-hero guide-hero-compact">
    <div class="kicker">SQL · {{ database }}</div>
    <h1>SQL · <em>{{ db_title }}</em></h1>
    <p class="lede">Read-only queries against {{ database }}. Time limit: 3 seconds. Row cap: 1,000.</p>
  </header>

  {% if canned %}
  <details class="canned-queries aux-card">
    <summary>Saved queries ({{ canned|length }})</summary>
    <ul>
      {% for name, q in canned.items() %}
      <li>
        <button type="button" class="canned-query" data-sql="{{ q.sql }}">
          {{ q.title or name }}
        </button>
      </li>
      {% endfor %}
    </ul>
  </details>
  {% endif %}

  <form class="sql-form aux-card" method="POST" action="/sql/{{ database }}">
    {% if detected_params %}
    <div class="sql-param-row">
      {% for name in detected_params %}
        <label>
          {{ name }}
          <input type="text" name="_sql_param_{{ name }}" placeholder="{{ name }}" value="">
        </label>
      {% endfor %}
    </div>
    {% endif %}

    <textarea name="sql" rows="8" class="sql-textarea" placeholder="{% if first_table %}SELECT * FROM {{ first_table }} LIMIT 10{% endif %}">{{ sql }}</textarea>

    <div class="sql-actions">
      <button type="submit" class="btn-primary">Run</button>
      <a href="/sql/{{ database }}" class="btn-ghost">Reset</a>
    </div>
  </form>

  {% if error %}
  <section class="aux-card sql-error">
    <h2>Query error</h2>
    <pre>{{ error }}</pre>
    <p class="hint">Common causes: typo in column name · table needs square brackets · syntax error</p>
  </section>
  {% endif %}

  {% if results %}
  <section class="aux-card sql-results">
    <p class="sql-results-meta">
      {{ results.rows|length }} {{ results.rows|length|pluralize('row') }}
      {% if results.query_ms %} · {{ "%.1f"|format(results.query_ms) }} ms{% endif %}
    </p>

    {% if results.truncated %}
    <p class="sql-truncation">
      Showing {{ results.rows|length }} rows (the maximum).
      <a href="/{{ database }}.csv?sql={{ sql|urlencode }}">Download all matching rows as CSV ↗</a>
    </p>
    {% endif %}

    <div class="sql-results-wrap">
      <table class="sql-results-table">
        <thead>
          <tr>{% for col in results.columns %}<th>{{ col }}</th>{% endfor %}</tr>
        </thead>
        <tbody>
          {% for row in results.rows %}
          <tr>
            {% for col in results.columns %}
              <td>{{ row[col] if row[col] is not none else "" }}</td>
            {% endfor %}
          </tr>
          {% endfor %}
        </tbody>
      </table>
    </div>

    <p class="sql-export-row">
      <a href="/{{ database }}.csv?sql={{ sql|urlencode }}">Download CSV ↗</a>
      ·
      <a href="/{{ database }}.json?sql={{ sql|urlencode }}">View JSON ↗</a>
    </p>
  </section>
  {% endif %}

  {% if results and not results.rows %}
  <section class="aux-card sql-empty-results">
    <p>Query ran successfully. 0 rows returned.</p>
  </section>
  {% endif %}
</div>
{% endblock %}
```

Critical:
- `{{ sql }}` inside `<textarea>` is autoescaped (Jinja default for .html) — protects from
  XSS even though the user submitted it.
- Export anchors `<a href="/{db}.csv?sql={{ sql|urlencode }}">` are URL-encoded; Caddy
  suffix matcher routes `.csv` direct to datasette (D-08).
- `data-sql="{{ q.sql }}"` is autoescaped — safe to render in HTML attribute (Jinja
  escapes quotes).

**Step 3 — Smoke test both templates parse:**
```
cd packages/zeeker-frontend && uv run python -c "
from jinja2 import Environment, FileSystemLoader
env = Environment(loader=FileSystemLoader('src/zeeker_frontend/templates'), autoescape=True)
env.get_template('pages/sql_landing.html')
env.get_template('pages/sql_db.html')
print('OK')
"
```
  </action>
  <verify>
    <automated>test -f packages/zeeker-frontend/src/zeeker_frontend/templates/pages/sql_landing.html &amp;&amp; test -f packages/zeeker-frontend/src/zeeker_frontend/templates/pages/sql_db.html &amp;&amp; grep -qE 'extends "base\.html"' packages/zeeker-frontend/src/zeeker_frontend/templates/pages/sql_landing.html &amp;&amp; grep -qE 'extends "base\.html"' packages/zeeker-frontend/src/zeeker_frontend/templates/pages/sql_db.html &amp;&amp; grep -qE '&lt;h1&gt;Run &lt;em&gt;SQL&lt;/em&gt;&lt;/h1&gt;' packages/zeeker-frontend/src/zeeker_frontend/templates/pages/sql_landing.html &amp;&amp; grep -qE '&lt;h1&gt;SQL · &lt;em&gt;' packages/zeeker-frontend/src/zeeker_frontend/templates/pages/sql_db.html &amp;&amp; grep -qE 'method="POST"' packages/zeeker-frontend/src/zeeker_frontend/templates/pages/sql_db.html &amp;&amp; grep -qE 'sql\|urlencode' packages/zeeker-frontend/src/zeeker_frontend/templates/pages/sql_db.html &amp;&amp; cd packages/zeeker-frontend &amp;&amp; uv run python -c "from jinja2 import Environment, FileSystemLoader; env=Environment(loader=FileSystemLoader('src/zeeker_frontend/templates'), autoescape=True); env.get_template('pages/sql_landing.html'); env.get_template('pages/sql_db.html'); print('OK')" | grep -q OK</automated>
  </verify>
  <acceptance_criteria>
    - `templates/pages/sql_landing.html` exists; extends `base.html`; H1 = `Run <em>SQL</em>`
    - `templates/pages/sql_db.html` exists; extends `base.html`; H1 = `SQL · <em>{{ db_title }}</em>`
    - sql_db.html has `method="POST"` form
    - sql_db.html uses `sql|urlencode` in export anchors (CSV + JSON deep-links)
    - sql_db.html has `<details class="canned-queries">` block (gated on `{% if canned %}`)
    - sql_db.html has `.sql-error` block (gated on `{% if error %}`)
    - sql_db.html has `.sql-truncation` block (gated on `{% if results.truncated %}`)
    - Jinja parses both templates (smoke command outputs `OK`)
  </acceptance_criteria>
  <done>2 templates parse cleanly; render landing page + per-db editor with all states.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fill in test_routes_sql.py with REAL assertions</name>
  <read_first>
    - packages/zeeker-frontend/tests/test_routes_table.py:1-200 (POST/GET integration test pattern)
    - packages/zeeker-frontend/tests/test_routes_aux.py (Plan 03 — fixture pattern)
    - packages/zeeker-frontend/tests/fixtures/metadata_with_canned_queries.json (Plan 01)
    - packages/zeeker-frontend/tests/fixtures/sql_error_400.json (Plan 01)
    - packages/zeeker-frontend/src/zeeker_frontend/routes_sql.py (post-Task-1 — _detect_params, _PARAM_RE)
    - .planning/phases/06-port-auxiliary-pages/06-PATTERNS.md "tests/test_routes_sql.py" section (lines ~963-986)
    - .planning/phases/06-port-auxiliary-pages/06-VALIDATION.md "Phase Requirements → Test Map" — test_sql_* rows
  </read_first>
  <behavior>
    Required tests (replace stubs):
    - test_sql_landing: GET /sql → 200, body lists databases (loops via .row pattern)
    - test_sql_db_get: GET /sql/sglawwatch → 200, contains <textarea>, contains canned queries section if metadata has them
    - test_sql_db_get_404: GET /sql/nonexistent → 404
    - test_sql_db_post_success: POST /sql/sglawwatch with valid SQL → 200, results table rendered, Cache-Control: no-store
    - test_sql_db_post_400_error: POST /sql/sglawwatch with bad SQL → 200 (NOT 503), .sql-error block + error message in body
    - test_sql_db_truncation_banner: POST → results with `truncated: true` → banner present, CSV deep-link present
    - test_sql_db_export_links: POST success → response body contains `/{db}.csv?sql=` URL-encoded + `/{db}.json?sql=` URL-encoded
    - test_sql_db_post_param_binding: POST with `_sql_param_id=42` and SQL `SELECT :id` → captured `_param_id=42` in upstream call
    - test_sql_db_post_drops_extra_form_fields: POST with extra form keys (e.g., `extra=evil`) → `extra` NOT forwarded to upstream
    - test_detect_params_regex: unit test of _detect_params (dedupe, encounter order)
  </behavior>
  <files>
    packages/zeeker-frontend/tests/test_routes_sql.py
  </files>
  <action>
Replace EVERY `pytest.skip(...)` body with real assertions:

```
"""Phase 6 — /sql + /sql/{db} route integration tests (Plan 05)."""
from __future__ import annotations

import json
import re
from pathlib import Path

import httpx
import pytest

from zeeker_frontend.main import app
from zeeker_frontend.routes_sql import _detect_params


_FIXTURES = Path(__file__).parent / "fixtures"
_ITALIC_H1 = re.compile(r"<h1>.*?<em[^>]*>[^<]+</em>.*?</h1>", re.DOTALL)


def _load(name: str):
    return json.loads((_FIXTURES / name).read_text())


def _mock_factory(*, success_response: dict | None = None,
                  error_400_response: dict | None = None,
                  capture: dict | None = None):
    """Return AsyncClient that:
    - GET /-/databases.json → list of dbs (from sglawwatch.json fixture stub)
    - GET /sglawwatch.json (no sql) → table list
    - GET /sglawwatch.json?sql=…:
        if error_400_response set → 400 with that body
        else → 200 with success_response
    - GET /-/metadata.json → canned-queries fixture
    `capture` (if provided) records the last upstream params under capture['params'].
    """
    sglawwatch = _load("sglawwatch.json")
    metadata = _load("metadata_with_canned_queries.json")

    def handler(request: httpx.Request) -> httpx.Response:
        path = request.url.path
        params = dict(request.url.params)
        if capture is not None:
            capture["last_path"] = path
            capture["last_params"] = params
        if path == "/-/databases.json":
            return httpx.Response(200, json=[{"name": "sglawwatch", "size": 100, "is_mutable": False, "hash": "x"}])
        if path == "/-/metadata.json":
            return httpx.Response(200, json=metadata)
        if path == "/sglawwatch.json":
            if "sql" in params:
                if error_400_response is not None:
                    return httpx.Response(400, json=error_400_response)
                return httpx.Response(200, json=success_response or {"ok": True, "rows": [{"a": 1}], "columns": ["a"], "truncated": False, "query_ms": 1.0, "error": None})
            return httpx.Response(200, json=sglawwatch)
        return httpx.Response(404, json={"ok": False, "error": "not found"})

    return httpx.AsyncClient(
        base_url="http://zeeker-datasette:8001",
        transport=httpx.MockTransport(handler),
    )


@pytest.fixture
async def client_sql():
    capture: dict = {}
    app.state.http = _mock_factory(capture=capture)
    app.state.searchable_tables = {}
    app.state.changelog = []
    app.state._sql_capture = capture
    try:
        async with httpx.AsyncClient(
            transport=httpx.ASGITransport(app=app), base_url="http://testserver",
        ) as ac:
            yield ac
    finally:
        await app.state.http.aclose()


@pytest.fixture
async def client_sql_400():
    err = _load("sql_error_400.json")
    app.state.http = _mock_factory(error_400_response=err)
    app.state.searchable_tables = {}
    app.state.changelog = []
    try:
        async with httpx.AsyncClient(
            transport=httpx.ASGITransport(app=app), base_url="http://testserver",
        ) as ac:
            yield ac
    finally:
        await app.state.http.aclose()


@pytest.fixture
async def client_sql_truncated():
    truncated = {
        "ok": True,
        "rows": [{"id": i, "title": f"row {i}"} for i in range(10)],
        "columns": ["id", "title"],
        "truncated": True,
        "query_ms": 28.4,
        "error": None,
    }
    app.state.http = _mock_factory(success_response=truncated)
    app.state.searchable_tables = {}
    app.state.changelog = []
    try:
        async with httpx.AsyncClient(
            transport=httpx.ASGITransport(app=app), base_url="http://testserver",
        ) as ac:
            yield ac
    finally:
        await app.state.http.aclose()


# --- _detect_params unit test ---

def test_detect_params_regex():
    assert _detect_params("SELECT * FROM t WHERE id = :id") == ["id"]
    # Dedupe + preserve encounter order
    assert _detect_params("SELECT :a, :b, :a FROM t WHERE x = :b") == ["a", "b"]
    # No match for invalid name (starts with digit)
    assert _detect_params("SELECT :1bad") == []
    assert _detect_params("") == []
    assert _detect_params("SELECT 1") == []


# --- /sql landing tests ---

@pytest.mark.asyncio
async def test_sql_landing(client_sql):
    r = await client_sql.get("/sql")
    assert r.status_code == 200
    assert _ITALIC_H1.search(r.text)
    assert "Run" in r.text and "<em>SQL</em>" in r.text
    assert "/sql/sglawwatch" in r.text
    cc = r.headers.get("cache-control", "")
    assert "max-age=60" in cc and "stale-while-revalidate=300" in cc


# --- GET /sql/{db} tests ---

@pytest.mark.asyncio
async def test_sql_db_get(client_sql):
    r = await client_sql.get("/sql/sglawwatch")
    assert r.status_code == 200
    assert _ITALIC_H1.search(r.text)
    assert "<textarea" in r.text
    # Canned queries section present (fixture has at least one)
    assert "Saved queries" in r.text or "canned-query" in r.text


@pytest.mark.asyncio
async def test_sql_db_get_404(client_sql):
    r = await client_sql.get("/sql/nonexistent")
    assert r.status_code == 404


# --- POST /sql/{db} tests ---

@pytest.mark.asyncio
async def test_sql_db_post_success(client_sql):
    r = await client_sql.post("/sql/sglawwatch", data={"sql": "SELECT 1 as a"})
    assert r.status_code == 200
    assert "<table" in r.text
    cc = r.headers.get("cache-control", "")
    assert "no-store" in cc


@pytest.mark.asyncio
async def test_sql_db_post_400_error(client_sql_400):
    """T-06-05-03 — Datasette 400 with error body must NOT 503; render inline."""
    r = await client_sql_400.post("/sql/sglawwatch", data={"sql": "SELECT * FROM nope"})
    assert r.status_code == 200, f"expected 200 with .sql-error block, got {r.status_code}"
    assert "no such table" in r.text or "Query error" in r.text


@pytest.mark.asyncio
async def test_sql_db_truncation_banner(client_sql_truncated):
    r = await client_sql_truncated.post("/sql/sglawwatch", data={"sql": "SELECT * FROM headlines"})
    assert r.status_code == 200
    assert "Showing" in r.text or "maximum" in r.text or "truncation" in r.text.lower()
    assert ".csv?sql=" in r.text  # CSV deep-link present


@pytest.mark.asyncio
async def test_sql_db_export_links(client_sql):
    r = await client_sql.post("/sql/sglawwatch", data={"sql": "SELECT 1"})
    assert r.status_code == 200
    # URL-encoded SQL in export anchors
    assert "/sglawwatch.csv?sql=" in r.text
    assert "/sglawwatch.json?sql=" in r.text


@pytest.mark.asyncio
async def test_sql_db_post_param_binding(client_sql):
    """T-06-05-01 — :name params bound via _param_<name> URL key."""
    r = await client_sql.post(
        "/sql/sglawwatch",
        data={"sql": "SELECT * FROM t WHERE id = :id", "_sql_param_id": "42"},
    )
    assert r.status_code == 200
    capture = app.state._sql_capture
    assert capture["last_params"].get("_param_id") == "42"
    assert capture["last_params"].get("sql") == "SELECT * FROM t WHERE id = :id"
    # _shape always present
    assert capture["last_params"].get("_shape") == "objects"


@pytest.mark.asyncio
async def test_sql_db_post_drops_extra_form_fields(client_sql):
    """T-06-05-02 — querystring smuggling prevention.

    Fields outside the allowlist (sql, _sql_param_<valid_name>) MUST be dropped.
    """
    r = await client_sql.post(
        "/sql/sglawwatch",
        data={
            "sql": "SELECT 1",
            "extra": "evil",
            "allow_execute_sql": "true",
            "_sql_param_id": "ok",  # but :id NOT in SQL → still dropped per detected_params filter
        },
    )
    assert r.status_code == 200
    capture = app.state._sql_capture
    assert "extra" not in capture["last_params"]
    assert "allow_execute_sql" not in capture["last_params"]
    # _param_id should NOT be present because :id not in the SQL string
    assert "_param_id" not in capture["last_params"]
```

Run:
```
cd packages/zeeker-frontend && uv run pytest tests/test_routes_sql.py -v
```
All 10 tests must pass. Then full suite:
```
cd packages/zeeker-frontend && uv run pytest -x
```
  </action>
  <verify>
    <automated>cd packages/zeeker-frontend &amp;&amp; uv run pytest tests/test_routes_sql.py -v 2&gt;&amp;1 | tee /tmp/p06-05-t3.log &amp;&amp; ! grep -q "skipped" /tmp/p06-05-t3.log &amp;&amp; grep -E "passed" /tmp/p06-05-t3.log &amp;&amp; uv run pytest -x 2&gt;&amp;1 | tail -3 | grep -E "passed"</automated>
  </verify>
  <acceptance_criteria>
    - `cd packages/zeeker-frontend && uv run pytest tests/test_routes_sql.py -v` reports >= 10 PASSED, 0 skipped, 0 failed
    - `cd packages/zeeker-frontend && uv run pytest -x` (full suite including Phase 4-5 + Plans 01-04) exits 0
    - test_sql_db_post_400_error returns 200 (not 503) — proves Pitfall 1 mitigation
    - test_sql_db_post_param_binding captures `_param_id=42` upstream — proves param binding (T-06-05-01)
    - test_sql_db_post_drops_extra_form_fields shows `extra` NOT in upstream params — proves allowlist (T-06-05-02)
    - test_sql_db_truncation_banner shows banner + CSV link present (T-06-05-04 mitigation)
    - test_detect_params_regex covers dedupe + invalid-name rejection
  </acceptance_criteria>
  <done>10 SQL-route tests pass; SQL injection prevention + 400-handling + truncation + allowlist invariants verified.</done>
</task>

</tasks>

<verification>
1. `cd packages/zeeker-frontend && uv run pytest tests/test_routes_sql.py -v` → 10 passed, 0 skipped
2. `cd packages/zeeker-frontend && uv run pytest -x` → full suite green
3. `routes_sql.py` has 3 route decorators (GET /sql, GET /sql/{db}, POST /sql/{db})
4. `_PARAM_RE = re.compile(r":([a-zA-Z_][a-zA-Z0-9_]*)")` literal present
5. POST handler builds `ds_params` allowlist (no `params=request.form()`)
6. main.py registers `sql_router` AFTER `search_router` and BEFORE `database_router`
7. sql_db.html has italic-accent H1, POST form, truncation block, error block
</verification>

<success_criteria>
- /sql landing lists databases via editorial-row pattern
- GET /sql/{db} renders empty editor + canned queries (when present)
- POST /sql/{db} executes via execute_sql, renders results/error/truncation correctly
- 400 errors render inline (HTTP 200 + .sql-error), NOT 503
- Param-binding via `_param_<name>` URL keys; querystring allowlist enforces SSRF prevention
- Cache-Control: no-store on POST
- 10 integration tests pass; SQL injection + allowlist + 400-rendering all verified
- File-disjoint with Plan 04 — both run parallel in Wave 2
</success_criteria>

<output>
After completion, create `.planning/phases/06-port-auxiliary-pages/06-05-SUMMARY.md` documenting:
- 3 handler signatures + Cache-Control assignment per method
- Critical invariants: param binding via _param_<name>, allowlist on form data, 400-handled-as-200
- _detect_params regex semantics (dedupe + encounter order)
- main.py final router order: home_router → aux_router → search_router → sql_router → database_router → table_router → row_router
- 10 integration tests + key SQL injection / allowlist / 400-handling / truncation assertions
</output>
