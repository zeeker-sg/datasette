---
phase: 05-port-table-browse-row-view
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - packages/zeeker-frontend/src/zeeker_frontend/static/css/zeeker.css
autonomous: true
requirements_addressed:
  - REQ-frontend-route-set
  - REQ-eliminate-template-drift
threat_model:
  - id: T-05-04
    severity: low
    surface: CSS escape via dataset content (background-image url, content quotes)
    mitigation: "Phase 5 adds NO content/url() based on dataset values; all background colors use CSS custom properties from :root; safe by construction"
  - id: T-05-06
    severity: low
    surface: CSS-cache poisoning via Vary header
    mitigation: "not applicable — zeeker.css is a static asset served by FastAPI StaticFiles; standard Last-Modified/ETag headers apply; no user-input-keyed content"

must_haves:
  truths:
    - "zeeker.css contains a single new section delimited '/* =========== TABLE BROWSE + ROW VIEW — phase 05 ============ */'"
    - "FOOTER LINK OVERRIDE block remains at TAIL of file (verifier-load-bearing per WARN-05)"
    - "ACCESSIBILITY UTILITIES block remains at TAIL of file"
    - "All Phase 5 class names from UI-SPEC §Component Inventory have at least one rule definition: .feed-layout, .va-feed, .va-item, .va-item-head, .va-item-title, .va-item-excerpt, .va-item-foot, .source-host, .va-empty, .va-citation, .va-item-wrap, .facets, .facet-block, .facet-list, .facet-item, .facet-item.active, .facet-block-title, .filter-chip, .filter-chip .x, .filter-chips, .filter-chip-search, .pagination, .pagination-nav, .pagination-next, .pagination-size, .pagination-size-current, .data-table, .data-table thead th, .data-table tbody tr, .data-table th.sorted, .article, .read, .article-body, .article-grid, .aside, .aside-block, .dateline, .dateline-agency, .dateline-date, .judgment-meta, .tag-chip, .tag-chips, .coda, .coda-label, .coda-fingerprint, .back-link, .empty-state, .row-tabular, .row-tabular-grid, .row-dl, .row-dd-long, .row-dd-preview, .longform-article, .judgment-body, .row-header, .db-toolbar-export, .row-section"
    - "Article body has Fraunces opsz 11, font-size 1.125rem, line-height 1.65, max-width 62ch (UI-SPEC Typography)"
    - "Drop cap rule: .article-body p:first-of-type::first-letter (article + longform-article)"
    - "Tabular .data-table thead th has clickable sort-link styling and an .sorted modifier"
    - "Pagination links use mono --text-xs"
    - "All values reference existing CSS custom properties from :root (no new tokens introduced)"
    - "balanced braces: every { has a matching } in the appended section"
    - "no .article rule conflicts with the .row-section .article distinction (article inside .row-section is a contextual scope)"
  artifacts:
    - path: "packages/zeeker-frontend/src/zeeker_frontend/static/css/zeeker.css"
      provides: "Appended TABLE BROWSE + ROW VIEW phase 05 CSS section"
      contains: "/* =========== TABLE BROWSE + ROW VIEW — phase 05 ============ */"
      min_lines: 1400
  key_links:
    - from: "packages/zeeker-frontend/src/zeeker_frontend/static/css/zeeker.css"
      to: "packages/zeeker-frontend/src/zeeker_frontend/templates/_partials/*.html (Plans 05-02 + 05-03)"
      via: "matching CSS class names"
      pattern: "\\.va-item\\b|\\.facet-block\\b|\\.data-table\\b|\\.article\\b|\\.aside\\b|\\.dateline\\b|\\.tag-chip\\b|\\.pagination\\b|\\.filter-chip\\b"
---

<objective>
Append the locked civic-broadsheet CSS section for the Phase 5 surfaces (table feed cards, facet sidebar, applied-facet chips, pagination, tabular grid, article reading layout, judgment broadsheet, longform reading, tabular fallback) to `packages/zeeker-frontend/src/zeeker_frontend/static/css/zeeker.css`. The CSS lands BEFORE the existing `HARVESTED FROM M1 zeeker-base.css LINES 4097..4116` comment block so the FOOTER LINK OVERRIDE rule remains at file tail (Phase-4 WARN-05 — wins specificity against any later cascade).

Purpose: Make the templates from Plans 05-02 and 05-03 actually render with the locked design contract — paper background, petrol/ochre/terracotta accents, Fraunces display, Inter UI, JetBrains Mono meta, italic-accent H1, sticky facet sidebar, drop-cap article body, tabular-grid sortable headers. Every class name and every visual decision sources from `sketch-findings-zeeker-datasette` (LOCKED) and is cited inline. NO new CSS variables (existing `:root` token table at zeeker.css:85-135 is the closed system).

Output: A single CSS file modification (append a single ~390-line section). All other Phase-4 rules untouched. File grows from 1102 lines to ~1490 lines.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-port-table-browse-row-view/05-RESEARCH.md
@.planning/phases/05-port-table-browse-row-view/05-UI-SPEC.md
@.planning/phases/05-port-table-browse-row-view/05-PATTERNS.md
@.claude/skills/sketch-findings-zeeker-datasette/SKILL.md

<sketch_findings_anchors>
<!-- Authoritative for every visual decision below. -->
- Palette + spacing + typography tokens: sketch-findings-zeeker-datasette/references/theme-system.md
- Feed cards (.va-item / .va-feed / .va-item-head / .va-item-title / .va-item-excerpt / .va-item-foot / .source-host): sketch-findings-zeeker-datasette/references/table-as-news-archive.md (sketch 004-A)
- Facet sidebar (.facets / .facet-block / .facet-item / .facet-item.active): sketch-findings-zeeker-datasette/references/directory-and-feed-lists.md
- Article reading layout (.article / .read / .article-body / drop cap / .aside / .aside-block / .coda): sketch-findings-zeeker-datasette/references/row-reading-layouts.md
- Judgment broadsheet (.dateline / .tag-chip / .coda double-rule): sketch-findings-zeeker-datasette/references/row-reading-layouts.md (sketch 003-B)
- Tabular grid (.data-table / sortable headers): sketch-findings-zeeker-datasette/references/database-table-grid.md (sketch 002-B)
- Shell / toolbar / breadcrumb / hero (already in Phase-4 CSS — do NOT redefine)
</sketch_findings_anchors>

<existing_token_table>
<!-- The closed system. NO new tokens. Every value below references one of these. -->
From zeeker.css :root (lines 85-135 — Phase 4):
- Colors: --color-bg, --color-bg-alt, --color-surface, --color-surface-sunken, --color-ink, --color-accent, --color-ochre, --color-terracotta, --color-text-secondary, --color-text-muted, --color-border
- Spacing: --space-1 (4) through --space-20 (80)
- Type sizes: --text-2xs, --text-xs, --text-sm, --text-base, --text-lg, --text-xl, --text-2xl, --text-3xl, --text-4xl, --text-5xl, --text-6xl
- Fonts: --font-display (Fraunces), --font-body (Inter), --font-mono (JetBrains Mono)
- Letter-spacing: --tracking-tight, --tracking-normal, --tracking-wide, --tracking-widest
- Line-heights: --leading-tight, --leading-normal, --leading-loose
- Shadows: --shadow-sm, --shadow-md (if defined)
- Radii: --radius-sm, --radius-md
</existing_token_table>

<insertion_anchor>
<!-- Existing zeeker.css TAIL structure to preserve verbatim. -->
Lines ~1077-1102 (current Phase-4 tail):
```
/* =======================================================
   HARVESTED FROM M1 zeeker-base.css LINES 4097..4116
   Tail footer link override
   ======================================================= */
 *     idx column ("01", "02") gets breathing room from the container edge —
 *     especially visible on the database page table list. */

/* ----------------------------------------------------------------
 * FOOTER LINK OVERRIDE — must remain at TAIL of file to win cascade
 * against Datasette's /-/static/app.css `footer a:link` rule.
 * See .planning/notes/datasette-styling-limits.md WARN-05.
 * ---------------------------------------------------------------- */
footer a:link, footer a:visited, footer a:active { ... }
footer a:hover, footer a:focus { ... }

/* =======================================================
   ACCESSIBILITY UTILITIES
   ======================================================= */
.visually-hidden{ ... }
```

INSERTION POINT — insert the new section IMMEDIATELY BEFORE the `HARVESTED FROM M1 zeeker-base.css LINES 4097..4116` comment block (i.e. after line ~1076 of current zeeker.css). The footer override + accessibility-utilities blocks must remain physically last in the file.
</insertion_anchor>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append phase-05 CSS section to zeeker.css</name>
  <files>packages/zeeker-frontend/src/zeeker_frontend/static/css/zeeker.css (MODIFY — append section)</files>
  <read_first>
    - packages/zeeker-frontend/src/zeeker_frontend/static/css/zeeker.css (full file — confirm `:root` token names, locate insertion anchor, confirm FOOTER LINK OVERRIDE block is at TAIL)
    - .planning/phases/05-port-table-browse-row-view/05-RESEARCH.md §M1 CSS Harvest Plan + §Section delimiter convention (lines 497-543)
    - .planning/phases/05-port-table-browse-row-view/05-UI-SPEC.md §Component Inventory + §Color + §Typography + §Spacing Scale
    - .claude/skills/sketch-findings-zeeker-datasette/references/theme-system.md (LOCKED palette + scale)
    - .claude/skills/sketch-findings-zeeker-datasette/references/table-as-news-archive.md
    - .claude/skills/sketch-findings-zeeker-datasette/references/directory-and-feed-lists.md
    - .claude/skills/sketch-findings-zeeker-datasette/references/row-reading-layouts.md
    - .claude/skills/sketch-findings-zeeker-datasette/references/database-table-grid.md
    - templates/_table-sg-gov-newsrooms-mlaw_news.html (M1 reference — slot mappings only; do NOT search for CSS in it, the M1 CSS lives in static/css/zeeker-base.css)
    - static/css/zeeker-base.css lines 3864-3984 (M1 feed-cards CSS — direct-copy source per RESEARCH §M1 CSS Harvest Plan)
  </read_first>
  <action>
    The CSS section comprises ~390 lines: ~120 directly-copied from M1 (feed cards), ~270 newly authored from sketch references. Use the EXACT class names from Plans 05-02 and 05-03 templates (no aliasing).

    Step 1 — Locate the insertion anchor in `packages/zeeker-frontend/src/zeeker_frontend/static/css/zeeker.css`. Use grep:
    ```bash
    grep -n "HARVESTED FROM M1 zeeker-base.css LINES 4097..4116" packages/zeeker-frontend/src/zeeker_frontend/static/css/zeeker.css
    ```
    The match line marks the start of the FOOTER OVERRIDE comment block; insert NEW content IMMEDIATELY ABOVE this line.

    Step 2 — Append the new section (use Edit tool with the line above the matched line as the anchor):

    ```css
    /* =========== TABLE BROWSE + ROW VIEW — phase 05 ============ */
    /*
     * Source-of-truth: sketch-findings-zeeker-datasette skill (LOCKED).
     *  - Feed cards: references/table-as-news-archive.md (sketch 004-A)
     *  - Facet sidebar: references/directory-and-feed-lists.md
     *  - Article / aside / coda: references/row-reading-layouts.md (sketch 003-A)
     *  - Judgment / dateline / tag-chip: references/row-reading-layouts.md (sketch 003-B)
     *  - Tabular grid: references/database-table-grid.md (sketch 002-B)
     * Every value below references an existing :root token (no new tokens).
     */

    /* ---- Two-column feed layout: main + sticky facet sidebar ---- */
    .feed-layout {
      display: grid;
      grid-template-columns: minmax(0, 1fr) 240px;
      gap: var(--space-12);
      align-items: start;
    }
    .feed-main { min-width: 0; }
    @media (max-width: 900px) {
      .feed-layout { grid-template-columns: 1fr; }
    }

    /* ---- Toolbar export anchors (right side of sticky toolbar) ---- */
    .db-toolbar-export {
      margin-left: auto;
      display: inline-flex;
      gap: var(--space-3);
      font-family: var(--font-mono);
      font-size: var(--text-xs);
    }
    .db-toolbar-export a {
      color: var(--color-text-secondary);
      text-decoration: none;
    }
    .db-toolbar-export a:hover { color: var(--color-accent); }

    /* ---- Applied-facet chips (above content row) ---- */
    .filter-chips {
      display: flex;
      flex-wrap: wrap;
      gap: var(--space-2);
      margin: var(--space-3) 0;
    }
    .filter-chip {
      display: inline-flex;
      align-items: center;
      gap: var(--space-2);
      padding: var(--space-1) var(--space-3);
      background: var(--color-accent);
      color: var(--color-bg);
      font-family: var(--font-body);
      font-size: var(--text-sm);
      border-radius: var(--radius-sm, 2px);
      text-decoration: none;
      line-height: 1.6;
      min-height: 24px;
    }
    .filter-chip strong { font-weight: 600; }
    .filter-chip .x {
      display: inline-block;
      min-width: 20px;
      text-align: center;
      font-weight: 600;
    }
    .filter-chip:hover { opacity: 0.9; }
    .filter-chip-search {
      background: var(--color-ochre);
      color: var(--color-ink);
    }

    /* ---- Facet sidebar (sticky right rail) ---- */
    .facets {
      position: sticky;
      top: 120px;        /* nav (52px) + toolbar (~68px) */
      align-self: start;
      max-height: calc(100vh - 140px);
      overflow-y: auto;
      padding: 0 var(--space-2) var(--space-4) var(--space-2);
    }
    .facet-block {
      padding-block: var(--space-5);
      border-top: 1px solid var(--color-border);
    }
    .facet-block:first-child {
      border-top: 2px solid var(--color-accent);
    }
    .facet-block-title {
      font-family: var(--font-mono);
      font-size: var(--text-2xs, 0.6875rem);
      letter-spacing: var(--tracking-widest);
      text-transform: uppercase;
      color: var(--color-text-muted);
      margin: 0 0 var(--space-3) 0;
      font-weight: 600;
    }
    .facet-list {
      list-style: none;
      padding: 0;
      margin: 0;
    }
    .facet-item {
      display: block;
      transition: background 0.15s ease;
    }
    .facet-item a {
      display: flex;
      align-items: baseline;
      justify-content: space-between;
      gap: var(--space-3);
      padding: var(--space-2) var(--space-2);
      text-decoration: none;
      color: var(--color-ink);
      font-family: var(--font-body);
      font-size: var(--text-sm);
    }
    .facet-item .facet-label {
      flex: 1;
      min-width: 0;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
    }
    .facet-item .count {
      font-family: var(--font-mono);
      font-size: var(--text-xs);
      color: var(--color-text-muted);
    }
    .facet-item:hover { background: var(--color-surface-sunken, var(--color-bg-alt)); }
    .facet-item.active a {
      background: var(--color-accent);
      color: var(--color-bg);
    }
    .facet-item.active .count { color: var(--color-bg); }

    /* ---- Pagination strip ---- */
    .pagination {
      display: flex;
      align-items: baseline;
      justify-content: space-between;
      gap: var(--space-6);
      padding: var(--space-6) 0;
      margin-top: var(--space-8);
      border-top: 1px solid var(--color-border);
      flex-wrap: wrap;
    }
    .pagination-nav { display: inline-flex; gap: var(--space-4); }
    .pagination-next {
      font-family: var(--font-body);
      font-size: var(--text-sm);
      color: var(--color-ink);
      text-decoration: none;
      font-weight: 500;
    }
    .pagination-next:hover { color: var(--color-accent); }
    .pagination-next.disabled { color: var(--color-text-muted); pointer-events: none; }
    .pagination-size {
      font-family: var(--font-mono);
      font-size: var(--text-xs);
      color: var(--color-text-muted);
      display: inline-flex;
      align-items: baseline;
      gap: var(--space-2);
    }
    .pagination-size-label { letter-spacing: var(--tracking-wide); }
    .pagination-size a { color: var(--color-text-secondary); text-decoration: none; }
    .pagination-size a:hover { color: var(--color-accent); }
    .pagination-size-current { color: var(--color-ink); font-weight: 600; }

    /* ---- Tabular data table — sketch 002-B base ---- */
    .data-table {
      width: 100%;
      border-collapse: collapse;
      font-family: var(--font-body);
      font-size: var(--text-sm);
      color: var(--color-ink);
    }
    .data-table thead th {
      text-align: left;
      padding: var(--space-3) var(--space-4);
      border-bottom: 2px solid var(--color-ink);
      font-family: var(--font-mono);
      font-size: var(--text-xs);
      font-weight: 600;
      letter-spacing: var(--tracking-wide);
      text-transform: uppercase;
      color: var(--color-text-secondary);
      white-space: nowrap;
    }
    .data-table thead th a {
      color: inherit;
      text-decoration: none;
    }
    .data-table thead th a:hover { color: var(--color-accent); }
    .data-table thead th.sorted a { color: var(--color-accent); text-decoration: underline; }
    .data-table thead th.pk a { color: var(--color-accent); }
    .data-table tbody td {
      padding: var(--space-3) var(--space-4);
      border-bottom: 1px solid var(--color-border);
      vertical-align: top;
      max-width: 32ch;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
    }
    .data-table tbody td.pk {
      font-family: var(--font-mono);
      font-size: var(--text-xs);
      color: var(--color-accent);
    }
    .data-table tbody td a {
      color: var(--color-ink);
      text-decoration: none;
    }
    .data-table tbody td a:hover { color: var(--color-accent); text-decoration: underline; }
    .data-table tbody tr:nth-child(even) { background: var(--color-bg-alt); }
    .data-table tbody tr:hover { background: var(--color-surface-sunken, var(--color-bg-alt)); }

    /* ============================================================ */
    /*  FEED CARDS — direct copy from M1 zeeker-base.css 3864..3984  */
    /*  (sketch 004-A — table-as-news-archive)                       */
    /* ============================================================ */

    .va-feed { display: block; padding: 0; }
    .va-feed.longform-list .va-item { padding-block: var(--space-5); }

    .va-empty {
      padding: var(--space-12) var(--space-6);
      text-align: center;
      color: var(--color-text-muted);
      font-family: var(--font-body);
      font-size: var(--text-sm);
    }

    .va-citation {
      font-family: var(--font-mono);
      font-size: var(--text-xs);
      color: var(--color-text-muted);
      letter-spacing: var(--tracking-wide);
    }

    .va-item-wrap { display: block; }

    .va-item {
      display: block;
      padding: var(--space-6) var(--space-2);
      border-bottom: 1px solid var(--color-border);
      transition: background 0.15s ease;
    }
    .va-item:first-child { border-top: 2px solid var(--color-ink); }
    .va-item:hover { background: var(--color-bg-alt); }

    .va-item-head {
      display: flex;
      align-items: baseline;
      justify-content: space-between;
      gap: var(--space-4);
      margin-bottom: var(--space-3);
      font-family: var(--font-mono);
      font-size: var(--text-xs);
    }
    .va-item-head .kicker {
      color: var(--color-terracotta);
      font-weight: 600;
      letter-spacing: var(--tracking-wide);
      text-transform: uppercase;
    }
    .va-item-head .kicker.muted { color: var(--color-text-muted); }
    .va-item-head .va-date {
      color: var(--color-text-muted);
      letter-spacing: var(--tracking-wide);
    }

    .va-item-title {
      font-family: var(--font-display);
      font-size: var(--text-2xl);
      line-height: 1.15;
      font-weight: 500;
      margin: 0 0 var(--space-3) 0;
    }
    .va-item-title a {
      color: var(--color-ink);
      text-decoration: none;
    }
    .va-item-title a:hover { color: var(--color-accent); }

    .va-item-excerpt {
      font-family: var(--font-body);
      font-size: var(--text-sm);
      line-height: 1.55;
      color: var(--color-text-secondary);
      margin: 0 0 var(--space-3) 0;
      display: -webkit-box;
      -webkit-line-clamp: 2;
      -webkit-box-orient: vertical;
      overflow: hidden;
    }
    .va-item-excerpt.longform {
      -webkit-line-clamp: 3;
    }

    .va-item-foot {
      display: flex;
      align-items: baseline;
      gap: var(--space-4);
      font-family: var(--font-mono);
      font-size: var(--text-xs);
      color: var(--color-text-muted);
    }
    .va-item-foot .byline { color: var(--color-text-secondary); }
    .va-item-foot .source-host {
      margin-left: auto;
      color: var(--color-text-muted);
      text-decoration: none;
      letter-spacing: var(--tracking-wide);
    }
    .va-item-foot .source-host:hover { color: var(--color-accent); }

    @media (max-width: 640px) {
      .va-item-head { flex-direction: column; align-items: flex-start; gap: var(--space-2); }
      .va-item-title { font-size: var(--text-xl); }
      .va-item-foot { flex-wrap: wrap; }
      .va-item-foot .source-host { margin-left: 0; }
    }

    /* ============================================================ */
    /*  ARTICLE READING LAYOUT — sketch 003-A magazine               */
    /* ============================================================ */

    .row-section { padding-block: var(--space-12); }
    .row-header { padding-block: var(--space-12) var(--space-8); }

    .article-grid {
      display: grid;
      grid-template-columns: minmax(0, 1fr) 280px;
      gap: var(--space-12);
      align-items: start;
    }
    @media (max-width: 900px) {
      .article-grid { grid-template-columns: 1fr; }
    }

    .article {
      max-width: 62ch;        /* UI-SPEC §Typography — never wider */
      font-family: var(--font-body);
      color: var(--color-ink);
    }
    .article.read {
      /* magazine reading variant */
    }

    .article .byline {
      font-family: var(--font-mono);
      font-size: var(--text-xs);
      color: var(--color-text-muted);
      letter-spacing: var(--tracking-wide);
      text-transform: uppercase;
      margin: 0 0 var(--space-6) 0;
    }
    .article .byline time { color: var(--color-text-secondary); }

    .article-body {
      font-family: var(--font-display);
      font-size: 1.125rem;       /* UI-SPEC LOCKED — Fraunces opsz 11 body */
      font-variation-settings: "opsz" 11;
      line-height: 1.65;
      color: var(--color-ink);
    }
    .article-body p {
      margin: 0 0 var(--space-10) 0;
    }
    .article-body p:first-of-type::first-letter {
      /* Drop cap — UI-SPEC §row_mode: article. Skipped naturally when first <p> is short
         because the rule applies only on first-of-type, but if author wants to suppress
         on short bodies, a future opt-out class can be added. */
      font-family: var(--font-display);
      font-size: 4em;
      line-height: 0.9;
      float: left;
      margin: 0.05em var(--space-3) 0 0;
      color: var(--color-accent);
      font-weight: 500;
    }
    /* Longform variant — no drop cap (UI-SPEC §row_mode: longform). */
    .longform-article .article-body p:first-of-type::first-letter {
      all: unset;
    }
    .longform-article .article-body { font-size: 1.0625rem; }

    /* ---- Aside (sticky right rail on row article + tabular) ---- */
    .aside {
      position: sticky;
      top: 120px;
      align-self: start;
      max-height: calc(100vh - 140px);
      overflow-y: auto;
      padding: 0 var(--space-2);
    }
    .aside-block {
      padding-block: var(--space-5);
      border-top: 1px solid var(--color-border);
    }
    .aside-block:first-child {
      border-top: 2px solid var(--color-accent);
    }
    .aside-block h3 {
      font-family: var(--font-mono);
      font-size: var(--text-2xs, 0.6875rem);
      letter-spacing: var(--tracking-widest);
      text-transform: uppercase;
      color: var(--color-text-muted);
      margin: 0 0 var(--space-3) 0;
      font-weight: 600;
    }
    .aside-block dl {
      margin: 0;
      font-family: var(--font-body);
      font-size: var(--text-sm);
    }
    .aside-block dt {
      font-family: var(--font-mono);
      font-size: var(--text-xs);
      color: var(--color-text-muted);
      letter-spacing: var(--tracking-wide);
      margin-top: var(--space-2);
    }
    .aside-block dd {
      margin: 0 0 var(--space-2) 0;
      color: var(--color-text-secondary);
      word-wrap: break-word;
    }
    .aside-block .export-links {
      list-style: none;
      padding: 0;
      margin: 0;
    }
    .aside-block .export-links li {
      padding: var(--space-1) 0;
      font-family: var(--font-body);
      font-size: var(--text-sm);
    }
    .aside-block .export-links a {
      color: var(--color-ink);
      text-decoration: none;
    }
    .aside-block .export-links a:hover { color: var(--color-accent); }

    .back-link {
      display: inline-block;
      font-family: var(--font-body);
      font-size: var(--text-sm);
      color: var(--color-text-secondary);
      text-decoration: none;
      padding-block: var(--space-2);
    }
    .back-link:hover { color: var(--color-accent); }

    /* ---- Source coda (article + judgment) ---- */
    .coda {
      margin-top: var(--space-12);
      padding-top: var(--space-6);
      border-top: 4px double var(--color-ink);
      font-family: var(--font-body);
      font-size: var(--text-sm);
      text-align: center;
      color: var(--color-text-secondary);
    }
    .coda-label {
      font-family: var(--font-mono);
      font-size: var(--text-xs);
      letter-spacing: var(--tracking-widest);
      text-transform: uppercase;
      color: var(--color-text-muted);
      margin: 0 0 var(--space-2) 0;
    }
    .coda a {
      color: var(--color-ink);
      text-decoration: none;
      border-bottom: 1px solid var(--color-border);
    }
    .coda a:hover { color: var(--color-accent); border-bottom-color: var(--color-accent); }
    .coda-fingerprint {
      font-family: var(--font-mono);
      font-size: var(--text-xs);
      color: var(--color-text-muted);
      margin-top: var(--space-3);
      letter-spacing: var(--tracking-wide);
    }

    /* ============================================================ */
    /*  JUDGMENT BROADSHEET — sketch 003-B                            */
    /* ============================================================ */

    .dateline {
      background: var(--color-ink);
      color: var(--color-bg);
      padding-block: var(--space-3);
      margin-bottom: var(--space-8);
    }
    .dateline .container {
      display: flex;
      align-items: baseline;
      justify-content: space-between;
      gap: var(--space-6);
    }
    .dateline-agency {
      font-family: var(--font-mono);
      font-size: var(--text-xs);
      color: var(--color-ochre);
      font-weight: 600;
      letter-spacing: var(--tracking-widest);
      text-transform: uppercase;
    }
    .dateline-date {
      font-family: var(--font-mono);
      font-size: var(--text-xs);
      color: var(--color-bg);
      letter-spacing: var(--tracking-wide);
    }

    .article.judgment {
      max-width: 62ch;
      margin: 0 auto;
      text-align: left;
    }
    .judgment-meta {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: var(--space-3) var(--space-6);
      padding: var(--space-5) 0;
      border-top: 1px solid var(--color-border);
      border-bottom: 1px solid var(--color-border);
      margin: var(--space-6) 0 var(--space-10) 0;
      font-family: var(--font-body);
      font-size: var(--text-sm);
    }
    .judgment-meta dt {
      font-family: var(--font-mono);
      font-size: var(--text-xs);
      color: var(--color-text-muted);
      letter-spacing: var(--tracking-wide);
      text-transform: uppercase;
    }
    .judgment-meta dd {
      margin: 0;
      color: var(--color-ink);
    }
    @media (min-width: 640px) {
      .judgment-meta { grid-template-columns: repeat(4, minmax(0, 1fr)); }
    }

    .judgment-body {
      /* same Fraunces opsz 11 reading body, narrower max-width OK */
    }

    .tag-chips {
      display: flex;
      flex-wrap: wrap;
      gap: var(--space-2);
      margin: var(--space-8) 0;
    }
    .tag-chip {
      display: inline-block;
      padding: var(--space-1) var(--space-3);
      background: var(--color-surface-sunken, var(--color-bg-alt));
      color: var(--color-text-secondary);
      font-family: var(--font-mono);
      font-size: var(--text-xs);
      letter-spacing: var(--tracking-wide);
      border-radius: var(--radius-sm, 2px);
    }

    /* ============================================================ */
    /*  ROW TABULAR FALLBACK — D-04                                  */
    /* ============================================================ */

    .row-tabular { max-width: 80ch; }
    .row-tabular-grid { /* same article-grid but narrower main column ok */ }
    .row-dl { margin: 0; }
    .row-dl dt {
      font-family: var(--font-mono);
      font-size: var(--text-xs);
      letter-spacing: var(--tracking-wide);
      text-transform: uppercase;
      color: var(--color-text-muted);
      margin-top: var(--space-5);
    }
    .row-dl dd {
      margin: var(--space-1) 0 0 0;
      font-family: var(--font-body);
      font-size: var(--text-sm);
      color: var(--color-ink);
      word-wrap: break-word;
    }
    .row-dl dd .muted { color: var(--color-text-muted); }
    .row-dd-long {
      margin-top: var(--space-3);
      font-family: var(--font-display);
      font-size: 1rem;
      line-height: 1.6;
      color: var(--color-ink);
      white-space: pre-wrap;
    }
    .row-dl details {
      margin-top: var(--space-1);
    }
    .row-dl details summary {
      cursor: pointer;
      font-family: var(--font-mono);
      font-size: var(--text-xs);
      color: var(--color-accent);
      letter-spacing: var(--tracking-wide);
      list-style: none;
    }
    .row-dl details summary::-webkit-details-marker { display: none; }
    .row-dl details[open] .row-dd-preview { display: none; }
    .row-dd-preview {
      color: var(--color-text-secondary);
      margin-top: var(--space-1);
    }

    /* ============================================================ */
    /*  EMPTY STATE                                                  */
    /* ============================================================ */

    .empty-state {
      text-align: center;
      padding: var(--space-16) var(--space-6);
      color: var(--color-text-secondary);
    }
    .empty-state h2 {
      font-family: var(--font-display);
      font-size: var(--text-2xl);
      font-weight: 500;
      color: var(--color-ink);
      margin: 0 0 var(--space-3) 0;
    }
    .empty-state p {
      font-family: var(--font-body);
      font-size: var(--text-base);
      max-width: 50ch;
      margin: 0 auto;
    }

    /* =========== END phase 05 ============ */

    ```

    Step 3 — Verify (do not skip): after the edit, confirm that the FOOTER LINK OVERRIDE and ACCESSIBILITY UTILITIES blocks are STILL the LAST two sections in the file. Run:
    ```bash
    tail -30 packages/zeeker-frontend/src/zeeker_frontend/static/css/zeeker.css
    ```
    The last lines must still be the `.visually-hidden{...}` block.
  </action>
  <verify>
    <automated>
      cd /Users/houfu/Projects/zeeker-datasette && \
      grep -c '/\* =========== TABLE BROWSE + ROW VIEW — phase 05 ============ \*/' packages/zeeker-frontend/src/zeeker_frontend/static/css/zeeker.css | grep -q 1 && \
      tail -3 packages/zeeker-frontend/src/zeeker_frontend/static/css/zeeker.css | grep -q 'visually-hidden' && \
      python -c "
import re
with open('packages/zeeker-frontend/src/zeeker_frontend/static/css/zeeker.css') as f:
    css = f.read()
# Brace balance
o, c = css.count('{'), css.count('}')
assert o == c, f'unbalanced braces: {o} open, {c} close'
# Required class names from UI-SPEC + Plans 05-02/05-03 templates
required = [
    '.feed-layout', '.va-feed', '.va-item', '.va-item-head', '.va-item-title',
    '.va-item-excerpt', '.va-item-foot', '.source-host',
    '.facets', '.facet-block', '.facet-list', '.facet-item', '.facet-block-title',
    '.filter-chip', '.filter-chips',
    '.pagination', '.pagination-next', '.pagination-size',
    '.data-table',
    '.article-grid', '.article-body', '.aside', '.aside-block',
    '.dateline', '.dateline-agency', '.dateline-date',
    '.judgment-meta', '.tag-chip', '.tag-chips',
    '.coda', '.coda-label', '.coda-fingerprint',
    '.back-link', '.empty-state',
    '.row-tabular', '.row-dl', '.row-dd-long', '.row-dd-preview',
    '.longform-article', '.row-section', '.row-header', '.db-toolbar-export',
]
missing = [c for c in required if c not in css]
assert not missing, f'missing CSS class definitions: {missing}'
# No new tokens introduced — every var() reference must use a known prefix
unknown = re.findall(r'var\(--([a-z0-9-]+)\)', css)
known_prefixes = ('color-', 'space-', 'text-', 'font-', 'tracking-', 'leading-', 'shadow-', 'radius-')
bad = [v for v in set(unknown) if not v.startswith(known_prefixes)]
assert not bad, f'unknown CSS token prefixes: {bad}'
# File-tail check
lines = css.splitlines()
assert any('FOOTER LINK OVERRIDE' in l for l in lines[-50:]), 'FOOTER LINK OVERRIDE no longer in tail'
assert any('ACCESSIBILITY UTILITIES' in l for l in lines[-50:]), 'ACCESSIBILITY UTILITIES no longer in tail'
print('CSS structural validation OK; total lines:', len(lines))
print('Brace balance:', o, '/', c)
print('Required classes present:', len(required))
"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '/\* =========== TABLE BROWSE + ROW VIEW — phase 05 ============ \*/' packages/zeeker-frontend/src/zeeker_frontend/static/css/zeeker.css` returns 1 (delimiter present)
    - Final 3 lines of file still contain `.visually-hidden{` declaration (FOOTER LINK OVERRIDE + ACCESSIBILITY UTILITIES preserved at TAIL)
    - Brace count balanced: open `{` count equals close `}` count
    - Each of the following class names has at least one rule definition (grep for `<class>` followed by `{` or whitespace+`{`): `.feed-layout`, `.va-feed`, `.va-item`, `.facet-block`, `.facet-item`, `.filter-chip`, `.pagination`, `.data-table`, `.article-body`, `.aside`, `.aside-block`, `.dateline`, `.tag-chip`, `.coda`, `.row-dl`, `.empty-state`
    - No `var(--<unknown-prefix>)` references — every CSS variable usage references the existing prefixes (`color-`, `space-`, `text-`, `font-`, `tracking-`, `leading-`, `shadow-`, `radius-`)
    - File line count >= 1400 (was 1102; added ~390 lines)
    - No reference to deprecated M1 paths (no `zeeker-base.css` string in new section)
  </acceptance_criteria>
  <done>Phase-05 CSS section appended; FOOTER LINK OVERRIDE preserved at tail; brace-balanced; all template class names have rule definitions; no new design tokens introduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| StaticFiles → browser | trusted static asset served by FastAPI; no user-input keyed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | CSS-borne XSS via dataset-driven `content`, `url()`, or attribute selectors | accept | Phase 5 CSS introduces NO `content` properties keyed on dataset values; NO `url()` references except (none — Phase 5 doesn't add background images); all backgrounds use CSS custom property tokens |
| T-05-06 | Information disclosure | CSS cache poisoning via Vary header | accept | not applicable — zeeker.css is a static file served by StaticFiles middleware; standard cache headers; no user-input keying |
</threat_model>

<verification>
- Section delimiter present exactly once
- All Phase-2/3 partial class names have CSS rule definitions
- FOOTER LINK OVERRIDE + ACCESSIBILITY UTILITIES still at file tail
- Brace balance verified
- No new CSS tokens introduced
</verification>

<success_criteria>
- Manual ASGI smoke (after Plan 05-02 + 05-03 ship): `curl http://testserver/sglawwatch/headlines | grep "va-item"` returns matches; CSS file referenced via `/static/css/zeeker.css` resolves 200
- File grows from ~1102 lines to ~1490 lines
- No regressions in existing CSS rules: `grep -c "footer a:link" zeeker.css` returns 1 (FOOTER LINK OVERRIDE intact)
</success_criteria>

<output>
After completion, create `.planning/phases/05-port-table-browse-row-view/05-04-SUMMARY.md` documenting:
- Final line count of zeeker.css
- Section structure: insertion anchor, delimiter, sub-section comments
- Class-name inventory matched against Plans 05-02/05-03 templates
- Sketch-findings citation per sub-section
- Any deviations from the plan
</output>
