---
phase: 05-port-table-browse-row-view
verified: 2026-04-25T16:30:00Z
status: human_needed
score: 7/7 must-haves verified (all programmatic)
overrides_applied: 1
overrides:
  - must_have: "API byte-parity vs phase-03-pre baselines"
    reason: "Phase-5 ship-checkpoint outcome locked: 6 byte-diffs operator-triaged into Category B (env drift — timestamps/row counts in /-/metadata.json, _zeeker_updates, sg-gov-newsrooms.json) and Category C (intentional metadata.json content evolution from Phase-4 platform branding rewrite + Phase-5 display.* blocks; pre-Plan-05-05 metadata at commit 4f79811^ already had live state). Zero Category-A regressions. Per locked Phase-3 stale-check-retirement-in-same-phase decision, verify_api_parity.sh check #11 retired in commit 4c39405 with full justification in 05-DEPLOY-NOTES.md ship-checkpoint outcome table. REQ-api-byte-parity satisfied-by-retirement."
    accepted_by: "operator"
    accepted_at: "2026-04-25T16:00:00+08:00"
human_verification:
  - test: "Visual sweep — feed mode parity with sketch 004-A"
    expected: "Visit /sglawwatch/headlines locally; confirm feed cards render with kicker (terracotta uppercase mono), italic-accent H1, hero with stat band, sticky toolbar with FTS + applied-chips + CSV/JSON exports, two-column layout (feed-main + sticky .facets sidebar with category facet block), pagination strip with Next → and 25/50/100 size selector"
    why_human: "Visual fidelity to locked sketch design contract; programmatic checks confirm CSS classes and template structure exist but cannot judge whether the rendered pixels match sketch 004-A"
  - test: "Visual sweep — article mode parity with sketch 003-A"
    expected: "Click into a headline; confirm row article layout shows kicker, italic-accent H1, byline+date, Fraunces opsz 11 body with drop cap on first paragraph, sticky aside with Record dl + Export links + back link, source coda double-rule"
    why_human: "Drop-cap typographic effect, sticky positioning behavior, and Fraunces opsz tuning require human eyes against sketch 003-A; CSS rules for these are present but render fidelity needs visual confirmation"
  - test: "Visual sweep — judgment mode parity with sketch 003-B"
    expected: "Visit /Zeeker-Judgements/judgments/{pk} (after capturing real PK); confirm dark .dateline strip with ochre court agency + mono date, italic-accent H1, 4-column .judgment-meta grid, Fraunces body, .tag-chip row from subject_tags, .coda double-rule with source link + fingerprint"
    why_human: "Editorial broadsheet visual rhythm; programmatic checks confirm CSS+template structure but live judgment data may produce edge cases (long case_name, missing court, malformed subject_tags JSON) that need human review"
  - test: "End-to-end verifier against running stack"
    expected: "docker compose up -d --build → bash scripts/verify_phase_05.sh exits 0 (with API-parity check #11 already retired). All 14 sections A-N green. Note: per ship-checkpoint outcome, this was already run on 2026-04-25 and passed all Phase-5-specific sections; verifier preserved on disk for future regression runs."
    why_human: "Integration tests use MockTransport against fixture data; only a real live stack with real datasette + real S3 metadata can validate end-to-end behavior. Operator already ran this; included for re-run if any code changes land."
  - test: "Longform-list mode parity with sketch 002-B variant without pills"
    expected: "Visit /sglawwatch/about_singapore_law; confirm va-feed.longform-list renders without category pills (kicker styled muted), longer truncate(180) excerpts, and click-through goes to /sglawwatch/about_singapore_law/{pk} which renders longform mode (article reading column only, NO sidebar, NO drop cap)"
    why_human: "Longform mode is a sub-variant of feed/article that downplays categorization chrome; visual confirmation that the muted styling and absent sidebar produce the intended editorial reading-room feel"
  - test: "Tabular fallback row view with native <details> long-text expand"
    expected: "Visit a row whose primary content is >200 chars; confirm native <details><summary>Show full content</summary> renders in tabular fallback row mode (D-04). Click summary; confirm full content appears and the .row-dd-preview hides via [open] CSS rule"
    why_human: "Native <details>/<summary> behavior + the [open] sibling-hide CSS rule combine to produce a click-to-expand pattern; programmatic tests confirm the markup but the interaction-state styling needs a real click"
---

# Phase 5: Port Table Browse + Row View — Verification Report

**Phase Goal:** Implement frontend routes `/{db}/{table}` (paginated rows, facets, export links, inline query form) and `/{db}/{table}/{pk}` (single row view).
**Verified:** 2026-04-25T16:30:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #   | Truth                                                                                                                                                                                                | Status     | Evidence                                                                                                                                                                                                                                                                                                                                                          |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `GET /{db}/{table}` route returns 200, renders facets, pagination, sort, FTS, with export links direct-to-datasette (Caddy-suffix-routed)                                                            | ✓ VERIFIED | `routes_table.py` registered at `/{db}/{table}` (confirmed via ASGI introspection). Mode dispatch in `table.html` to feed/longform-list/tabular partials. Export anchors use `export_url(database, table, 'csv'\|'json', request_qs)` → direct path (D-05). 18+ integration tests passing in `test_routes_table.py` (covers facets, applied chips, pagination relative-href, FTS no-results, sort, export-direct, Cache-Control, italic H1, hidden-table 404, rowid-PK fallback, 503 on httpx error). |
| 2   | `GET /{db}/{table}/{pk}` route returns 200, renders single-record view dispatched by row_mode (article / judgment / longform / tabular)                                                              | ✓ VERIFIED | `routes_row.py` registered at `/{db}/{table}/{pk}`. Mode dispatch in `row.html` to row_article/row_judgment/row_longform/row_tabular partials. 15 integration tests passing in `test_routes_row.py` (article+aside, judgment+dateline+tag-chip, longform-no-aside, tabular+dl+details, Cache-Control, italic H1, 3-segment breadcrumb, hidden-table 404, unknown-row 404, 503 error, export-anchor present, rowid-only fallback). |
| 3   | Hidden-table guard active at both routes — `_zeeker_*` prefix and `_fts*` suffixes return 404                                                                                                          | ✓ VERIFIED | ASGI smoke confirms `/sglawwatch/_zeeker_schemas`, `/sglawwatch/headlines_fts`, `/sglawwatch/some_fts_data`, `/sglawwatch/_zeeker_schemas/anything`, `/sglawwatch/headlines_fts/anything` all return 404. Both `routes_table.py` and `routes_row.py` declare `_HIDDEN_TABLE_PREFIXES = ("_zeeker",)` and `_HIDDEN_TABLE_SUFFIXES` containing 5 FTS suffixes; guard is FIRST conditional in both handlers. T-05-03 mitigation in place; same wording for missing vs hidden (no info disclosure). |
| 4   | Export links route directly to datasette (D-05 — bypass frontend)                                                                                                                                    | ✓ VERIFIED | `urls.py:export_url()` returns plain `/{db}/{table}.{ext}?{qs}` strings. `table.html` renders `<a href="{{ export_url(...) }}">CSV/JSON</a>` in toolbar. `row_article.html` and `row_tabular.html` use direct `/{db}/{table}/{pk}.json` and `/{db}/{table}.csv?{pk}={value}` anchors. Caddy `@datasette` matcher routes `*.csv`/`*.json` direct (Phase-3 contract). Test `test_export_anchors_are_direct` asserts regex match on `href="/sglawwatch/headlines.csv?[^"]*"` AND `"/export?" not in body`. |
| 5   | Mode-dispatch logic works: `display.table_mode` ∈ {feed, longform-list, tabular}; `display.row_mode` ∈ {article, judgment, longform, tabular}; default = tabular                                       | ✓ VERIFIED | `routes_table.py:50-51` — `table_mode = display.get("table_mode") or "tabular"`; `row_mode = display.get("row_mode") or "tabular"`. `routes_row.py:62` — same pattern. `table.html` dispatch covers feed/longform-list/tabular branches; `row.html` dispatch covers article/judgment/longform/tabular branches. Tests `test_table_returns_200_feed_mode`, `test_table_tabular_fallback`, `test_row_returns_200_article_mode`, `test_row_judgment_mode_renders_dateline`, `test_row_longform_mode_no_aside`, `test_row_tabular_fallback_renders_dl` all pass. `metadata.json` ships 11 display.* blocks exercising 4 distinct mode combinations. |
| 6   | Cache-Control header `public, max-age=60, stale-while-revalidate=300` on both routes                                                                                                                  | ✓ VERIFIED | Literal string at `routes_table.py:129` and `routes_row.py:113`. Tests `test_table_cache_control_header` and `test_row_cache_control_header` assert presence. |
| 7   | next_url Pitfall-2 rewrite — datasette internal hostname stripped, only querystring reused                                                                                                            | ✓ VERIFIED | `routes_table.py:55-60` — `urlparse(ds_next)`, then `f"/{db}/{table}?{parsed.query}"`. Test `test_pagination_next_link_when_next_url` asserts `"zeeker-datasette:8001" not in body`. |

**Score:** 7/7 truths verified.

### Required Artifacts

| Artifact                                                                                                  | Expected                                                              | Status     | Details                                                                                                                                                                                                                  |
| --------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/zeeker-frontend/src/zeeker_frontend/routes_table.py`                                            | Full GET /{db}/{table} handler                                        | ✓ VERIFIED | 131 lines; imports `fetch_table` + `fetch_site_metadata`; hidden-table guard FIRST; allowlist consumed via fetch_table; Cache-Control set; mode dispatch context populated; httpx.HTTPError → 503; payload None → 404.                                                                  |
| `packages/zeeker-frontend/src/zeeker_frontend/routes_row.py`                                              | Full GET /{db}/{table}/{pk} handler                                   | ✓ VERIFIED | 115 lines; imports `fetch_row` + `fetch_site_metadata`; hidden-table guard FIRST; pk_label truncation to 12 chars + ellipsis; row_mode dispatch context populated; long_text_columns precomputed for tabular fallback. |
| `packages/zeeker-frontend/src/zeeker_frontend/main.py`                                                    | Registers table_router + row_router AFTER database_router; 9 Jinja globals from urls.py | ✓ VERIFIED | Lines 99-100: imports both routers. Lines 116-119: `app.include_router` calls in order home → database → table → row. Lines 82-91: 9 Jinja globals registered (path_with_added/replaced/removed_args, toggle/clear_facet_value, set_sort, export_url, row_url, tilde_encode). `/frontend-test` registered before include_router calls. |
| `packages/zeeker-frontend/src/zeeker_frontend/urls.py`                                                    | 9 helper functions                                                    | ✓ VERIFIED | All 9 functions defined: path_with_added_args, path_with_replaced_args, path_with_removed_args, toggle_facet_value, clear_facet_value, set_sort, export_url, tilde_encode, row_url. 25 unit tests in `test_urls.py` all passing.                                                       |
| `packages/zeeker-frontend/src/zeeker_frontend/datasette_client.py` extension                              | fetch_table + fetch_row                                               | ✓ VERIFIED | Lines 74-99: `fetch_table` with `_TABLE_ALLOWED_PARAMS` allowlist + forced `_shape=objects` + 404→None + 5xx→raise. Lines 102-113: `fetch_row` with same 404/5xx contract + `_shape=objects`. 12 unit tests passing in `test_datasette_client_table_row.py`. |
| `packages/zeeker-frontend/src/zeeker_frontend/templates/table.html`                                       | Mode-dispatch shell                                                   | ✓ VERIFIED | Extends base.html; italic-accent H1 split via `page_title.rfind(' ')`; hero + stat band + sticky toolbar + 2-column feed-layout (main + sticky .facets sidebar); mode dispatch covers feed/longform-list/tabular; export anchors use `export_url`; empty-state with FTS-aware copy. |
| `packages/zeeker-frontend/src/zeeker_frontend/templates/row.html`                                         | Mode-dispatch shell                                                   | ✓ VERIFIED | Extends base.html; italic-accent H1 split; NO stat band (UI-SPEC explicit); row_mode dispatch covers article/judgment/longform/tabular. |
| `templates/_partials/table_feed.html`                                                                     | Feed-card mode (sketch 004-A)                                         | ✓ VERIFIED | `va-feed` + `va-item` + `va-item-head/title/excerpt/foot` + slot-driven via display.columns; tilde-encoded row links. |
| `templates/_partials/table_tabular.html`                                                                  | Default tabular `<table>` with sortable headers                       | ✓ VERIFIED | `data-table` class; column headers use `set_sort()` for cycling; pk-class on PK column; first cell links to row; sorted-class indicator. |
| `templates/_partials/table_longform_list.html`                                                            | Longform-list (feed without category pills)                            | ✓ VERIFIED | `va-feed.longform-list` + muted kicker. |
| `templates/_partials/facet_sidebar.html`                                                                  | Sticky facet sidebar                                                  | ✓ VERIFIED | `facet-block` + `facet-list` + `facet-item.active`; uses `path_with_added_args` and `clear_facet_value`. |
| `templates/_partials/applied_facets.html`                                                                 | Filter-chip strip                                                     | ✓ VERIFIED | `filter-chips` + `filter-chip` + `filter-chip-search` (search variant); × clear via `clear_facet_value` and `path_with_removed_args`. |
| `templates/_partials/pagination.html`                                                                     | Cursor pagination + size selector                                     | ✓ VERIFIED | `pagination` + `pagination-next` + `pagination-size` with 25/50/100 options; uses `path_with_replaced_args`. |
| `templates/_partials/row_article.html`                                                                    | Magazine article (sketch 003-A)                                       | ✓ VERIFIED | `article.read` + `aside` two-col grid; striptags+split-on-blank-lines paragraph rendering; `coda` source-block; export-links + back-link in aside. |
| `templates/_partials/row_judgment.html`                                                                   | Editorial broadsheet (sketch 003-B)                                   | ✓ VERIFIED | `dateline` (ink + ochre agency + mono date) + `judgment-meta` 4-col grid + `judgment-body` + `tag-chips` (split JSON-array string subject_tags) + `coda` with fingerprint. |
| `templates/_partials/row_longform.html`                                                                   | Article without aside (no sidebar, no drop cap)                       | ✓ VERIFIED | `article.read.longform-article` (no `aside` element); CSS suppresses drop cap via `all: unset` on first-of-type::first-letter. |
| `templates/_partials/row_tabular.html`                                                                    | Default `<dl>` with `<details>` for long-text                          | ✓ VERIFIED | `row-dl` + `<details><summary>Show full content` for fields >200 chars; preview/long sibling pair driven by long_text_columns dict; aside with export-links. |
| `packages/zeeker-frontend/src/zeeker_frontend/static/css/zeeker.css`                                      | Phase-05 CSS section appended                                         | ✓ VERIFIED | Section delimiter `/* =========== TABLE BROWSE + ROW VIEW — phase 05 ============ */` present at line ~1060. File grew from 1102 → 1746 lines. FOOTER LINK OVERRIDE + ACCESSIBILITY UTILITIES preserved at TAIL. All Phase-5 class names have rule definitions (verified via grep). |
| `metadata.json` display.* blocks                                                                          | 11 in-scope tables with display hints                                 | ✓ VERIFIED | `grep -c '"display"'` returns 11. JSON parses; sglawwatch.headlines (feed/article), sglawwatch.about_singapore_law (longform-list/longform), Zeeker-Judgements.judgments (tabular/judgment), 8× sg-gov-newsrooms.*_news (feed/article). databases.*.tables._zeeker_schemas/_updates `hidden:true` preserved. |
| `scripts/verify_phase_05.sh`                                                                              | End-to-end verifier mirroring verify_phase_04.sh                      | ✓ VERIFIED | File present, executable (`-rwxr-xr-x`), `bash -n` exits 0. Delegates to `verify_phase_04.sh` and `verify_api_parity.sh`. 14 sections A-O. Operator-confirmed: ran on 2026-04-25, all Phase-5-specific sections green; section O (API parity) failures all triaged Category B/C, REQ-api-byte-parity satisfied via stale-check retirement. |
| `.planning/phases/05-port-table-browse-row-view/05-DEPLOY-NOTES.md`                                       | Deploy decision + ship-checkpoint outcome                             | ✓ VERIFIED | Three deploy options documented; four-category triage labels (A/B/C/D); pre-checkpoint operator checklist; ship-checkpoint outcome table filled in (Decision 2 — defer; verifier exit 1 due to retired check #11; rollback NOT executed; STATE.md/ROADMAP.md updated). |

### Key Link Verification

| From                  | To                                          | Via                                                    | Status   | Details                                                                                                                                                                                            |
| --------------------- | ------------------------------------------- | ------------------------------------------------------ | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| routes_table.py       | datasette_client.fetch_table                | `await fetch_table(client, db, table, ...)`           | ✓ WIRED  | Line 37; payload returned, used to populate context.                                                                                                                                                |
| routes_table.py       | datasette_client.fetch_site_metadata        | `await fetch_site_metadata(client)`                    | ✓ WIRED  | Line 44; site_metadata used for table_meta + display.* extraction.                                                                                                                                  |
| routes_table.py       | templates/table.html                        | `TemplateResponse(name="table.html", ...)`             | ✓ WIRED  | Line 96-128; full context dict populated.                                                                                                                                                            |
| routes_row.py         | datasette_client.fetch_row                  | `await fetch_row(client, db, table, pk)`              | ✓ WIRED  | Line 42; payload populates row + columns + display dispatch.                                                                                                                                         |
| routes_row.py         | templates/row.html                          | `TemplateResponse(name="row.html", ...)`               | ✓ WIRED  | Line 86-112.                                                                                                                                                                                         |
| table.html            | _partials/table_{feed,longform_list,tabular}.html | `{% include %}` mode dispatch                       | ✓ WIRED  | Lines 98-104; conditional include matches all 3 table_mode values.                                                                                                                                  |
| row.html              | _partials/row_{article,judgment,longform,tabular}.html | `{% include %}` mode dispatch                  | ✓ WIRED  | Lines 38-47; conditional include matches all 4 row_mode values.                                                                                                                                      |
| table.html            | Caddy @datasette suffix matcher              | `<a href="{{ export_url(...) }}">`                     | ✓ WIRED  | Line 86-87; export_url returns `/{db}/{table}.{csv\|json}?{qs}` plain string; Caddy @datasette matcher catches `*.csv` and `*.json`. D-05 honored.                                                  |
| main.py Jinja env     | urls.py 9 helpers                            | `templates.env.globals[...]`                            | ✓ WIRED  | Lines 82-91; all 9 helpers registered. Templates call them by plain function name (`{{ export_url(...) }}`, `{{ tilde_encode(...) }}`, etc.).                                                       |
| main.py app           | routes_table.router                          | `app.include_router(table_router)`                      | ✓ WIRED  | Line 118 — AFTER `database_router` (line 117) and AFTER `home_router` (line 116). Specificity order correct.                                                                                       |
| main.py app           | routes_row.router                            | `app.include_router(row_router)`                        | ✓ WIRED  | Line 119 — AFTER `table_router`. Most-specific route last.                                                                                                                                          |
| zeeker.css            | Plan 05-02/05-03 partial class names         | matching CSS class definitions                          | ✓ WIRED  | All locked classes (`.va-item`, `.facet-block`, `.data-table`, `.article-grid`, `.aside`, `.dateline`, `.tag-chip`, `.coda`, `.row-dl`, `.empty-state`, etc.) have rule definitions; verified by grep. |

### Data-Flow Trace (Level 4)

| Artifact            | Data Variable                                     | Source                                                                | Produces Real Data                                                                                                                                                                              | Status      |
| ------------------- | ------------------------------------------------- | --------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- |
| routes_table.py     | `payload` (rows, columns, facet_results, next_url) | `fetch_table(client, db, table, dict(request.query_params))`         | YES — fetch_table makes a real HTTP GET against `http://zeeker-datasette:8001/{db}/{table}.json?_shape=objects&...`; returns dict on 200, None on 404, raises on other. Response dict drives template rendering. | ✓ FLOWING   |
| routes_table.py     | `site_metadata`                                   | `fetch_site_metadata(client)`                                          | YES — fetches `/-/metadata.json` from datasette (60s TTL cached); returned dict drives display.* extraction.                                                                                  | ✓ FLOWING   |
| routes_row.py       | `payload`, `row` (single dict)                    | `fetch_row(client, db, table, pk)`                                     | YES — HTTP GET against `/{db}/{table}/{pk}.json?_shape=objects`; rows[0] populates the row dict; long_text_columns precomputed from real string lengths.                                       | ✓ FLOWING   |
| table.html (rendered) | `rows`, `columns`, `facet_results`, `next_url`  | route context populated from payload                                   | YES — context flows from real datasette response; tests use MockTransport with captured baseline payloads, but production stack proves data flows end-to-end (operator confirmed verifier ran on 2026-04-25). | ✓ FLOWING   |
| row.html (rendered) | `row`, `columns`, `display_columns`               | route context populated from payload                                   | YES — same as above.                                                                                                                                                                            | ✓ FLOWING   |

### Behavioral Spot-Checks

| Behavior                                                                                                       | Command                                                                                                       | Result                                          | Status |
| -------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------- | ----------------------------------------------- | ------ |
| Phase-5 specific test suite passes (4 test files: test_urls, test_datasette_client_table_row, test_routes_table, test_routes_row) | `cd packages/zeeker-frontend && uv run pytest tests/test_routes_table.py tests/test_routes_row.py tests/test_urls.py tests/test_datasette_client_table_row.py -q` | `75 passed in 0.08s`                            | ✓ PASS |
| Full frontend test suite passes (Phase-4 + Phase-5)                                                              | `cd packages/zeeker-frontend && uv run pytest -q`                                                            | `116 passed in 0.11s`                           | ✓ PASS |
| Routes registered in correct specificity order (home → database → table → row)                                  | ASGI introspection — list paths from `app.routes`                                                            | order correct: `/` → `/{db}` → `/{db}/{table}` → `/{db}/{table}/{pk}` | ✓ PASS |
| Hidden-table guard active at table route — `_zeeker_schemas`, `headlines_fts`, `some_fts_data` → 404            | ASGI smoke via `httpx.AsyncClient(transport=ASGITransport(app=app))`                                          | (404, 404, 404)                                 | ✓ PASS |
| Hidden-table guard active at row route — `_zeeker_schemas/anything`, `headlines_fts/anything` → 404              | ASGI smoke via same transport                                                                                  | (404, 404)                                      | ✓ PASS |
| metadata.json parses + has 11 display blocks                                                                     | `python -c "import json; d=json.load(open('metadata.json')); ..." + grep -c '"display"' metadata.json`     | parses cleanly; 11 display blocks               | ✓ PASS |
| verify_phase_05.sh syntactically valid + executable                                                              | `test -x scripts/verify_phase_05.sh && bash -n scripts/verify_phase_05.sh`                                   | both succeed                                     | ✓ PASS |
| zeeker.css contains phase-05 section + grew to 1746 lines                                                        | `grep '/* =========== TABLE BROWSE + ROW VIEW — phase 05 ============ */' && wc -l`                          | section present; 1746 lines                     | ✓ PASS |
| End-to-end verifier against running stack                                                                       | `bash scripts/verify_phase_05.sh` (operator-run on 2026-04-25)                                                | All Phase-5-specific sections green; section O failures all triaged Category B/C; REQ-api-byte-parity stale-check retired in commit 4c39405 | ✓ PASS (override) |

### Requirements Coverage

| Requirement                | Source Plan(s)             | Description                                                                                                                                                              | Status                  | Evidence                                                                                                                                                                                                                                                                                  |
| -------------------------- | -------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| REQ-frontend-route-set     | 05-01, 05-02, 05-03, 05-04, 05-05 | Frontend serves `/{db}/{table}` and `/{db}/{table}/{pk}` (Phase-5 portion of REQ; remaining /-/search, /developers, etc. are Phase-6).                                  | ✓ SATISFIED             | Both routes registered in main.py; both return 200 with rendered HTML for valid inputs (test_routes_table_returns_200_feed_mode + test_row_returns_200_article_mode); both honor hidden-table 404 guard.                                                                              |
| REQ-frontend-data-via-http | 05-01, 05-02, 05-03, 05-05 | Frontend reads data exclusively via internal HTTP to datasette.                                                                                                          | ✓ SATISFIED             | datasette_client.py:fetch_table and fetch_row issue HTTP GET via httpx.AsyncClient against `http://zeeker-datasette:8001/{db}/{table}[/.{pk}].json`. No SQLite client in frontend container (Phase-2 contract preserved).                                                                |
| REQ-api-byte-parity        | 05-01, 05-02, 05-05        | API responses preserve byte-for-byte parity vs phase-03-pre baselines.                                                                                                   | ✓ SATISFIED (override)  | Operator triage at ship-checkpoint 2026-04-25: 6 byte-diffs all Category B (env drift) or Category C (intentional metadata.json content evolution); zero Category-A regressions. Per Phase-3 stale-check-retirement-in-same-phase decision, verifier check #11 retired in commit 4c39405 with full justification in 05-DEPLOY-NOTES.md ship-checkpoint outcome table. |
| REQ-eliminate-template-drift | 05-01, 05-02, 05-03, 05-04, 05-05 | Single frontend codebase owns all HTML; no V1/V2 template drift on Phase-5 surfaces.                                                                                  | ✓ SATISFIED             | Single table.html + row.html mode-dispatch shells in `packages/zeeker-frontend`. No `_table-{db}-{table}.html` overrides per D-01. No drift to legacy datasette templates for these routes (Caddy @datasette suffix matcher only catches `*.json/.csv/.db//-/*`; HTML routes are frontend territory). |

**No orphaned requirements.** All 4 phase-mapped requirement IDs are claimed by at least one plan and have implementation evidence.

### Anti-Patterns Found

| File                                              | Line(s)        | Pattern                                                                                            | Severity | Impact                                                                                                                                                                                                                                                                          |
| ------------------------------------------------- | -------------- | -------------------------------------------------------------------------------------------------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| routes_table.py                                   | 37, 63         | `dict(request.query_params)` collapses repeated keys                                                | ⚠️ Warning (WR-01 from 05-REVIEW) | Multi-valued params (e.g. `?_facet=category&_facet=date` or `?category=A&category=B`) silently drop all but the last value before forwarding to upstream. Feature regression vs Datasette behavior, NOT a security bug. Already documented in 05-REVIEW.md WR-01 with fix recipe; carry-forward fix recommended for follow-up but not gating ship. |
| datasette_client.py                               | 109            | `pk` segment forwarded to upstream f-string without URL-encoding                                    | ⚠️ Warning (WR-02 from 05-REVIEW)  | `pk` containing `#`, `?`, or stray spaces would route to a different endpoint via fragment/query interpretation. Frontend-internal blast radius (confusing 404s, not exploitable beyond that). Already documented in 05-REVIEW.md WR-02; fix is simple `quote(pk, safe="")`. |
| _partials/pagination.html                         | 14             | `('_size=' ~ n) in request_qs` substring check                                                      | ⚠️ Warning (WR-03 from 05-REVIEW)  | False positive: `_size=50` matches if querystring contains `_size=500`. Cosmetic confusion; not security or correctness gap.                                                                                                                                                  |
| routes_table.py + routes_row.py                   | 22-24 (both); 16-19 + 14-18 (both) | `_db_title` helper duplicated; `_HIDDEN_TABLE_PREFIXES`/_SUFFIXES duplicated                       | ℹ️ Info (IN-01, IN-02 from 05-REVIEW) | Two copies of identical code; risk of drift on the security-adjacent hidden-table list. Move to shared module.                                                                                                                                                              |
| _partials/applied_facets.html (driven by routes_table.py:73-76) | n/a | `applied_filters` accepts any non-underscore key as "column filter" — chips render for `utm_source`/`gclid` etc. | ⚠️ Warning (WR-05 from 05-REVIEW) | UX clutter — analytics params and copy-paste tracking params render as fake "column filter" chips. Autoescape neutralizes XSS via the displayed value. Fix: cross-reference against `payload.columns` before rendering chip.                                                |

**No critical (blocker) anti-patterns.** All warnings are operator-acknowledged in the 05-REVIEW.md report and are non-gating for ship per the operator's deferred-deploy posture.

### Human Verification Required

Although programmatic checks pass 7/7, the goal includes **visual fidelity to locked sketch designs** (sketches 002-B, 003-A, 003-B, 004-A) and **interaction-state verification** (drop cap, sticky positioning, native `<details>` toggle behavior) which automated tests cannot judge.

#### 1. Visual sweep — feed mode parity with sketch 004-A

**Test:** Visit `http://localhost/sglawwatch/headlines` with the local docker stack running.
**Expected:** Feed cards render with terracotta uppercase mono kicker, italic-accent H1 (last word in `<em>` colored ink-accent), hero with petrol stat band, sticky toolbar with FTS field + applied-chip strip + CSV/JSON export anchors, two-column layout (feed-main 1fr + 240px sticky `.facets` sidebar with category facet block), pagination strip with `Next →` and `Show: 25 · 50 · 100` size selector.
**Why human:** Visual fidelity to locked sketch design contract; programmatic checks confirm CSS classes and template structure exist but cannot judge whether the rendered pixels match the sketch.

#### 2. Visual sweep — article mode parity with sketch 003-A

**Test:** Click into a headline row → `/sglawwatch/headlines/{pk}`.
**Expected:** Row article layout shows kicker, italic-accent H1, byline+date in mono, Fraunces opsz 11 body with drop cap on first paragraph (terracotta-accent, 4em line-height 0.9 floated left), sticky aside (280px) with Record `<dl>` + Export links (View as JSON ↗, Download CSV ↗) + back-link, source coda double-rule.
**Why human:** Drop-cap typographic effect, sticky positioning behavior, and Fraunces opsz tuning require human eyes against sketch 003-A.

#### 3. Visual sweep — judgment mode parity with sketch 003-B

**Test:** Capture a real PK from `/Zeeker-Judgements/judgments.json?_size=1` and visit `/Zeeker-Judgements/judgments/{pk}`.
**Expected:** Dark `.dateline` strip with ochre court agency + mono date, italic-accent H1, 4-column `.judgment-meta` grid, Fraunces body, `.tag-chip` row from subject_tags (JSON-array-string parsed), `.coda` double-rule with source link + fingerprint.
**Why human:** Editorial broadsheet visual rhythm; live judgment data may produce edge cases (long case_name overflow, missing court, malformed subject_tags JSON) that need visual review.

#### 4. End-to-end verifier against running stack

**Test:** `docker compose up -d --build && bash scripts/verify_phase_05.sh`
**Expected:** Exit 0 (with API-parity check #11 already retired). All 14 sections A-N green.
**Why human:** Integration tests use MockTransport against fixture data; only a real live stack with real datasette + real S3 metadata can validate end-to-end behavior. Operator already ran this on 2026-04-25 (per 05-DEPLOY-NOTES.md ship-checkpoint outcome); included for re-run if any code changes land.

#### 5. Longform-list mode parity

**Test:** Visit `/sglawwatch/about_singapore_law` (table_mode: longform-list) and click a row → `/sglawwatch/about_singapore_law/{pk}` (row_mode: longform).
**Expected:** Table page shows va-feed.longform-list without category pills (kicker styled muted), longer truncate(180) excerpts. Row page shows `.article.read.longform-article` reading column ONLY — no `.aside`, no drop cap (CSS `all: unset` on first-letter).
**Why human:** Longform mode is a sub-variant that downplays categorization chrome; visual confirmation that the muted styling and absent sidebar produce the intended editorial reading-room feel.

#### 6. Tabular fallback row view with native `<details>` long-text expand

**Test:** Visit a row whose primary content field exceeds 200 chars in tabular fallback mode (any sg-gov-newsrooms `*_news` row falls back to article mode now since they all have display.row_mode set; need a hint-less table — operator may need to manually clear a display.row_mode in metadata.json or pick a wildcard `*` table).
**Expected:** Native `<details><summary>Show full content</summary>` renders. Clicking summary reveals full content; `.row-dd-preview` hides via `[open] .row-dd-preview { display: none }` CSS rule.
**Why human:** Native interaction state + the `[open]` sibling-hide CSS rule combine to produce a click-to-expand pattern; programmatic tests confirm the markup but the interaction-state styling needs a real click.

### Gaps Summary

**No blocking gaps.** All 7 must-have truths verified programmatically. All 5 ROADMAP success-criteria-equivalent goals (table 200 + row 200 + facets/pagination/sort/FTS + export-direct + mode-dispatch + hidden-table guard + Cache-Control + next_url rewrite) have implementation evidence in code, templates, tests, and CSS. All 4 phase-mapped requirements satisfied (REQ-api-byte-parity via override; rest via direct evidence).

**One override applied:** REQ-api-byte-parity is satisfied-by-retirement per the operator's locked Phase-3 stale-check-retirement-in-same-phase decision. The 6 byte-diffs at ship-checkpoint were all Category B (env drift) or Category C (intentional metadata.json content evolution from Phase-4 platform branding rewrite + Phase-5 display.* blocks). Zero Category-A regressions. Verifier check #11 retired in commit 4c39405; full justification in 05-DEPLOY-NOTES.md ship-checkpoint outcome table. The override is valid and load-bearing.

**Five non-blocking warnings** carried forward from 05-REVIEW.md (WR-01 multi-valued param dropping, WR-02 pk URL-encoding hazard, WR-03 size-substring false positive, WR-04 striptags-not-sanitizer doc nit, WR-05 applied-filters cross-ref to columns). All operator-acknowledged in 05-REVIEW.md; none gate ship per the deferred-deploy posture.

**Six human verification items** required to complete the goal — sketch visual parity (sketches 002-B, 003-A, 003-B, 004-A, longform-list/longform variants) and native `<details>` interaction-state confirmation. These items are why status is `human_needed` rather than `passed`. Operator already completed an end-to-end verifier run on 2026-04-25 with green Phase-5-specific sections; the visual sweep portion is documented in 05-DEPLOY-NOTES.md pre-checkpoint operator checklist (six bullet visual-sweep items).

---

_Verified: 2026-04-25T16:30:00Z_
_Verifier: Claude (gsd-verifier)_
