---
phase: 5
slug: port-table-browse-row-view
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-25
---

# Phase 5 — UI Design Contract: Table Browse + Row View

> Visual and interaction contract for `/{db}/{table}` and `/{db}/{table}/{pk}` routes.
> Generated by gsd-ui-researcher. Verified by gsd-ui-checker.

**Canonical skill reference:** All palette, typography, spacing, and component decisions derive
from `sketch-findings-zeeker-datasette` (`.claude/skills/sketch-findings-zeeker-datasette/`).
The rules in that skill are LOCKED. This document applies them specifically to the two new routes
and specifies the interaction contracts not covered by Phase 4 carry-forwards.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (custom CSS via `zeeker.css`) |
| Preset | not applicable |
| Component library | none |
| Icon library | none (text/Unicode glyphs only — `›`, `·`, `↗`, `×`) |
| Font — display | Fraunces (self-hosted woff2 in `static/fonts/`) |
| Font — body | Inter (self-hosted woff2 in `static/fonts/`) |
| Font — mono | JetBrains Mono (self-hosted woff2 in `static/fonts/`) |

---

## Spacing Scale

Source: `sketch-findings-zeeker-datasette/references/theme-system.md` — 4px-based scale.
All values already defined as CSS custom properties in `zeeker.css`.

| Token | Value | Usage |
|-------|-------|-------|
| --space-1 | 4px | Icon gaps, pill padding (vertical), chip inner |
| --space-2 | 8px | Inline element gaps, facet-item padding |
| --space-3 | 12px | Breadcrumb, toolbar inner padding |
| --space-4 | 16px | Default element spacing, card padding |
| --space-5 | 20px | Facet block padding, statband cell |
| --space-6 | 24px | Row padding, section gutters |
| --space-8 | 32px | Hero grid gap, sidebar gap |
| --space-10 | 40px | Article body paragraph margin |
| --space-12 | 48px | Feed-layout gap |
| --space-16 | 64px | Hero vertical padding, article outer margins |
| --space-20 | 80px | Footer margin-top |

Exceptions:
- Sticky toolbar `top: 52px` (nav height — not a spacing token, a positional constant)
- Facet sidebar `top: 120px` (nav + toolbar height combined)
- Applied-facet chip `×` button: 24px touch target minimum

---

## Typography

Source: `sketch-findings-zeeker-datasette/references/theme-system.md`.
All sizes use pre-defined `--text-*` CSS custom properties in `zeeker.css`.

| Role | Family | Size token | Weight | Line Height | Usage |
|------|--------|-----------|--------|-------------|-------|
| Display / H1 | Fraunces | `--text-6xl` (clamp 3rem–6rem) | 400 | 0.95 | Table/row page H1 |
| Heading / H2–H3 | Fraunces | `--text-3xl` / `--text-2xl` | 500 | 1.1 | Feed card titles, section headings |
| Article body | Fraunces (opsz 11) | 1.125rem | 400 | 1.65 | Long-form body in article/judgment/longform row modes |
| Body / UI | Inter | `--text-sm` (0.875rem) | 400 | 1.5 | Facet labels, filter chips, descriptions, toolbar |
| Mono / meta | JetBrains Mono | `--text-xs` (0.75rem) | 400–600 | 1.7 | Kickers, dates, column names, facet counts, breadcrumb, stat labels |
| Display numbers | Fraunces | `--text-3xl` | 500 | 1.0 | Stat band numbers |

Additional rules (from skill):
- Italic-accent `<em>` on H1: `font-style: italic; color: var(--color-accent); font-weight: 500`
- Drop cap on first paragraph of `article-body` (article + longform row modes only, skip when body < 3 paragraphs)
- Max-width 62ch on all reading columns (article-body / longform body) — never wider

---

## Color

Source: `sketch-findings-zeeker-datasette/references/theme-system.md`.
All tokens already defined in `zeeker.css` `:root`.

| Role | Token | Hex | Usage |
|------|-------|-----|-------|
| Dominant (60%) | `--color-bg` | `#F5F2EA` | Page background, off-white paper |
| Secondary (30%) | `--color-bg-alt` | `#EEE9DC` | Toolbar, alternating section bands, row-hover background |
| Surface | `--color-surface` | `#FFFFFF` | Cards, sidebar aside blocks, facet panel |
| Ink / text | `--color-ink` | `#14201F` | All primary text, nav background |
| Accent / primary (10%) | `--color-accent` | `#0A4F55` | Reserved for specific elements — see below |
| Ochre | `--color-ochre` | `#C08A2E` | Speech category pills, stat band numbers, underline decoration |
| Terracotta | `--color-terracotta` | `#B5552F` | Announcement pills, kicker text |
| Text secondary | `--color-text-secondary` | `#3E4F4E` | Descriptions, aside values |
| Text muted | `--color-text-muted` | `#6E7A79` | Timestamps, column names, facet counts, mono labels |
| Border | `--color-border` | `#D9D3C4` | Separators, facet block dividers, toolbar hairlines |
| Destructive | none | — | No destructive actions in this phase |

**Accent (`--color-accent`) is reserved for:**
- Left-edge slider bar on `.row:hover::before`
- Active facet item background (`.facet-item.active`)
- Applied-facet chip background
- Active toolbar view-toggle button
- FTS `<mark>` highlight outline/underline
- Primary CTA button background
- Current page in breadcrumb
- H1 `<em>` italic-accent word
- Meta-column `border-top: 2px solid` rule
- `.aside-block` first `border-top: 2px solid` rule
- PK column text in tabular mode (`.cols .pk`)

**Category pill colors:** Press-release → petrol-soft; Speech → ochre-soft; Announcement → terracotta-soft; Newsletter → surface-sunken. Maximum four categories — do not add a fifth.

---

## Page Surfaces and Layout

### `/{db}/{table}` — Table Browse Page

**Always-present chrome (from Phase 4 `base.html`):**
- Dark editorial nav (`.db-nav`)
- Breadcrumb strip: Home › {db} › {table} (`.db-crumb`)
- Hero header with italic-accent H1 + meta-col DL (`.db-header`)
- Petrol stat band: row count + column count + (page size if filtered) (`.db-statband`)
- Sticky sub-toolbar at `top: 52px` (`.db-toolbar`)

**Toolbar contents (left → right):**
1. FTS search input (`?_search=`) — flex-grow, max-width 360px
2. Sort indicator (active column name + asc/desc, as text label — no view-toggle buttons needed here)
3. Applied-facet chips (each with `×` removal link) — wraps
4. Export links (right-aligned): "CSV ↗" and "JSON ↗" as suffix-routed direct links

**Export link construction (D-05):**
```
href="/{db}/{table}.csv?{current_querystring}"
href="/{db}/{table}.json?{current_querystring}"
```
These hit Caddy's `@datasette` matcher and never enter the frontend.

**Layout modes (driven by `display.table_mode` from metadata):**

#### `table_mode: feed` (sketch 004-A)
Two-column grid: main feed list + sticky facet sidebar.
```
.feed-layout { grid-template-columns: minmax(0,1fr) 240px; gap: var(--space-12); }
```
Feed list: `<article class="va-item">` per row — title (Fraunces 500), date (mono), category pill, excerpt (max 2 lines Inter), source host link.
Facet sidebar: sticky at `top: 120px`. Each facet column is a `.facet-block` with petrol rule-top on the first, border rule on subsequent. Facet items show label + count (mono --text-2xs). Applied → `.facet-item.active` (petrol bg, paper text). Up to 10 values shown; no "show all" in Phase 5 (deferred).

#### `table_mode: tabular` (default fallback, sketch 002-B base)
Standard `<table>` with `<thead>` column headers. Sortable columns: click header → `?_sort=col`, click again → `?_sort_desc=col`, click again → clear. Active sort column header underlined in accent. Rows use paper background; alternate rows use `--color-bg-alt` for zebra stripe.
Facets sidebar: same `.feed-layout` two-column grid as feed mode — sidebar renders when facets available regardless of table_mode.

#### `table_mode: longform-list`
Feed list without category pills. One editorial row per record: title (Fraunces) + muted meta line. No excerpt unless `body` column slot mapped. Same two-column layout with facet sidebar.

**Pagination (all modes):**
- Datasette native cursor tokens (`next_url`, `next` from JSON payload)
- Rendered as: `← Previous` (when previous URL available) and `Next →` (when `next_url` present)
- Page-size selector: 25 / 50 / 100 as `?_size=N` links, inline with pagination row
- No numbered pages
- Pagination strip sits below the content area, above the footer

**FTS search:**
- Submits `?_search={term}` — datasette native param
- Active search term displayed as a chip in the toolbar (dismissable with `×` → clears `_search` from URL)
- When `_search_highlight` data present in JSON rows, apply `<mark>` with ochre background + ink text (no accent — too heavy) on the highlighted fragment
- No results message: "No results for '{term}' in {table title}. Try broadening your search."

**Sort behavior (tabular mode):**
- Column header `<a>` links; active sort column shows ↑ or ↓ Unicode arrow suffix
- Sort state carries through pagination (`next_url` from datasette already includes `_sort`)

**Empty table state:**
- Heading: "No records found"
- Body: "This table has no data yet, or all records were filtered out. Try removing filters."
- No illustration — plain text centered in the content area.

---

### `/{db}/{table}/{pk}` — Row Detail Page

**Always-present chrome:**
- Dark editorial nav
- Breadcrumb strip: Home › {db} › {table} › {pk} (truncate pk to 12 chars + ellipsis if UUID/hash)
- Hero header (no stat band — per skill rule: "Reserve stat band for home / database / table; row page competes with article")

**Layout modes (driven by `display.row_mode` from metadata):**

#### `row_mode: article` (sketch 003-A — magazine variant)
Two-column article layout: `minmax(0,1fr) 280px`
- Left: kicker (mono terracotta) + H1 (italic-accent Fraunces) + byline dateline + `<div class="article-body">` with drop cap on first paragraph (when body ≥ 3 paragraphs) + source coda (double-rule divider variant B)
- Right: sticky `.aside` panel — `.aside-block` with petrol rule-top; `<dl>` of record metadata fields; export links (JSON / CSV direct suffix links)
- Body column: Fraunces opsz 11, 1.125rem, line-height 1.65, max-width 62ch

#### `row_mode: judgment` (sketch 003-B — editorial broadsheet)
Full-width centered column, max-width 62ch:
- Dark dateline strip below breadcrumb (`.dateline` — ink bg, ochre agency, mono date)
- H1 at `clamp(2.5rem, 6vw, 5rem)` Fraunces, italic-accent
- 4-column meta strip (court · citation · date · judges) below title
- Article body: Fraunces opsz 11 serif
- Subject tags as chip row below body
- Source coda: double-rule `border-top: 4px double var(--color-ink)` + centered source URL + fingerprint (SHA/id) in mono muted
- No sticky sidebar (judgment content IS the experience)

#### `row_mode: longform` (sketch 003-A variant without aside)
Reading column only, max-width 62ch, no sidebar:
- Kicker + H1 + simple meta line (Inter sm, muted)
- Article body: Fraunces opsz 11
- Source link-out when `item_url` / `source_url` column present
- No drop cap (longform-guide content rarely has a distinct "first paragraph")

#### `row_mode: tabular` (default fallback)
Key-value `<dl>` of all columns:
- `<dt>` in JetBrains Mono xs uppercase muted
- `<dd>` in Inter sm ink
- Long text fields (`len > 200`) get `<details>/<summary>` expand toggle ("Show full content / Collapse")
- Export sidebar: same `.aside` panel as article mode, right-aligned at 280px
- Layout: `minmax(0,1fr) 280px` two-column grid (reading dl + aside)

**Row export links (both modes with aside):**
- "View as JSON ↗" → `/{db}/{table}/{pk}.json`
- "Download CSV" → `/{db}/{table}.csv?{pk_filter_querystring}`
Both are suffix-routed via Caddy; no frontend processing.

**Navigation within table (row detail):**
- "← Back to {table title}" link in breadcrumb or as secondary link below kicker
- No prev/next row navigation in Phase 5 (datasette doesn't expose this cheaply via JSON)

---

## Component Inventory

All components are implemented directly in Jinja templates + `zeeker.css` (no external component library).

| Component | New in Phase 5 | CSS class(es) | Notes |
|-----------|---------------|---------------|-------|
| Feed layout grid | Yes | `.feed-layout` | 1fr + 240px sidebar |
| Feed card (va-item) | Yes | `.va-item`, `.va-item-head`, `.va-item-title`, `.va-item-excerpt`, `.va-item-foot` | sketch 004-A |
| Facet sidebar | Yes | `.facets`, `.facet-block`, `.facet-item`, `.facet-item.active` | Sticky |
| Applied-facet chips | Yes | `.filter-chip` | Pill with × removal |
| FTS search toolbar | Yes | `.db-toolbar-search` (reuse Phase 4 pattern) | Phase 4 already has base class |
| Pagination strip | Yes | `.pagination` | cursor-based prev/next + size selector |
| Tabular data table | Yes | `.data-table`, `.data-table thead th`, `.data-table tbody tr` | Sort header links |
| Article layout | Yes | `.article`, `.read`, `.article-body`, `.kicker`, `.byline` | sketch 003-A |
| Article drop cap | Yes | `.article-body p:first-of-type::first-letter` | ≥ 3 paragraphs only |
| Aside panel | Yes | `.aside`, `.aside-block` | Sticky metadata sidebar |
| Dark dateline strip | Yes | `.dateline` | judgment mode only |
| Subject tag chips | Yes | `.tag-chip` | judgment mode subject_tags |
| Source coda | Yes | `.coda` | Double-rule divider |
| Details/expand toggle | Yes | `<details><summary>` (browser native) | tabular long-text fields |
| Category pills | Existed (Phase 4 CSS) | `.cat-pill` + modifier | Already in zeeker.css line 699 |
| FTS badge | Existed (Phase 4 CSS) | `.fts-badge` | Already in zeeker.css line 715 |

**CSS delivery:** Phase 5 appends a `/* === TABLE BROWSE + ROW VIEW — phase 05 === */` section to `zeeker.css` following the Phase 4 `DATABASE EDITORIAL ROWS` section. No separate CSS file.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA (export) | "Download CSV ↗" / "View as JSON ↗" |
| Table page H1 kicker | "№ 01 · {table title}" |
| Row page H1 kicker | "{category value}" (feed/article) or "{court}" (judgment) or "Record" (tabular fallback) |
| Empty table heading | "No records found" |
| Empty table body | "This table has no data yet, or all records were filtered out. Try removing filters." |
| No FTS results | "No results for '{term}' in {table title}. Try broadening your search." |
| Facet section header | column name, uppercase mono (e.g. "CATEGORY", "COURT", "SOURCE") |
| Pagination — previous | "← Previous" |
| Pagination — next | "Next →" |
| Pagination — size label | "Show: 25 · 50 · 100" |
| Back to table link | "← Back to {table title}" |
| Aside export heading | "Export this record" |
| Aside record heading | "Record" |
| Long-text expand | "Show full content" / "Collapse" |
| Source coda label | "Original source" |
| No body content | (omit excerpt block entirely — do not write "No content available") |
| Error: table not found | "Table not found. The table '{table}' does not exist in the '{db}' database." |
| Error: row not found | "Record not found. No row with primary key '{pk}' in '{table}'." |

---

## Interaction Contracts

### Sort (tabular mode)
- Column header is `<a href="?_sort={col}">` when unsorted
- Active asc: `<a href="?_sort_desc={col}">` + ↑ suffix text
- Active desc: `<a href="/{db}/{table}">` (clear sort) + ↓ suffix text
- Carries `_search`, `_facet`, applied facet filters through sort toggle (append to querystring)

### Facet application
- Facet item click → navigate to `?{col}={val}&_facet={col}` (other active facets preserved)
- Applied facet chip `×` → navigate to URL with that facet value removed (other params preserved)
- Applied facets display as `.filter-chip` pills above the content list in the toolbar area
- Format: `{column}: {value} ×` in mono xs

### FTS search
- Search input `type="search"`, submits `GET` with `?_search={term}` appended
- Active search term displayed as dismissable chip in toolbar (`×` clears `_search`)
- FTS `<mark>` highlight: `background: rgba(192,138,46,0.25); color: var(--color-ink); border-radius: 2px`

### Pagination
- "← Previous" link: present only when `prev_url` available from datasette JSON
- "Next →" link: present only when `next_url` available from datasette JSON
- Page size: `?_size=25` / `?_size=50` / `?_size=100`; active size is current non-link text
- All other querystring params preserved when changing page size

### Row detail — long text expand
- Fields with value length > 200 chars truncate to 200 chars + `<details>` wrapping rest
- `<summary>` text: "Show full content"
- Expanded `<details>` attribute: `open` renders full text, `<summary>` text changes to "Collapse" via CSS `details[open] summary { display: none }` + pseudo-content trick is NOT used — just always render both states visible via JS toggle or native `<details>`.

### Transitions
- Feed card hover: `background: var(--color-bg-alt)` transition 0.15s ease (no translateY — cards are list items not floating cards)
- Editorial row hover (tabular table, longform-list): left-edge slider `::before` width/height transition 0.2s ease (per skill pattern)
- Facet item hover: `background: var(--color-surface-sunken)` 0.15s ease
- No other transitions

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — no shadcn |
| third-party | none | not applicable |

---

## CSS Harvest Required (Phase 5)

The existing `zeeker.css` (1102 lines) already contains category pills (line 699) and FTS badge (line 715) from Phase 4. Phase 5 must harvest and append the following CSS ranges from the legacy `static/css/zeeker-base.css`:

| Section | Source lines (approx) | Target class(es) |
|---------|----------------------|-----------------|
| Feed layout + `.va-item` cards | harvest from M1 `_table-*.html` + sketch 004-A | `.feed-layout`, `.va-item`, `.va-item-head`, `.va-item-title`, `.va-item-excerpt`, `.va-item-foot` |
| Facet sidebar | sketch 004-A CSS | `.facets`, `.facet-block`, `.facet-item` |
| Filter chips | new for Phase 5 | `.filter-chip` |
| Pagination strip | new for Phase 5 | `.pagination` |
| Tabular data table | harvest/new | `.data-table` |
| Article reading layout | sketch 003-A/B | `.article`, `.read`, `.article-body`, `.aside`, `.dateline`, `.coda` |
| Drop cap | sketch 003-A | `.article-body p:first-of-type::first-letter` |

Section delimiter: `/* =========== TABLE BROWSE + ROW VIEW — phase 05 ============ */`
Append directly after the `DATABASE EDITORIAL ROWS — phase 01` section.

---

## Pre-Population Sources

| Decision | Source |
|----------|--------|
| Palette, typography, spacing | `sketch-findings-zeeker-datasette/references/theme-system.md` (LOCKED) |
| Feed card anatomy | `sketch-findings-zeeker-datasette/references/directory-and-feed-lists.md` (LOCKED) |
| Row reading layouts (article/judgment/longform) | `sketch-findings-zeeker-datasette/references/row-reading-layouts.md` (LOCKED) |
| Shell chrome (nav, breadcrumb, hero, stat band, toolbar) | `sketch-findings-zeeker-datasette/references/shell-and-chrome.md` (LOCKED) |
| table_mode / row_mode separation (D-03) | `05-CONTEXT.md` (LOCKED) |
| Tabular fallback (D-04) | `05-CONTEXT.md` (LOCKED) |
| Export direct suffix routing (D-05) | `05-CONTEXT.md` (LOCKED) |
| No inline SQL editor (D-06) | `05-CONTEXT.md` (LOCKED — deferred to Phase 6) |
| Facets / pagination / FTS / sort UX | `05-CONTEXT.md §Claude's Discretion` (pre-populated per context recommendation) |
| Route handler pattern, Cache-Control, hidden filter | `04-CONTEXT.md carry-forwards` |
| CSS append pattern, zeeker.css structure | `packages/zeeker-frontend/src/zeeker_frontend/static/css/zeeker.css` inspection |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
