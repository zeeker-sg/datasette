---
phase: 07-prune-zeeker-datasette
plan: 02
type: execute
wave: 1
depends_on:
  - 07-01
files_modified:
  - metadata.json
  - .planning/baselines/phase-07-pre/
  - scripts/verify_phase_03.sh
  - scripts/verify_phase_04.sh
  - scripts/verify_phase_06.sh
autonomous: false
requirements:
  - REQ-api-byte-parity
  - REQ-frontend-route-set
must_haves:
  truths:
    - "metadata.json no longer carries `extra_css_urls` (referenced 2 deleted CSS paths) or `extra_js_urls` (referenced 3 deleted JS paths)."
    - "metadata.json STILL carries `menu_links` array with 5 frontend routes — frontend `base.html` reads this via fetch_site_metadata to render the dark editorial nav."
    - "metadata.json STILL carries `plugins.datasette-search-all` block — D-01 keeps `/-/search` reachable as a developer surface."
    - "metadata.json STILL carries `databases.*` config (allow_sql/facet/download wildcard) — data-layer config, not UI."
    - "`.planning/baselines/phase-07-pre/` contains 13+ JSON baselines captured against the post-metadata-edit stack via Caddy on `http://localhost`."
    - "verify_phase_03.sh, verify_phase_04.sh, verify_phase_06.sh baseline-cascade arrays prepend `phase-07-pre` so subsequent verifier runs prefer it over `phase-06-pre`."
  artifacts:
    - path: "metadata.json"
      provides: "Cleaned base metadata for Datasette image — UI overlay refs gone, frontend nav config retained"
      contains: "menu_links"
      negative_pattern: "extra_css_urls|extra_js_urls"
    - path: ".planning/baselines/phase-07-pre/-_metadata.json.json"
      provides: "Post-edit /-/metadata.json baseline for byte-parity diff"
    - path: "scripts/verify_phase_03.sh"
      provides: "Verifier preferring phase-07-pre baseline"
      pattern: "phase-07-pre phase-06-pre"
    - path: "scripts/verify_phase_04.sh"
      provides: "Verifier preferring phase-07-pre baseline"
      pattern: "phase-07-pre phase-06-pre"
    - path: "scripts/verify_phase_06.sh"
      provides: "Verifier preferring phase-07-pre baseline"
      pattern: "phase-07-pre phase-06-pre"
  key_links:
    - from: "frontend base.html"
      to: "metadata.menu_links"
      via: "fetch_site_metadata + Jinja loop"
      pattern: "metadata\\.menu_links"
    - from: "scripts/verify_api_parity.sh"
      to: ".planning/baselines/phase-07-pre/"
      via: "ZEEKER_BASELINE_DIR env var"
      pattern: "phase-07-pre"
---

<objective>
Wave 1: clean `metadata.json` of the two UI-overlay reference arrays (`extra_css_urls` + `extra_js_urls`), capture a fresh `phase-07-pre/` baseline against the post-edit stack via Caddy, then update the baseline-cascade arrays in three verifier scripts so they prefer the new baseline.

Purpose: After this plan ships, `/-/metadata.json` no longer references files that Plan 07-04 will delete. Capturing the baseline AFTER the edit but BEFORE the mass-delete (per 07-RESEARCH Q1 lock) means the post-Plan-07-04 byte-parity diff is clean — no false-positive Category-D drift to triage. The frontend nav still renders because `menu_links` is preserved verbatim.

Output: One commit edits metadata.json (rollback = git revert); one commit captures + commits 13+ baseline JSON files + sidecar `.url` files + README; one commit updates the three verifier baseline-cascade arrays.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07-prune-zeeker-datasette/07-RESEARCH.md
@.planning/phases/07-prune-zeeker-datasette/07-VALIDATION.md
@metadata.json
@scripts/capture_baseline.sh
@scripts/verify_phase_03.sh
@scripts/verify_phase_04.sh
@scripts/verify_phase_06.sh

<interfaces>
<!-- metadata.json — current shape, lines 1-273. Only top-level keys named here.
     Verified via Read on 2026-04-26.
-->

Top-level keys present today (in source order):
  - title          (KEEP — string)
  - description    (KEEP — string)
  - license        (KEEP — string)
  - license_url    (KEEP — string)
  - source         (KEEP — string)
  - source_url     (KEEP — string)
  - about          (KEEP — string)
  - about_url      (KEEP — string)
  - databases      (KEEP — object; sg-gov-newsrooms, sglawwatch, Zeeker-Judgements, "*")
  - plugins        (KEEP — object; datasette-search-all)
  - extra_css_urls (DELETE — array of 2; both /static/css/* paths)
  - extra_js_urls  (DELETE — array of 3; all /static/js/* paths)
  - menu_links     (KEEP — array of 5; consumed by frontend base.html lines 18-25)

Frontend consumer of menu_links (verified):
  packages/zeeker-frontend/src/zeeker_frontend/templates/base.html:18
    {% if metadata and metadata.menu_links %}
  packages/zeeker-frontend/src/zeeker_frontend/templates/base.html:19
    {% for link in metadata.menu_links %}

The 60s TTL cache on /-/metadata.json (datasette_client.py.fetch_site_metadata)
means the post-edit metadata.json takes effect in the frontend within 60s of
the datasette container restart. Plan 07-05's UAT verifies this.
</interfaces>

<baseline_capture_contract>
<!-- scripts/capture_baseline.sh contract (verified by reading the file).
     Inputs:  ZEEKER_BASELINE_URL (default http://localhost:8001)
              ZEEKER_BASELINE_DIR (default .planning/baselines/phase-03-pre)
     Outputs: One <safe>.json per probed URL + matching <safe>.url sidecar
              file holding the original path (so verify_api_parity.sh can
              re-derive the URL); plus a README.md and a .gitkeep marker.
     Probed URLs:
       /-/versions.json (always)
       /-/metadata.json (always — THIS is the file whose body changes after the edit)
       /-/plugins.json  (always)
       /.json           (always)
       For each db in /.json keys: /{db}.json?_size=10
       For each (first 2) tables of {db}: /{db}/{t}.json?_size=10
     Stable strip filter: walk(del(.query_ms, .__time__, .request_duration_ms))

     Phase-7 capture command (the exact invocation Task 2 runs):
       ZEEKER_BASELINE_URL=http://localhost \
       ZEEKER_BASELINE_DIR=/Users/houfu/Projects/zeeker-datasette/.planning/baselines/phase-07-pre \
       bash scripts/capture_baseline.sh

     Note: BASE URL is http://localhost (Caddy on :80), NOT :8001 — Phase 2+
     made datasette internal-only. Capturing through Caddy proves the frontend
     baseline matches what /verify_api_parity.sh will later diff against.
</baseline_capture_contract>

<verifier_baseline_cascade>
<!-- The three verifier scripts each contain a `for cand in ...` loop that picks
     the most-recent baseline directory present locally. Currently each lists
     `phase-06-pre phase-05-pre phase-04-pre phase-03-pre`. Plan 07-02 prepends
     `phase-07-pre` so the new baseline takes precedence.

     Exact lines (verified via Read):
       scripts/verify_phase_03.sh:249
         for cand in phase-06-pre phase-05-pre phase-04-pre phase-03-pre; do
       scripts/verify_phase_04.sh:174
         for cand in phase-06-pre phase-05-pre phase-04-pre phase-03-pre; do
       scripts/verify_phase_06.sh:244
         for cand in phase-06-pre phase-05-pre phase-04-pre phase-03-pre; do

     The cascade falls through if the preferred dir isn't on disk; the
     newly-prepended `phase-07-pre` only takes effect after Task 2 commits
     the baseline directory.
</verifier_baseline_cascade>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Edit metadata.json — drop extra_css_urls + extra_js_urls (keep everything else)</name>
  <files>metadata.json</files>
  <read_first>
    - metadata.json entire file (273 lines) — currently in context from the planner; the deletion targets are lines 242-250 (extra_css_urls) and lines 246-250 (extra_js_urls).
    - 07-RESEARCH §"The metadata.json edits are the highest-risk part of this phase" paragraph — verifies KEEP list (menu_links, plugins.datasette-search-all, databases.*).
  </read_first>
  <action>
    Edit `metadata.json` to remove TWO top-level keys: `extra_css_urls` (current value: array of 2 paths) and `extra_js_urls` (current value: array of 3 paths). Both arrays exclusively reference `/static/...` paths that Plan 07-04 will delete from the image build context.

    Exact deletions (verified line numbers from the current file):
    - Lines 242-245 — the entire `"extra_css_urls": [ ... ]` block including the comma at end of line 241 / start of the deletion. Today this block reads:
      ```
        "extra_css_urls": [
          "/static/css/vendor/prism.css",
          "/static/css/zeeker-base.css"
        ],
      ```
    - Lines 246-250 — the entire `"extra_js_urls": [ ... ]` block. Today:
      ```
        "extra_js_urls": [
          "/static/js/vendor/prism-core.min.js",
          "/static/js/vendor/prism-sql.min.js",
          "/static/js/zeeker-base.js"
        ],
      ```

    After deletion, line 241 (`"plugins": { ... }` block close: `},`) must be followed directly by line 251 (`"menu_links": [ ... ]`). Adjust the trailing comma after the `plugins` block close so the resulting JSON is valid (the `plugins` close should keep its comma since `menu_links` follows).

    DO NOT touch any other key. Specifically PRESERVE verbatim:
    - `"menu_links": [...]` array of 5 objects (lines 251-272) — frontend `base.html` line 18-25 iterates this.
    - `"plugins": {"datasette-search-all": {...}}` block — D-01 keeps `/-/search` (provided by datasette-search-all) reachable as a developer surface.
    - `"databases": {...}` block including the `"*"` wildcard with `allow_sql/allow_facet/allow_download` and the `_zeeker_*` hidden-table declarations — pure data-layer config.
    - All top-level scalars (`title`, `description`, `license`, `license_url`, `source`, `source_url`, `about`, `about_url`).

    Validate the result is well-formed JSON: `python -c "import json; json.load(open('metadata.json'))"` must exit 0. Validate it has the expected key set: the top-level keys must be exactly `{title, description, license, license_url, source, source_url, about, about_url, databases, plugins, menu_links}` — 11 keys (down from 13).

    Single-file commit; rollback is `git revert <commit>`.
  </action>
  <verify>
    <automated>python3 -c "import json; data=json.load(open('metadata.json')); assert 'extra_css_urls' not in data, 'extra_css_urls still present'; assert 'extra_js_urls' not in data, 'extra_js_urls still present'; assert 'menu_links' in data, 'menu_links missing'; assert len(data['menu_links']) == 5, f'menu_links count {len(data[\"menu_links\"])} != 5'; assert 'datasette-search-all' in data['plugins'], 'datasette-search-all plugin block missing'; assert '*' in data['databases'], 'wildcard databases.* config missing'; print('metadata.json shape OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "import json; json.load(open('metadata.json'))"` exits 0 (valid JSON).
    - `! grep -q 'extra_css_urls' metadata.json` (key fully removed).
    - `! grep -q 'extra_js_urls' metadata.json` (key fully removed).
    - `grep -q '"menu_links"' metadata.json` (key preserved).
    - `grep -q '"datasette-search-all"' metadata.json` (plugin block preserved).
    - `grep -q '"allow_sql": true' metadata.json` (databases wildcard preserved).
    - `python3 -c "import json; print(len(json.load(open('metadata.json'))))"` returns `11` (top-level key count down from 13).
    - Frontend `base.html` rendering proof: after `docker compose up -d --build zeeker-datasette`, `curl -fsS http://localhost/-/metadata.json | jq -r '.menu_links | length'` returns `5`. (Run only if local stack is up.)
  </acceptance_criteria>
  <done>metadata.json shape: 11 top-level keys, no `extra_*_urls`, `menu_links` + plugins + databases blocks intact.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2 (a): Bring local stack up against post-edit metadata.json before baseline capture</name>
  <files>(no file modifications — operator-action checkpoint)</files>
  <what-built>
    Task 1 edited metadata.json. The new shape will only appear in `/-/metadata.json` after the datasette container is rebuilt and restarted. The baseline capture in Task 2(b) MUST run against the rebuilt stack — capturing against the old image would record the pre-edit body and defeat the whole point of re-baselining.
  </what-built>
  <action>
    Operator action (NOT autonomous): bring up the rebuilt local stack so Task 2(b) captures the baseline against the post-edit metadata.json. The exact commands the operator runs are listed in `<how-to-verify>` below — `docker compose up -d --build zeeker-datasette` + `docker compose ps` healthcheck wait + the 3-line jq sanity check. This task pauses execution until the operator types "approved".
  </action>
  <how-to-verify>
    Operator runs (or confirms already running):
    ```
    docker compose up -d --build zeeker-datasette
    docker compose ps zeeker-datasette         # → running, healthy
    curl -fsS http://localhost/-/metadata.json | jq 'has("extra_css_urls"), has("extra_js_urls"), .menu_links | length'
    ```
    Expected output:
    ```
    false
    false
    5
    ```

    If `extra_css_urls` or `extra_js_urls` shows true: the container is still serving the old metadata.json (rebuild didn't pick up the file edit). Re-run `docker compose up -d --build zeeker-datasette --force-recreate` and re-check.

    If `menu_links` length is not 5: Task 1 over-deleted; revert and redo.

    Once all three assertions match, type "approved" to proceed to Task 2(b).
  </how-to-verify>
  <verify>
    <automated>curl -fsS http://localhost/-/metadata.json | jq -e 'has("extra_css_urls") | not' && curl -fsS http://localhost/-/metadata.json | jq -e 'has("extra_js_urls") | not' && curl -fsS http://localhost/-/metadata.json | jq -e '.menu_links | length == 5'</automated>
  </verify>
  <resume-signal>Type "approved" once `curl -fsS http://localhost/-/metadata.json | jq 'has("extra_css_urls"), has("extra_js_urls"), .menu_links | length'` returns `false / false / 5`.</resume-signal>
  <acceptance_criteria>
    - Operator confirmation that local stack is serving the post-edit metadata.json shape (verified via the three jq assertions above).
    - `docker compose ps zeeker-datasette` reports state `running` and health `healthy` (Plan 07-04's mass delete will rebuild the image again from scratch; this baseline only needs the metadata edit to be visible).
  </acceptance_criteria>
  <done>Local stack confirmed serving post-edit `/-/metadata.json`; ready to capture baselines.</done>
</task>

<task type="auto">
  <name>Task 2 (b): Capture phase-07-pre baseline through Caddy</name>
  <files>.planning/baselines/phase-07-pre/</files>
  <read_first>
    - scripts/capture_baseline.sh entire file — Phase-7 uses the existing parameterized script unchanged.
    - .planning/baselines/phase-06-pre/ directory listing (`ls .planning/baselines/phase-06-pre/`) — confirm what file naming pattern to expect (`<safe>.json` + `<safe>.url` pairs + README + .gitkeep).
  </read_first>
  <action>
    Run the existing baseline-capture script against the post-Task-1-edit local stack via Caddy. Exact invocation (per the contract documented in capture_baseline.sh's BASE_URL + OUT_DIR env vars):

    ```
    ZEEKER_BASELINE_URL=http://localhost \
    ZEEKER_BASELINE_DIR="$(git rev-parse --show-toplevel)/.planning/baselines/phase-07-pre" \
    bash scripts/capture_baseline.sh
    ```

    Expected behaviour (verified by reading capture_baseline.sh):
    - Creates `.planning/baselines/phase-07-pre/` if absent.
    - Probes `/-/versions.json` first as a reachability check; aborts if Caddy isn't on :80.
    - Captures the always-list endpoints: `/-/versions.json`, `/-/metadata.json`, `/-/plugins.json`, `/.json`.
    - Discovers databases via `curl /.json | jq -r 'keys[]?'` (current state: `sg-gov-newsrooms`, `sglawwatch`, `Zeeker-Judgements`).
    - For each database: captures `/{db}.json?_size=10` + first 2 tables' `/{db}/{t}.json?_size=10`.
    - Strips `query_ms`, `__time__`, `request_duration_ms` via the JQ_STRIP filter.
    - Writes a sidecar `<safe>.url` for each `<safe>.json` so verify_api_parity.sh can re-derive the path.
    - Drops a README.md and a .gitkeep.

    Expected output count: with 3 databases and ~2 tables each, the directory should contain ~13 `.json` files + ~13 `.url` files + 1 README.md + 1 .gitkeep ≈ 28 files total.

    After the script completes:
    1. Verify the metadata baseline reflects the post-Task-1 edit:
       ```
       jq 'has("extra_css_urls"), has("extra_js_urls"), .menu_links | length' \
         .planning/baselines/phase-07-pre/-_metadata.json.json
       ```
       Must print `false / false / 5`.
    2. Verify the always-list endpoints captured:
       ```
       ls .planning/baselines/phase-07-pre/-_versions.json.json \
          .planning/baselines/phase-07-pre/-_metadata.json.json \
          .planning/baselines/phase-07-pre/-_plugins.json.json \
          .planning/baselines/phase-07-pre/_.json.json
       ```
       (the last filename is `/.json` → safe-encoding `_.json` per the `tr '/?=&' '____'` substitution + leading-underscore strip; verify the actual filename in the dir if this doesn't match — capture_baseline.sh's exact safe-encoding may vary.)

    `git add .planning/baselines/phase-07-pre/` and commit. The directory + sidecar files are committed to git so verify_api_parity.sh has a stable reference.

    NOTE on byte stability: the sg-gov-newsrooms `*_news` tables are subject to daily-import drift (see Phase-6 SHIPPED notes — Category-B drift). This baseline captures the rows AS OF NOW; if Plan 07-05's verifier run later reports diffs on those tables, the four-category triage at the deploy checkpoint distinguishes Category-B (data drift, expected) from Category-D (regression, blocking ship).
  </action>
  <verify>
    <automated>test -d .planning/baselines/phase-07-pre && test -f .planning/baselines/phase-07-pre/README.md && jq -e '.menu_links | length == 5' .planning/baselines/phase-07-pre/-_metadata.json.json >/dev/null && jq -e 'has("extra_css_urls") | not' .planning/baselines/phase-07-pre/-_metadata.json.json >/dev/null && [ "$(ls .planning/baselines/phase-07-pre/*.json | wc -l)" -ge 10 ]</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/baselines/phase-07-pre/` directory exists with at least 10 `.json` files (3 always-list + 4 db-list + at least 3 db.json + first-table-of-each-db ≈ 13).
    - `.planning/baselines/phase-07-pre/README.md` exists (auto-generated by capture_baseline.sh).
    - `jq 'has("extra_css_urls")' .planning/baselines/phase-07-pre/-_metadata.json.json` returns `false`.
    - `jq 'has("extra_js_urls")' .planning/baselines/phase-07-pre/-_metadata.json.json` returns `false`.
    - `jq '.menu_links | length' .planning/baselines/phase-07-pre/-_metadata.json.json` returns `5`.
    - Every `.json` file in the directory has a matching `.url` sidecar (capture_baseline.sh contract): `for f in .planning/baselines/phase-07-pre/*.json; do [ -f "${f%.json}.url" ] || echo "MISSING SIDECAR: $f"; done` produces no output.
    - The directory is committed to git: `git ls-files .planning/baselines/phase-07-pre/ | wc -l` returns ≥ 10.
  </acceptance_criteria>
  <done>phase-07-pre baseline directory captured + committed; metadata baseline reflects post-edit shape; sidecar `.url` files preserved for verify_api_parity.sh.</done>
</task>

<task type="auto">
  <name>Task 3: Prepend phase-07-pre to baseline cascade in 3 verifier scripts</name>
  <files>scripts/verify_phase_03.sh, scripts/verify_phase_04.sh, scripts/verify_phase_06.sh</files>
  <read_first>
    - scripts/verify_phase_03.sh line 249 region (the `for cand in ... do` loop in section G/Parity)
    - scripts/verify_phase_04.sh line 174 region (the `for cand in ... do` loop in section G)
    - scripts/verify_phase_06.sh line 244 region (the `for cand in ... do` loop in section K)
  </read_first>
  <action>
    Edit three lines, one per verifier script. Each currently reads:
    ```
    for cand in phase-06-pre phase-05-pre phase-04-pre phase-03-pre; do
    ```
    Replace each with:
    ```
    for cand in phase-07-pre phase-06-pre phase-05-pre phase-04-pre phase-03-pre; do
    ```

    Exact files + line targets (verified):
    - `scripts/verify_phase_03.sh` line 249 (in section G — API byte-parity wrap)
    - `scripts/verify_phase_04.sh` line 174 (in section G — API byte-parity wrap)
    - `scripts/verify_phase_06.sh` line 244 (in section K — API byte-parity wrap)

    The cascade behaviour (verified by reading the surrounding `for ... do; if [ -d "$ROOT/.planning/baselines/$cand" ]; then ...; break; fi; done`): once `phase-07-pre/` is present locally, every subsequent verifier run picks it. If a future operator clones the repo without running `git pull` for the baseline, the cascade falls through to whichever older baseline IS present, so older-checkout compatibility is preserved.

    DO NOT touch the surrounding code (the `if [ -d ... ] then break fi done` shell logic must stay verbatim). DO NOT change verify_phase_05.sh (it doesn't have this cascade — verified by `grep -n 'for cand in phase-' scripts/verify_phase_05.sh` returning empty).

    `bash -n` must validate each script after edit.

    Single commit covering all three files.
  </action>
  <verify>
    <automated>bash -n scripts/verify_phase_03.sh && bash -n scripts/verify_phase_04.sh && bash -n scripts/verify_phase_06.sh && grep -c 'phase-07-pre phase-06-pre phase-05-pre phase-04-pre phase-03-pre' scripts/verify_phase_03.sh scripts/verify_phase_04.sh scripts/verify_phase_06.sh | grep -v ':0$' | wc -l | grep -qx 3</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/verify_phase_03.sh && bash -n scripts/verify_phase_04.sh && bash -n scripts/verify_phase_06.sh` all exit 0.
    - All three scripts contain the line `for cand in phase-07-pre phase-06-pre phase-05-pre phase-04-pre phase-03-pre; do`: `grep -lE 'for cand in phase-07-pre phase-06-pre' scripts/verify_phase_*.sh | wc -l` returns `3`.
    - `scripts/verify_phase_05.sh` is UNTOUCHED in this plan: the file's mtime is older than the most recent commit's timestamp, OR `git status scripts/verify_phase_05.sh` shows no modification.
    - Smoke-run: `bash scripts/verify_phase_06.sh` exits 0 against the local stack — Section K should now report `verify_api_parity.sh exit 0 against phase-07-pre` (not phase-06-pre).
  </acceptance_criteria>
  <done>All three verifiers prefer the new phase-07-pre baseline; older baselines remain as fallthrough for older checkouts; verify_phase_05.sh is intentionally untouched (it has no cascade and no parity wrap).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev workstation → metadata.json (image input) | Edit affects every container restart's served metadata; load-bearing for frontend nav |
| live datasette → frontend (HTTP, internal docker network) | metadata.menu_links flows from datasette → frontend via fetch_site_metadata |
| baseline capture → verify_api_parity.sh | Captured bytes become the immutable reference; capturing against the wrong stack contaminates every subsequent verifier run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Tampering | metadata.json menu_links accidentally dropped | mitigate | Acceptance criterion explicitly asserts `menu_links` length == 5 + `grep -q '"menu_links"'` post-edit; Task 2(a) human-verify gate independently checks `jq '.menu_links \| length'` returns 5 against the running stack BEFORE baseline capture |
| T-07-02-02 | Repudiation | baseline captured against stale image | mitigate | Task 2(a) is a blocking human-verify checkpoint that confirms the post-edit metadata is being served BEFORE Task 2(b) runs the capture; jq assertions on the captured baseline file (acceptance criteria) double-check |
| T-07-02-03 | Tampering (HIGH) | extra_*_urls removal accidentally drops a non-UI plugin's stylesheet | mitigate | The two arrays today exclusively contain `/static/css/*` and `/static/js/*` paths (verified line-by-line in metadata.json lines 242-250 — every entry begins with `/static/`); no plugin-injected URLs present; deleting them is safe by inspection |
| T-07-02-04 | DoS | malformed JSON breaks Datasette boot | mitigate | Acceptance criterion: `python3 -c "import json; json.load(open('metadata.json'))"` exits 0; rollback = `git revert` of the single-file commit |
| T-07-02-05 | Info-disclosure | baseline directory commits sensitive data | accept | Captured JSON contains only public API responses already served by data.zeeker.sg; no secrets; same content category as phase-06-pre baselines already committed |
</threat_model>

<verification>
End-to-end verification (after all tasks ship):

1. **metadata.json shape:**
   ```
   python3 -c "import json; d=json.load(open('metadata.json')); print(sorted(d.keys()))"
   # → ['about', 'about_url', 'databases', 'description', 'license', 'license_url', 'menu_links', 'plugins', 'source', 'source_url', 'title']
   ```

2. **Live stack serves the new shape:**
   ```
   curl -fsS http://localhost/-/metadata.json | jq 'keys'
   # → 11-key array, no extra_css_urls, no extra_js_urls, menu_links present
   ```

3. **Frontend nav still renders:**
   ```
   curl -fsS http://localhost/ | grep -oE '<a href="/[^"]*">[^<]+</a>' | grep -E 'How to Use|Developers|About|Status'
   # → 4 lines (Home is rendered separately as the logo)
   ```

4. **Baseline directory committed + jq-checked:**
   ```
   git ls-files .planning/baselines/phase-07-pre/*.json | wc -l   # ≥ 10
   jq -e 'has("extra_css_urls") | not' .planning/baselines/phase-07-pre/-_metadata.json.json
   ```

5. **Verifier baseline cascade prefers phase-07-pre:**
   ```
   bash scripts/verify_phase_06.sh 2>&1 | grep -E 'verify_api_parity\.sh exit 0 against phase-07-pre'
   ```
</verification>

<success_criteria>
- metadata.json: 11 top-level keys; no `extra_*_urls`; `menu_links`/`plugins`/`databases` blocks preserved verbatim.
- Local stack rebuilt; `/-/metadata.json` body matches the on-disk file.
- `phase-07-pre/` baseline directory contains ≥10 JSON files + matching `.url` sidecars + README + .gitkeep; all committed to git.
- 3 verifier scripts prepend `phase-07-pre` to the baseline cascade; verify_phase_05.sh untouched.
- Frontend nav (rendered via metadata.menu_links) still shows the 5 menu items on a `curl http://localhost/` body.
- `bash scripts/verify_phase_06.sh` exits 0 against local stack; Section K reports parity against `phase-07-pre`.
</success_criteria>

<output>
After completion, create `.planning/phases/07-prune-zeeker-datasette/07-02-SUMMARY.md` with:
- Diff of metadata.json (extra_*_urls removed, everything else byte-identical)
- Listing of phase-07-pre/ directory contents + jq-derived shape of -_metadata.json.json baseline
- Diff of the three verifier scripts (one-line cascade prepend per file)
- Output of `bash scripts/verify_phase_06.sh` (Section K should report phase-07-pre)
- Threat register dispositions (T-07-02-01..05 all addressed)
</output>
