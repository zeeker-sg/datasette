---
phase: 07-prune-zeeker-datasette
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - scripts/verify_phase_03.sh
autonomous: true
requirements:
  - REQ-api-byte-parity
  - REQ-internal-only-datasette-exposure
must_haves:
  truths:
    - "A rollback git tag `pre-phase-7-prune` points at the head commit BEFORE any deletion changes land."
    - "ROADMAP Phase 7 scope description names the actual top-level repo paths (`plugins/`, `templates/`, `static/`, `Dockerfile`, `metadata.json`) — not the non-existent `packages/zeeker-datasette/`."
    - "verify_phase_03.sh body-content fallthrough sniff identifies Datasette HTML using a Datasette-bundled fingerprint (`/-/static/datasette-manager.js` + literal `Powered by Datasette`) — NOT the `zeeker-base.css` overlay path which disappears in Plan 07-04."
    - "Frontend pytest suite (`uv run pytest -q` in packages/zeeker-frontend) still passes with no regressions; this plan only edits documentation + a verifier script."
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Authoritative Phase 7 scope at top-level repo paths"
      contains: "Phase 7 — Prune zeeker-datasette"
    - path: "scripts/verify_phase_03.sh"
      provides: "Phase-3 verifier with Datasette-bundled fingerprint (post-prune-safe)"
      pattern: "datasette-manager\\.js|Powered by Datasette"
  key_links:
    - from: "scripts/verify_phase_03.sh"
      to: "Datasette bundled defaults (no user overlay required)"
      via: "fingerprint sniff"
      pattern: "datasette-manager\\.js"
    - from: "git tag pre-phase-7-prune"
      to: "current HEAD before Plan 07-02 ships"
      via: "git tag"
      pattern: "pre-phase-7-prune"
---

<objective>
Wave 0 scaffolding: lock a rollback point, fix the wrong-path scope description in ROADMAP, and rebase the Phase-3 verifier's silent-fallthrough fingerprint to a Datasette-bundled string that survives the templates/+static/ deletion.

Purpose: Plans 07-02..07-05 will delete `static/css/zeeker-base.css` from the repo AND remove its `extra_css_urls` reference from `metadata.json`. After that, Datasette's own HTML pages no longer ship the `zeeker-base.css` link tag — but `verify_phase_03.sh` uses that exact string as its load-bearing "did this URL fall through to Datasette HTML?" guard. Without rebasing the fingerprint FIRST, the moment Plan 07-04 lands, every subsequent Phase-3/4/6 verifier run produces false negatives (negative-routing checks pass when they shouldn't, because the tell-tale string is gone). Phase 3's accumulated decisions explicitly authorise stale-check retirement in lockstep with the change that invalidates them.

Output: One commit retagging HEAD with `pre-phase-7-prune`; one commit updating ROADMAP scope text; one commit updating verify_phase_03.sh fingerprint sentinel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07-prune-zeeker-datasette/07-RESEARCH.md
@.planning/phases/07-prune-zeeker-datasette/07-VALIDATION.md
@scripts/verify_phase_03.sh

<interfaces>
<!-- Phase-3 verifier fingerprint surface — exact lines that reference `zeeker-base.css`.
     Verified via `grep -n 'zeeker-base.css' scripts/verify_phase_03.sh`:
       106: if echo "$BODY" | grep -qiE 'zeeker-base\.css|datasette'; then
       116: if echo "$BODY" | grep -qiE 'zeeker-base\.css|datasette|search'; then
       154: if grep -q 'zeeker-base.css' /tmp/zeeker-neg-body; then
       228: if echo "$BODY_UPPER" | grep -qE 'zeeker-base\.css|"error"|"ok"|"tables"'; then
     Lines 106 and 116 already include `datasette` as an OR-alt, so they survive
     the prune unchanged (the `datasette` literal still appears in Datasette's
     bundled HTML). The decisive fix is line 154 (the `check_negative` body sniff
     — load-bearing "are we silently falling through to Datasette HTML?" check)
     which has NO OR-alternative today. Line 228 also needs widening so the
     uppercase-routing check still asserts a Datasette tell. -->

Datasette's bundled HTML reliably contains:
  - The literal string `Powered by Datasette` (footer, every Datasette HTML page)
  - The path `/-/static/datasette-manager.js` (script tag, every Datasette HTML page)
  - The path `/-/static/app.css` (stylesheet link, every Datasette HTML page)

These come from datasette's own template defaults under `datasette/templates/_base.html`
and `datasette/static/` — they ship in the Python package and do not depend on
user-supplied templates or extra_css_urls. They survive Plan 07-04's mass delete.
</interfaces>

# Source artifact references
- Phase-7 RESEARCH §"A second verifier hazard" — names `/-/static/datasette-manager.js` and "Powered by Datasette" as the post-prune-safe fingerprints.
- Phase-3 ROADMAP §"Phase 3 decisions accumulated" — establishes the precedent for stale-check retirement in the same phase that invalidates the check.
- Phase-7 RESEARCH §"scope_correction" — `packages/zeeker-datasette/` does not exist; deletion targets are at repo root.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tag pre-prune rollback point</name>
  <files>.git/refs/tags/pre-phase-7-prune</files>
  <read_first>
    - git log -1 --format='%H %s' HEAD (confirm we are tagging the right commit; should be the most recent Phase-6 close-out, currently `c36c1fe docs(phase-6): close out`)
  </read_first>
  <action>
    Create an annotated git tag `pre-phase-7-prune` at the current HEAD commit. This is the single-command rollback target if any subsequent Phase-7 plan ships a regression that survives the verifier gate.

    Exact command:
    ```
    git tag -a pre-phase-7-prune -m "Pre-Phase-7 rollback point (head before any deletion lands). Per 07-01 must_haves + 07-RESEARCH §Q5."
    ```

    Verify the tag exists locally with `git tag -l pre-phase-7-prune` returning the tag name. Do NOT push the tag to the remote in this task — pushing is a separate operator decision (per Phase-3 precedent of single-file/single-decision commits). The tag survives squash merges because it is a direct ref, not a commit-history annotation.
  </action>
  <verify>
    <automated>git tag -l pre-phase-7-prune | grep -qx pre-phase-7-prune</automated>
  </verify>
  <acceptance_criteria>
    - `git tag -l pre-phase-7-prune` outputs exactly `pre-phase-7-prune` (no other text).
    - `git rev-parse pre-phase-7-prune` returns a 40-char SHA (proves it's a real ref).
    - `git rev-parse pre-phase-7-prune` equals `git rev-parse HEAD` (tag points at current HEAD).
  </acceptance_criteria>
  <done>Annotated tag `pre-phase-7-prune` created at current HEAD; rollback is `git revert pre-phase-7-prune..HEAD` once Plan 07-04 lands.</done>
</task>

<task type="auto">
  <name>Task 2: Fix ROADMAP Phase 7 scope description (repo-root paths)</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md lines 300-317 (Phase 7 section as currently written — the lines naming `packages/zeeker-datasette/` are the bug)
  </read_first>
  <action>
    Edit `.planning/ROADMAP.md` lines 300-317 (the `#### Phase 7: Prune zeeker-datasette` section). The Goal paragraph and the Scope — in bullet list both reference `packages/zeeker-datasette/` which does not exist on disk (`docker-compose.yml` line 14 builds the datasette image from `context: .` — repo root). Replace every instance of `packages/zeeker-datasette/` in this section with a description that matches the actual top-level repo layout.

    Replacements (exact, in order they appear in lines 300-317):

    1. Line 302 (current): `**Goal:** Delete UI-coupled plugins and template/static directories from `packages/zeeker-datasette/`. The package becomes data-only: `Dockerfile`, `metadata.json`, `scripts/`, `entrypoint.sh`.`
       → New: `**Goal:** Delete UI-coupled plugins and the entire top-level `templates/` and `static/` directories from the Datasette image build context (repository root — the Datasette container is built from `docker-compose.yml` `context: .`). The image becomes data-only: `Dockerfile`, `metadata.json`, `scripts/`, `entrypoint.sh`, and `plugins/{__init__.py,cache_headers.py}` (the surviving non-UI ASGI cache wrapper).`

    2. Line 305 (current): `- Delete `developers_page.py`, `status_page.py`, `sources_page.py`, `string_manager.py`, `template_filters.py`.`
       → New: `- Delete `plugins/{developers_page.py, status_page.py, sources_page.py, string_manager.py, template_filters.py, strings.yaml}` (5 UI plugins + the orphan YAML config consumed only by `string_manager.py`). KEEP `plugins/__init__.py` (empty package marker) and `plugins/cache_headers.py` (pure ASGI Cloudflare wrapper, verified line-by-line — no template/static refs).`

    3. Line 306 (current): `- Delete `templates/` (all of it) and `static/` (all of it) from `packages/zeeker-datasette/`.`
       → New: `- Delete top-level `templates/` (entire directory, ~20 files including `_table-{db}-{table}.html` partials and `pages/`) and top-level `static/` (entire directory: `css/`, `js/`, `fonts/`).`

    4. After line 306, insert two NEW bullets (the load-bearing edits beyond raw deletes):
       - `- Edit `metadata.json`: drop `extra_css_urls` array (currently references `/static/css/vendor/prism.css` + `/static/css/zeeker-base.css`) and `extra_js_urls` array (currently references three `/static/js/...` paths). KEEP `menu_links` (consumed by frontend `base.html` line 18-25 to render the dark editorial nav), KEEP `plugins.datasette-search-all` block (data-layer plugin behind D-01-preserved `/-/search`), KEEP `databases.*` config block.`
       - `- Edit `Dockerfile` lines 30-32: drop `COPY templates/`, `COPY static/`, narrow `COPY plugins/` to a whitelist of the two surviving files (`__init__.py` + `cache_headers.py`).`
       - `- Edit `scripts/download_from_s3.py` lines 159-202: stop redownloading `templates/`/`static/`/`plugins/` from S3 on container restart (per 07-RESEARCH Q3 Option A — without this edit the prune is cosmetic-only at runtime because S3 re-overlays the deleted files on every restart). PRESERVE the database `.db` file pull path (lines 108-142, the `_download_database_files` method) — that is the load-bearing data-sync behaviour.`

    5. Line 309 (current): `- `packages/zeeker-datasette/` contains 0 UI plugins, no `templates/`, no `static/`.`
       → New: `- Repository root contains 0 UI plugins under `plugins/` (only `__init__.py` + `cache_headers.py` remain), no top-level `templates/` directory, no top-level `static/` directory. Datasette image rebuilds clean.`

    Also append, immediately after line 314 (`**References:** PRD §10 Steps 4 + 5, §7.1, §12.`), a new line:
    `**Requirements:** REQ-api-byte-parity, REQ-eliminate-template-drift, REQ-frontend-route-set, REQ-internal-only-datasette-exposure, REQ-escape-datasette-template-surface, REQ-reduce-plugin-count`

    These are the requirement IDs from REQUIREMENTS.md that Phase 7 closes (verified by reading REQUIREMENTS.md — REQ-escape-datasette-template-surface explicitly names "Phase 07" and REQ-reduce-plugin-count explicitly names "Phase 07").

    Do NOT change the Plans count (gsd-plan-phase will fill that in via the `update_roadmap` step at the end of this run). Do NOT touch any other phase's section.
  </action>
  <verify>
    <automated>grep -c 'packages/zeeker-datasette/' .planning/ROADMAP.md | grep -qx 0 && grep -q 'top-level `templates/`' .planning/ROADMAP.md && grep -q 'plugins/cache_headers.py' .planning/ROADMAP.md && grep -q '**Requirements:** REQ-api-byte-parity' .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'packages/zeeker-datasette/' .planning/ROADMAP.md` returns `0` (the bad path is fully eliminated from the file).
    - `grep -q 'plugins/{developers_page.py, status_page.py, sources_page.py, string_manager.py, template_filters.py, strings.yaml}' .planning/ROADMAP.md` exits 0 (specific plugin list landed).
    - `grep -q 'plugins/cache_headers.py' .planning/ROADMAP.md` exits 0 (KEEP statement landed).
    - `grep -q 'extra_css_urls' .planning/ROADMAP.md` exits 0 (metadata.json edit named in scope).
    - `grep -q 'scripts/download_from_s3.py' .planning/ROADMAP.md` exits 0 (Q3 Option A landed in scope).
    - `grep -qE '^\*\*Requirements:\*\* REQ-api-byte-parity.*REQ-reduce-plugin-count' .planning/ROADMAP.md` exits 0 (requirements line present).
    - All other phase headings (`#### Phase 1`, `Phase 2`, `Phase 3`, `Phase 4`, `Phase 5`, `Phase 6`, `Phase 8`) untouched: `grep -c '^#### Phase ' .planning/ROADMAP.md` returns `8` (unchanged from before edit).
  </acceptance_criteria>
  <done>ROADMAP Phase 7 scope text now describes the actual repo layout; downstream plans + verifier scripts reference repo-root paths consistent with the source code.</done>
</task>

<task type="auto">
  <name>Task 3: Rebase verify_phase_03.sh fingerprint to Datasette-bundled string</name>
  <files>scripts/verify_phase_03.sh</files>
  <read_first>
    - scripts/verify_phase_03.sh entire file (276 lines) — particularly lines 105-110 (positive `/-/sql` reach), 113-123 (positive `/-/search` reach), 138-175 (the `check_negative` function — load-bearing fallthrough sniff), 226-232 (uppercase case-insensitive check)
    - 07-RESEARCH.md §"A second verifier hazard" paragraph
  </read_first>
  <action>
    Edit `scripts/verify_phase_03.sh` to replace the `zeeker-base.css` fallthrough fingerprint with Datasette-bundled strings that survive Phase-7's static/ deletion. Four edit sites:

    **Edit 1 — line 154** (the `check_negative` body sniff, the load-bearing assertion of the script):
    Current:
    ```
      if grep -q 'zeeker-base.css' /tmp/zeeker-neg-body; then
        fail "$path FALLTHROUGH BUG: datasette HTML served (zeeker-base.css present in body)"
        return
      fi
    ```
    Replace with:
    ```
      # Post-Phase-7 fingerprint: Datasette's bundled HTML always references
      # /-/static/datasette-manager.js and the literal "Powered by Datasette" footer.
      # These ship in the datasette package and survive deletion of user
      # templates/+static/ overlays. Match either to flag silent fallthrough.
      if grep -qE '/-/static/datasette-manager\.js|Powered by Datasette' /tmp/zeeker-neg-body; then
        fail "$path FALLTHROUGH BUG: datasette HTML served (datasette-manager.js or 'Powered by Datasette' present in body)"
        return
      fi
    ```

    **Edit 2 — also update the comment block at lines 17-24** (the script's banner explaining the load-bearing test):
    Current relevant lines:
    ```
    # Background: the load-bearing test in this script is the
    # `check_negative` function. It probes URLs that should hit frontend
    # (and 404) and grep's the response body for `zeeker-base.css` — the
    # unique datasette-HTML fingerprint. If a "negative" URL returns body
    # containing `zeeker-base.css`, the matcher is silently fall-through-ing
    # to datasette and the routing flip is broken even though status codes
    # may look normal.
    ```
    Replace with:
    ```
    # Background: the load-bearing test in this script is the
    # `check_negative` function. It probes URLs that should hit frontend
    # and grep's the response body for `/-/static/datasette-manager.js`
    # or "Powered by Datasette" — Datasette-bundled HTML fingerprints
    # that ship inside the datasette package itself (no user overlay
    # required). If a "negative" URL returns body containing either,
    # the matcher is silently fall-through-ing to datasette and the
    # routing flip is broken even though status codes may look normal.
    # Pre-Phase-7 this used `zeeker-base.css` (an M1 overlay path), but
    # Plan 07-04 deletes that file, so the fingerprint was rebased to
    # the Datasette-bundled string in Plan 07-01 (per Phase-3 stale-
    # check retirement decision).
    ```

    **Edit 3 — line 152-153** (the comment immediately above the grep — keep it factual):
    Current:
    ```
      # The decisive fallthrough sniff: datasette HTML always references
      # zeeker-base.css (verified live 2026-04-21 across /, /sglawwatch,
      # /sglawwatch/headlines, datasette's own 404 page).
    ```
    Replace with:
    ```
      # The decisive fallthrough sniff: datasette HTML always references
      # /-/static/datasette-manager.js (script tag) and contains the literal
      # "Powered by Datasette" footer (verified in datasette 0.65.2 bundled
      # default templates — both ship in the datasette Python package and
      # survive Phase-7's user templates/+static/ deletion).
    ```

    **Edit 4 — line 228** (the uppercase case-insensitive check). It currently uses `'zeeker-base\.css|"error"|"ok"|"tables"'` — keep `datasette` markers but extend the alternation to include the new fingerprint so the check still positively asserts datasette was reached:
    Current:
    ```
    if echo "$BODY_UPPER" | grep -qE 'zeeker-base\.css|"error"|"ok"|"tables"'; then
      ok "uppercase .JSON also routes to datasette (case-insensitive)"
    ```
    Replace with:
    ```
    if echo "$BODY_UPPER" | grep -qE 'datasette-manager\.js|Powered by Datasette|"error"|"ok"|"tables"'; then
      ok "uppercase .JSON also routes to datasette (case-insensitive)"
    ```

    Lines 106 (`/-/sql` positive) and 116 (`/-/search` positive) already use `'zeeker-base\.css|datasette'` and `'zeeker-base\.css|datasette|search'` — both have OR-alt `datasette` (a literal string Datasette HTML always contains) so they keep working post-prune. Leave those two lines alone (the OR-alt absorbs the loss). This is an intentional minimal-surface edit; do not touch what does not need touching.

    Do NOT touch verify_phase_04.sh or verify_phase_06.sh in this plan — they delegate to verify_phase_03.sh for negative-routing checks, so fixing 03.sh propagates the fix automatically. Plan 07-02 will edit the baseline-cascade arrays in 04.sh and 06.sh to add `phase-07-pre`.
  </action>
  <verify>
    <automated>bash -n scripts/verify_phase_03.sh && grep -c 'zeeker-base\.css' scripts/verify_phase_03.sh | grep -qxE '[0-2]' && grep -q 'datasette-manager\.js' scripts/verify_phase_03.sh && grep -q 'Powered by Datasette' scripts/verify_phase_03.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/verify_phase_03.sh` exits 0 (script remains syntactically valid bash after edits).
    - `grep -c 'datasette-manager\.js' scripts/verify_phase_03.sh` returns at least `2` (one in the comment block, one in the live grep at the rebased line 154 region; Edit 4 contributes a third).
    - `grep -c 'Powered by Datasette' scripts/verify_phase_03.sh` returns at least `2` (comment + live grep).
    - `grep -nE "grep -q 'zeeker-base.css'" scripts/verify_phase_03.sh` returns no lines (the bare-`zeeker-base.css`-only sniff is fully retired). Surviving references must all be in OR-alternations or comments.
    - The two intentional surviving references (lines 106 and 116, OR-alt with `datasette`) still match: `grep -cE "'zeeker-base\\\\.css\\|datasette" scripts/verify_phase_03.sh` returns `2`.
    - Run a smoke pass against the live local stack (only if `docker compose ps zeeker-datasette` reports running): `BASE_URL=http://localhost bash scripts/verify_phase_03.sh`. The verifier should still exit 0 — Phase-3's behaviour is unchanged (Datasette HTML still contains both `zeeker-base.css` AND the new fingerprint pre-prune; the OR-alt covers both eras). If the local stack isn't up, skip this acceptance criterion (don't fail on missing docker).
  </acceptance_criteria>
  <done>verify_phase_03.sh fallthrough fingerprint identifies Datasette HTML using a string that ships inside the datasette package, so the verifier survives Plan 07-04's static/+templates/ deletion intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev workstation → git remote | Tag push is a separate operator decision; not in this plan |
| dev workstation → ROADMAP.md (planning surface) | Doc-only edit; no runtime impact |
| dev workstation → verify_phase_03.sh (test surface) | Script edit; affects subsequent verifier passes only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Tampering | git tag `pre-phase-7-prune` | mitigate | Tag is created BEFORE any deletion lands (Task 1 runs first; Task 2 + Task 3 only edit non-runtime files); rollback target is verifiably the head of master before Phase-7 deletion sequence |
| T-07-01-02 | Repudiation | verify_phase_03.sh fingerprint update | mitigate | Old `zeeker-base.css` reference retained in OR-alternations on lines 106 and 116 so the verifier remains green on a NOT-YET-PRUNED stack (Plan 07-01 ships standalone before Plan 07-02..04 land); commit message + comment-block update document the rebase rationale + cite Phase-3 stale-check-retirement precedent |
| T-07-01-03 | DoS (build) | ROADMAP scope edit | accept | Doc-only; no build, deploy, or runtime impact; reverting is `git revert <commit>` |
| T-07-01-04 | Info-disclosure | tag points at HEAD | accept | HEAD is the public Phase-6 close-out commit; no secrets exposed |
</threat_model>

<verification>
End-to-end verification commands (run after all 3 tasks ship):

1. **Tag exists + at HEAD:**
   ```
   git rev-parse pre-phase-7-prune == git rev-parse HEAD
   ```

2. **ROADMAP free of bad path:**
   ```
   grep -c 'packages/zeeker-datasette/' .planning/ROADMAP.md  # → 0
   grep -c 'plugins/cache_headers.py' .planning/ROADMAP.md    # → ≥1
   ```

3. **verify_phase_03.sh syntactically valid + fingerprint rebased:**
   ```
   bash -n scripts/verify_phase_03.sh
   grep -c 'datasette-manager\.js' scripts/verify_phase_03.sh  # → ≥2
   ```

4. **Local-stack smoke (if docker compose up):**
   ```
   bash scripts/verify_phase_03.sh  # → exits 0; pre-prune state is unchanged
   ```

5. **Frontend pytest unaffected (regression gate):**
   ```
   cd packages/zeeker-frontend && uv run pytest -q  # → 165 passed (or whatever the current count is)
   ```
</verification>

<success_criteria>
- Tag `pre-phase-7-prune` exists at current HEAD; rollback for the entire Phase-7 sequence is `git revert pre-phase-7-prune..HEAD`.
- ROADMAP Phase 7 section names actual repo paths; the wrong `packages/zeeker-datasette/` path is fully eliminated.
- ROADMAP Phase 7 section explicitly enumerates the metadata.json edit + Dockerfile narrowing + download_from_s3.py edit (Q3 Option A) as in-scope.
- verify_phase_03.sh body-content sniff uses Datasette-bundled fingerprint; verifier still exits 0 against the current pre-prune stack (mixed-era safety: both fingerprints present today; only the new one will remain post-Plan-07-04).
- Frontend pytest suite unchanged (no regressions; this plan touches no frontend code).
- Three commits: tag-only (no commit needed for tag), ROADMAP edit, verifier edit.
</success_criteria>

<output>
After completion, create `.planning/phases/07-prune-zeeker-datasette/07-01-SUMMARY.md` with:
- Tag SHA captured + verification it points at the right commit
- Diff stats for ROADMAP.md and scripts/verify_phase_03.sh edits
- Confirmation of `bash -n scripts/verify_phase_03.sh` exit 0
- Confirmation of frontend pytest count unchanged
- Threat register dispositions (T-07-01-01..04 all addressed)
</output>
