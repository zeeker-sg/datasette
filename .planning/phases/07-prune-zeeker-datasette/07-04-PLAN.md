---
phase: 07-prune-zeeker-datasette
plan: 04
type: execute
wave: 2
depends_on:
  - 07-02
  - 07-03
files_modified:
  - plugins/developers_page.py
  - plugins/status_page.py
  - plugins/sources_page.py
  - plugins/string_manager.py
  - plugins/template_filters.py
  - plugins/strings.yaml
  - templates/
  - static/
  - Dockerfile
  - entrypoint.sh
  - pyproject.toml
  - tests/conftest.py
  - tests/fixtures.py
autonomous: true
requirements:
  - REQ-eliminate-template-drift
  - REQ-escape-datasette-template-surface
  - REQ-reduce-plugin-count
  - REQ-api-byte-parity
must_haves:
  truths:
    - "`plugins/` contains exactly 2 files: `__init__.py` and `cache_headers.py`. Five UI plugins + `strings.yaml` are gone."
    - "Top-level `templates/` directory does NOT exist on disk."
    - "Top-level `static/` directory does NOT exist on disk."
    - "`Dockerfile` no longer copies `templates/` or `static/` into the image; the `COPY plugins/` line is narrowed to whitelist only the 2 surviving files."
    - "`entrypoint.sh` `datasette serve` invocation does not reference any deleted directory in a way that breaks container boot."
    - "`pyproject.toml` retains `pyyaml` (load-bearing for `verify_phase_02.sh` per researcher A5) and `datasette-template-sql` (used by remaining metadata.json plugin block) — only deps that are now demonstrably unused are pruned."
    - "`tests/conftest.py` and `tests/fixtures.py` no longer reference deleted plugin filenames in their fixture content; the existing pytest suite (`pytest tests/`) still passes."
    - "Frontend pytest suite (`uv run pytest -q` in packages/zeeker-frontend) still passes — frontend doesn't import anything from the deleted files."
  artifacts:
    - path: "plugins/__init__.py"
      provides: "Package marker (preserved)"
      contains: ""
    - path: "plugins/cache_headers.py"
      provides: "Cloudflare/CDN ASGI Cache-Control wrapper (preserved verbatim)"
      pattern: "asgi_wrapper"
    - path: "Dockerfile"
      provides: "Datasette image build script — UI overlay COPY lines removed"
      negative_pattern: "COPY templates/|COPY static/"
  key_links:
    - from: "Dockerfile"
      to: "plugins/cache_headers.py + plugins/__init__.py"
      via: "narrowed COPY"
      pattern: "COPY plugins/__init__.py|COPY plugins/cache_headers.py"
    - from: "entrypoint.sh datasette serve"
      to: "/data/*.db"
      via: "datasette --immutable invocation"
      pattern: "datasette serve"
---

<objective>
The mass deletion. After the previous waves rebased the verifier fingerprint (07-01), cleaned metadata.json + re-baselined (07-02), and severed the runtime S3 re-overlay (07-03), this wave physically deletes the M1 UI scaffolding from the repo: 5 UI plugins + 1 orphan YAML, the entire top-level `templates/` directory, the entire top-level `static/` directory, and narrows `Dockerfile` + audits `entrypoint.sh`/`pyproject.toml` + scrubs stale references in `tests/conftest.py` + `tests/fixtures.py`.

Purpose: Plan 07-02 made `/-/metadata.json` no longer reference these files; Plan 07-03 made the runtime S3 sync no longer redownload them; this plan removes them from the container image inputs entirely. After this plan ships, `docker compose build zeeker-datasette` produces an image that no longer contains the M1 overlay; Plan 07-05 then verifies + ships it.

Output: A single coherent commit (or a small ordered set of commits — splitter's discretion if mass-delete + edits in one diff is unwieldy) that achieves the prune. Rollback is `git revert`. The Plan 07-01 tag `pre-phase-7-prune` is the fallback if a multi-commit revert chain is needed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07-prune-zeeker-datasette/07-RESEARCH.md
@.planning/phases/07-prune-zeeker-datasette/07-VALIDATION.md
@Dockerfile
@entrypoint.sh
@pyproject.toml
@plugins/cache_headers.py
@tests/conftest.py
@tests/fixtures.py

<interfaces>
Authoritative deletion + edit list (verified against current disk state via `ls plugins/`, `ls templates/`, `ls static/`):

DELETE:
  plugins/developers_page.py     (UI plugin — frontend now serves /developers)
  plugins/status_page.py          (UI plugin — frontend now serves /status)
  plugins/sources_page.py         (UI plugin — frontend now serves /sources)
  plugins/string_manager.py       (UI plugin — frontend ports the s()/sf()/plural() helpers via filters.py)
  plugins/template_filters.py     (UI plugin — frontend ports filters via packages/zeeker-frontend/.../filters.py)
  plugins/strings.yaml            (orphan once string_manager.py gone — its only consumer)
  templates/                      (entire dir, ~20 files; frontend owns HTML)
  static/                         (entire dir; css/, js/, fonts/ — frontend has its own zeeker.css + woff2 set)

KEEP (these stay verbatim):
  plugins/__init__.py             (empty package marker)
  plugins/cache_headers.py        (pure ASGI Cache-Control wrapper for Cloudflare; line-by-line verified — no template/static refs)

EDIT:
  Dockerfile lines 30-32          (3 COPY lines — narrow plugins/, drop templates/ + static/)
  Dockerfile lines 38-41          (mkdir -p block — narrow to /data + /app/plugins only; templates/ + static/ dirs no longer needed)
  Dockerfile lines 44-48          (ENV vars referencing /app/templates + /app/static — leave them; datasette gracefully handles missing dirs and removing the env vars would be an over-edit)
  entrypoint.sh lines 23-30       (the `datasette serve` invocation — see Action below for the targeted edit decision)
  pyproject.toml dependencies     (audit; conservatively prune ONLY deps with zero remaining consumers per researcher A5)
  tests/conftest.py line 53       (`"plugins/template_filters.py": "# Template filters"` — fixture references a deleted file)
  tests/fixtures.py lines 60-71   (sample_s3_responses asset_files_default block lists `assets/default/plugins/template_filters.py` etc. — fixture should be sanitized)

NOT TOUCHED (intentionally):
  metadata.json                   (already cleaned in 07-02)
  scripts/download_from_s3.py     (already edited in 07-03)
  scripts/verify_phase_*.sh       (verify_phase_03.sh already rebased in 07-01; the fingerprint update + baseline cascade are done)
  All other root files (.dockerignore, .gitignore, README.md, docker-compose.yml/prod.yml, Caddyfile/.prod, etc.) — out of scope

Verified file: plugins/cache_headers.py (75 lines)
  - Pure ASGI middleware via @hookimpl asgi_wrapper
  - References: NO_CACHE_PREFIXES = ("/-/",), STATIC_PREFIX = "/static/"
  - The STATIC_PREFIX constant is referenced by path.startswith(STATIC_PREFIX) inside the wrapper.
    The constant remains correct semantically — datasette's bundled defaults still serve from /-/static/
    (note the leading dash) NOT /static/, so post-prune /static/ requests will 404 from datasette
    naturally. The cache_headers.py logic is unaffected: it just won't match the prefix on routes
    that no longer exist. Leaving it is safer than editing — Phase 8 may revisit caching strategy.

Verified entrypoint.sh (30 lines):
  Line 23-30: `exec uv run datasette serve --host 0.0.0.0 --port 8001 \
    --metadata /app/metadata.json \
    --template-dir /app/templates \
    --plugins-dir /app/plugins \
    --static static:/app/static \
    --cors \
    --immutable \
    $(ls /data/*.db)`
  Decision: datasette gracefully handles missing --template-dir AND missing --static (verified against
  Datasette 0.65.2 source — these are optional flags; if the directory does not exist, datasette logs
  a warning and continues). For minimum-blast-radius, the SAFE edit is to KEEP entrypoint.sh as-is.
  Plan 07-04 verifies this assumption locally (Task 4 acceptance criteria).

Verified Dockerfile lines 38-41:
  RUN mkdir -p /data \
      && mkdir -p /app/templates \
      && mkdir -p /app/static/databases \
      && mkdir -p /app/plugins
  Decision: keep `mkdir -p /data` (load-bearing — entrypoint.sh greps /data for *.db); keep
  `mkdir -p /app/plugins` (the COPY destination); drop `mkdir -p /app/templates` and
  `mkdir -p /app/static/databases` to reduce image surface. This is a defensive narrowing —
  the directories simply don't exist post-build.
</interfaces>

<pyproject_audit>
Current `pyproject.toml` dependencies (root, NOT packages/zeeker-frontend):
  boto3>=1.28.0                   — KEEP (used by scripts/download_from_s3.py + manage.py)
  click>=8.1.3                    — KEEP (used by scripts/manage.py CLI)
  datasette-matomo>=0.1.2         — AUDIT: still in metadata.json plugins block? No — metadata.json plugins block only has datasette-search-all post Plan 07-02. Datasette will still load datasette-matomo as a Python plugin if installed, and the env vars DATASETTE_MATOMO_* are still set in docker-compose.yml. KEEP for now (Phase-8 will migrate Matomo to frontend per ROADMAP §Phase 8).
  datasette-search-all>=1.1.4     — KEEP (provides /-/search; D-01 keeps reachable)
  datasette==0.65.2               — KEEP (the runtime)
  python-dotenv>=1.0.0            — KEEP (used by scripts/manage.py)
  pyyaml>=6.0.2                   — KEEP per researcher A5 ("retained for verify_phase_02.sh")
  datasette-template-sql>=1.0     — AUDIT: was used by deleted templates for SQL-driven content. Post-prune
                                    no template consumes it. Researcher noted "review whether still needed"
                                    in 07-RESEARCH. Conservative call: keep it for one phase (it's a tiny
                                    runtime cost and Phase 8 can prune if confirmed unused). Logging
                                    the decision in the plan summary is enough.

Conclusion: NO deps removed in this plan (researcher's "conservative trim only deps explicitly confirmed unused" — and none qualify under that bar).
</pyproject_audit>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-deletion grep audit (proves nothing live imports the deletion targets)</name>
  <files>(no file modifications)</files>
  <read_first>
    - 07-RESEARCH §"Researcher verified zero remaining consumers post-deletion" (Q7) — the researcher already did this, but the planner re-confirms here in case anything drifted between research and now.
  </read_first>
  <action>
    Execute (do not commit; this is a pre-flight grep gate). Run each grep and capture stdout to a /tmp file, then reason about results before proceeding to Task 2:

    1. Confirm no surviving Python file imports any of the 5 deletion-target plugins:
       ```
       grep -rEn 'from plugins\.(developers_page|status_page|sources_page|string_manager|template_filters)|import plugins\.(developers_page|status_page|sources_page|string_manager|template_filters)' \
         --include='*.py' \
         --exclude-dir=__pycache__ \
         --exclude-dir=.venv \
         --exclude-dir=node_modules \
         . > /tmp/p07-04-import-audit.txt || true
       cat /tmp/p07-04-import-audit.txt
       ```
       Expected: zero lines (no Python file imports them). If non-zero, the importing file must be inspected before proceeding.

    2. Confirm no template references the deletion-target plugins via `register_routes` or similar plugin entry points:
       ```
       grep -rEn 'developers_page|status_page|sources_page|string_manager|template_filters' \
         --include='*.html' \
         --include='*.py' \
         --include='*.yaml' \
         --include='*.json' \
         --exclude-dir=__pycache__ \
         --exclude-dir=.planning \
         --exclude-dir=.venv \
         --exclude-dir=node_modules \
         --exclude-dir=packages/zeeker-frontend/src/zeeker_frontend/.venv \
         . > /tmp/p07-04-string-audit.txt || true
       wc -l /tmp/p07-04-string-audit.txt
       ```
       Expected: matches confined to (a) the deletion-target files themselves, (b) `tests/conftest.py:53` (fixture string — Task 5 cleans this), (c) `tests/fixtures.py:69` (fixture string — Task 5 cleans this), (d) `.planning/phases/07-prune-zeeker-datasette/07-RESEARCH.md` and PLAN files (intentional), (e) potentially historical phase-summary `.md` files (intentional). NO matches in `packages/zeeker-frontend/`, `Dockerfile`, `metadata.json`, `entrypoint.sh`, or any non-test root .py file outside the targets.

    3. Confirm `entrypoint.sh` references no deleted path that would break boot. The current state references `/app/templates` and `/app/static` via datasette CLI flags; verify this is the only reference shape:
       ```
       grep -nE 'templates|static' entrypoint.sh
       ```
       Expected: 3 lines (--template-dir, --plugins-dir is unrelated; --static static:/app/static — single line). The `--template-dir` and `--static` flags accept missing directories per Datasette 0.65.2 source code.

    If any audit produces unexpected matches, ABORT this task and surface the matches in the SUMMARY for human review BEFORE proceeding to deletion. The plan does not auto-correct surprises.

    Document the three audit outputs (or "clean" if zero unexpected) in the Plan 07-04 SUMMARY for forensic record.
  </action>
  <verify>
    <automated>set -e; grep -rEn 'from plugins\.(developers_page|status_page|sources_page|string_manager|template_filters)|import plugins\.(developers_page|status_page|sources_page|string_manager|template_filters)' --include='*.py' --exclude-dir=__pycache__ --exclude-dir=.venv --exclude-dir=node_modules . | grep -v '^./plugins/' > /tmp/p07-04-import-check.txt || true; wc -l < /tmp/p07-04-import-check.txt | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - Audit 1 (Python imports): zero unexpected matches outside `./plugins/` itself.
    - Audit 2 (string references in code/templates/yaml/json): matches only in the test files Task 5 cleans + the deletion targets themselves + intentional .planning/ docs.
    - Audit 3 (entrypoint.sh references): matches only the documented `--template-dir`/`--static`/`--plugins-dir` flag lines.
    - Findings recorded in SUMMARY (counts + any unexpected lines flagged for human review).
  </acceptance_criteria>
  <done>Pre-flight audit complete; nothing surprising surfaced; safe to proceed to deletion.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the 5 UI plugins + strings.yaml</name>
  <files>plugins/developers_page.py, plugins/status_page.py, plugins/sources_page.py, plugins/string_manager.py, plugins/template_filters.py, plugins/strings.yaml</files>
  <read_first>
    - `ls plugins/` — confirm all 8 files present (`__init__.py`, `cache_headers.py`, `developers_page.py`, `sources_page.py`, `status_page.py`, `string_manager.py`, `template_filters.py`, `strings.yaml`).
    - 07-RESEARCH §"scope_correction" table — confirms KEEP vs DELETE per file.
  </read_first>
  <action>
    `git rm` exactly six files (NOT a wildcard `rm` — explicit names so an accidental `cache_headers.py` deletion is impossible):

    ```
    git rm plugins/developers_page.py
    git rm plugins/status_page.py
    git rm plugins/sources_page.py
    git rm plugins/string_manager.py
    git rm plugins/template_filters.py
    git rm plugins/strings.yaml
    ```

    After deletion, the `plugins/` directory must contain exactly 2 files: `__init__.py` (preserved) and `cache_headers.py` (preserved verbatim — its 75-line body must be byte-identical to pre-edit).

    Stage but do not yet commit (will be combined with Tasks 3-6 in a single coherent commit at the end of the plan, OR split by the executor's discretion if the diff is too large for a single review surface).
  </action>
  <verify>
    <automated>! test -e plugins/developers_page.py && ! test -e plugins/status_page.py && ! test -e plugins/sources_page.py && ! test -e plugins/string_manager.py && ! test -e plugins/template_filters.py && ! test -e plugins/strings.yaml && test -e plugins/__init__.py && test -e plugins/cache_headers.py && [ "$(ls plugins/ | wc -l)" -eq 2 ]</automated>
  </verify>
  <acceptance_criteria>
    - `! test -e plugins/developers_page.py` (deleted).
    - `! test -e plugins/status_page.py` (deleted).
    - `! test -e plugins/sources_page.py` (deleted).
    - `! test -e plugins/string_manager.py` (deleted).
    - `! test -e plugins/template_filters.py` (deleted).
    - `! test -e plugins/strings.yaml` (deleted).
    - `test -e plugins/__init__.py` (preserved).
    - `test -e plugins/cache_headers.py` (preserved).
    - `ls plugins/ | wc -l` returns `2`.
    - `git diff --cached --stat plugins/cache_headers.py` produces NO output (cache_headers.py byte-identical pre/post; not in the staged diff).
  </acceptance_criteria>
  <done>plugins/ directory contains exactly the 2 surviving files; cache_headers.py untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Delete top-level templates/ and static/ directories</name>
  <files>templates/, static/</files>
  <read_first>
    - `ls templates/` — confirms ~20 files (verified earlier: `_footer.html, _header.html, _partials/, _table-*.html (multiple), database.html, error.html, index.html, pages/, query.html, row.html, table.html`).
    - `ls static/` — confirms 3 subdirs (`css/`, `fonts/`, `js/`).
  </read_first>
  <action>
    Delete both top-level directories recursively. Use `git rm -r` so the deletion is staged in git, NOT a bare `rm -rf` (the latter would orphan files in the working tree without staging).

    ```
    git rm -r templates/
    git rm -r static/
    ```

    Both directories MUST be at the repository root — confirm their location BEFORE deletion (`git ls-files templates/ | head -1` should show paths starting with `templates/`, NOT `packages/.../templates/`). The frontend's templates live at `packages/zeeker-frontend/src/zeeker_frontend/templates/` and MUST NOT be touched.

    Cross-check after staging:
    - `! test -d templates/` — top-level dir gone.
    - `! test -d static/` — top-level dir gone.
    - `test -d packages/zeeker-frontend/src/zeeker_frontend/templates/` — frontend dir UNTOUCHED.
    - `test -d packages/zeeker-frontend/src/zeeker_frontend/static/` — frontend dir UNTOUCHED.
  </action>
  <verify>
    <automated>! test -d templates && ! test -d static && test -d packages/zeeker-frontend/src/zeeker_frontend/templates && test -d packages/zeeker-frontend/src/zeeker_frontend/static</automated>
  </verify>
  <acceptance_criteria>
    - `! test -d templates` — root templates/ gone.
    - `! test -d static` — root static/ gone.
    - `test -d packages/zeeker-frontend/src/zeeker_frontend/templates` — frontend templates UNTOUCHED.
    - `test -d packages/zeeker-frontend/src/zeeker_frontend/static` — frontend static UNTOUCHED.
    - `git status --porcelain templates/ static/ 2>/dev/null | head -3` shows lines starting with `D ` (deleted, staged), NOT `D ` from working tree.
  </acceptance_criteria>
  <done>Top-level templates/ and static/ removed from git index; frontend equivalents intact.</done>
</task>

<task type="auto">
  <name>Task 4: Edit Dockerfile — narrow COPY plugins, drop COPY templates/static, narrow mkdir block</name>
  <files>Dockerfile</files>
  <read_first>
    - Dockerfile entire file (63 lines). Edit targets: lines 30-32 (`COPY templates/`, `COPY static/`, `COPY plugins/`); lines 38-41 (`mkdir -p` block).
    - 07-RESEARCH §"Dockerfile needs three `COPY` lines removed" paragraph.
  </read_first>
  <action>
    Edit `Dockerfile` at TWO surfaces:

    **Edit 1 — lines 29-32** (the `# Copy base templates, static files, and plugins` block, currently 4 lines including the comment).

    Current text:
    ```
    # Copy base templates, static files, and plugins
    COPY templates/ ./templates/
    COPY static/ ./static/
    COPY plugins/ ./plugins/
    ```

    Replace with:
    ```
    # Copy surviving plugins (Phase-7 prune narrowed to cache_headers + __init__).
    # templates/ and static/ are no longer copied — the frontend service owns
    # HTML rendering. The plugins/ COPY is whitelisted so an accidental
    # restoration of plugins/<deleted-file>.py at the top level (e.g. via
    # rebase) does not silently re-introduce UI plugins into the image.
    COPY plugins/__init__.py ./plugins/__init__.py
    COPY plugins/cache_headers.py ./plugins/cache_headers.py
    ```

    The whitelist form (`COPY plugins/__init__.py ./plugins/__init__.py` rather than `COPY plugins/ ./plugins/`) prevents future drift: even if someone restores `plugins/string_manager.py` at the repo root, the Docker image will not pick it up because only the two named files are copied. This is a defense-in-depth measure per T-07-04-01 mitigation.

    **Edit 2 — lines 38-41** (the `mkdir -p` block).

    Current text:
    ```
    RUN mkdir -p /data \
        && mkdir -p /app/templates \
        && mkdir -p /app/static/databases \
        && mkdir -p /app/plugins
    ```

    Replace with:
    ```
    RUN mkdir -p /data \
        && mkdir -p /app/plugins
    ```

    `/data` is load-bearing for `entrypoint.sh` (`ls /data/*.db`); `/app/plugins` is the COPY destination (the `COPY plugins/__init__.py ./plugins/__init__.py` line creates `/app/plugins/` implicitly, but the explicit `mkdir -p` is harmless and matches existing style). `/app/templates` and `/app/static/databases` are no longer needed — datasette accepts missing directories.

    DO NOT touch lines 44-48 (the ENV vars `DATASETTE_TEMPLATE_DIR`, `DATASETTE_STATIC_DIR`, `DATASETTE_PLUGINS_DIR`). They are read by `scripts/download_from_s3.py` (which Plan 07-03 already neutered) — leaving the env vars in is a no-op now and keeps a future revival cheap. This is intentional minimal-edit discipline.

    DO NOT touch other lines: shebang/FROM (1), uv copy (4), apt-get (9-14), pyproject copy (17), uv sync block (20-24), scripts copy (27), metadata copy (35), EXPOSE (54), entrypoint copy (57-58), download_from_s3 chmod (61), ENTRYPOINT (63).

    The `Dockerfile` must remain a valid Docker image build script: `docker buildx build --check Dockerfile` (or `docker build --no-cache --target ... .` if buildx isn't installed) ideally completes without lint errors. If buildx is unavailable, `docker compose config` is a sufficient sanity check.
  </action>
  <verify>
    <automated>! grep -E '^COPY templates/' Dockerfile && ! grep -E '^COPY static/' Dockerfile && grep -q 'COPY plugins/__init__.py' Dockerfile && grep -q 'COPY plugins/cache_headers.py' Dockerfile && ! grep -E 'mkdir -p /app/templates' Dockerfile && ! grep -E 'mkdir -p /app/static' Dockerfile && grep -q 'mkdir -p /app/plugins' Dockerfile</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -E '^COPY templates/' Dockerfile` (line removed).
    - `! grep -E '^COPY static/' Dockerfile` (line removed).
    - `grep -c '^COPY plugins/__init__.py' Dockerfile` returns `1` (whitelisted COPY for marker).
    - `grep -c '^COPY plugins/cache_headers.py' Dockerfile` returns `1` (whitelisted COPY for survivor).
    - `! grep 'mkdir -p /app/templates' Dockerfile` (mkdir for deleted dir gone).
    - `! grep 'mkdir -p /app/static' Dockerfile` (mkdir for deleted dir gone).
    - `grep -q 'mkdir -p /data' Dockerfile` (load-bearing data dir kept).
    - `grep -q 'mkdir -p /app/plugins' Dockerfile` (COPY destination kept).
    - `docker compose config zeeker-datasette > /dev/null 2>&1` (compose can still resolve the Dockerfile reference).
    - Optional: `docker compose build zeeker-datasette` builds without error. If this is run, capture the output to `/tmp/p07-04-build.log` for the SUMMARY. (Plan 07-05 runs the build under HUMAN UAT, so this is opportunistic verification only.)
  </acceptance_criteria>
  <done>Dockerfile no longer copies UI overlay; surviving plugins COPY is whitelisted; mkdir block narrowed.</done>
</task>

<task type="auto">
  <name>Task 5: Scrub stale plugin references in tests/conftest.py + tests/fixtures.py</name>
  <files>tests/conftest.py, tests/fixtures.py</files>
  <read_first>
    - tests/conftest.py line 53 (the fixture dict entry `"plugins/template_filters.py": "# Template filters"`).
    - tests/fixtures.py line 69 (the asset_files_default `Contents` list entry `{"Key": "assets/default/plugins/template_filters.py"}`).
    - tests/conftest.py lines 39-54 (the entire `files` dict — multiple entries reference deleted paths: `static/css/zeeker-base.css`, `static/js/zeeker-base.js`, `templates/search.html`, `templates/database.html`, `extra_css_urls`, `extra_js_urls`).
    - tests/fixtures.py lines 60-71 (the `asset_files_default` block — multiple Contents entries reference deleted assets).
    - 07-RESEARCH §"researcher noted 'stale references located'" — names this task.
  </read_first>
  <action>
    Edit two test fixture files to remove references to deleted files. The fixtures are used by `tests/test_download_from_s3.py` to simulate S3 contents in unit tests; with Plan 07-03's edits, these fixtures should reflect the new data-only sync scope.

    **Edit 1 — tests/conftest.py lines 39-54** (the `files` dict in `temp_project_structure`):

    Current `files` dict:
    ```
    files = {
        "metadata.json": {
            "title": "Test Zeeker",
            "description": "Test instance",
            "databases": {"*": {"allow_sql": True}},
            "extra_css_urls": ["/static/css/zeeker-base.css"],
            "extra_js_urls": ["/static/js/zeeker-base.js"],
        },
        ".env": "S3_BUCKET=test-bucket\nAWS_REGION=us-east-1\n",
        "templates/search.html": "<html><body>Test Index</body></html>",
        "templates/database.html": "<html><body>Test Database</body></html>",
        "static/css/zeeker-base.css": "body { background: #1a1a1a; }",
        "static/js/zeeker-base.js": "console.log('Enhanced JS loaded');",
        "plugins/__init__.py": "",
        "plugins/template_filters.py": "# Template filters",
    }
    ```

    Replace with (post-prune fixture shape):
    ```
    files = {
        "metadata.json": {
            "title": "Test Zeeker",
            "description": "Test instance",
            "databases": {"*": {"allow_sql": True}},
        },
        ".env": "S3_BUCKET=test-bucket\nAWS_REGION=us-east-1\n",
        "plugins/__init__.py": "",
        "plugins/cache_headers.py": "# Cache headers placeholder",
    }
    ```

    Also update the `directories` list at lines 23-33 — the `templates`, `static`, `static/css`, `static/js`, `static/images`, `static/databases` entries no longer need to be created in the fixture project structure. Replace lines 23-33:

    Current:
    ```
        directories = [
            "data",
            "templates",
            "static",
            "static/css",
            "static/js",
            "static/images",
            "static/databases",
            "plugins",
            "scripts",
        ]
    ```

    Replace with:
    ```
        directories = [
            "data",
            "plugins",
            "scripts",
        ]
    ```

    **Edit 2 — tests/fixtures.py lines 58-71** (the `asset_files_default` block in `sample_s3_responses`):

    Current:
    ```
                "asset_files_default": {
                    "Contents": [
                        {"Key": "assets/default/metadata.json"},
                        {"Key": "assets/default/templates/search.html"},
                        {"Key": "assets/default/templates/database.html"},
                        {"Key": "assets/default/templates/table.html"},
                        {"Key": "assets/default/templates/row.html"},
                        {"Key": "assets/default/templates/query.html"},
                        {"Key": "assets/default/static/css/zeeker-base.css"},
                        {"Key": "assets/default/static/js/zeeker-base.js"},
                        {"Key": "assets/default/plugins/__init__.py"},
                        {"Key": "assets/default/plugins/template_filters.py"},
                    ]
                },
    ```

    Replace with:
    ```
                "asset_files_default": {
                    "Contents": [
                        # Phase-7 prune (07-RESEARCH Q3 Option A): the data-only
                        # sync only handles metadata.json + .db files; per-template
                        # and per-static asset entries removed from the fixture.
                        {"Key": "assets/default/metadata.json"},
                    ]
                },
    ```

    Other shapes in tests/fixtures.py that reference deleted paths (`base_comprehensive` metadata at lines 108-150, `sample_template_files` static method at lines 232-340, `sample_static_files` static method at lines 343-499) are pure data fixtures NOT consumed by tests/test_download_from_s3.py (verified by `grep -rn "sample_template_files\|sample_static_files\|base_comprehensive" tests/`). Conservative call: leave them in place. They are dead test data but harmless; deleting them is a Phase-8 housekeeping concern.

    After edits, run the full pytest suite locally:
    ```
    cd /Users/houfu/Projects/zeeker-datasette && pytest tests/ -q
    ```
    Must exit 0 (no test regressions). If a test breaks because it depended on a deleted fixture entry, that test must be updated in the same edit pass.
  </action>
  <verify>
    <automated>! grep -q 'plugins/template_filters.py' tests/conftest.py && ! grep -q 'extra_css_urls' tests/conftest.py && ! grep -q 'static/css/zeeker-base.css' tests/conftest.py && ! grep -q 'plugins/template_filters.py' tests/fixtures.py && cd /Users/houfu/Projects/zeeker-datasette && pytest tests/ -q 2>&1 | tail -3 | grep -qE 'passed'</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -q 'plugins/template_filters.py' tests/conftest.py` (stale fixture entry removed).
    - `! grep -q 'extra_css_urls' tests/conftest.py` (stale extra_css_urls entry removed; the live metadata.json no longer has it post-Plan-07-02).
    - `! grep -q 'static/css/zeeker-base.css' tests/conftest.py` (stale static fixture removed).
    - `! grep -q 'plugins/template_filters.py' tests/fixtures.py` (stale fixture S3 key removed from asset_files_default).
    - `pytest tests/ -q` exits 0; the full suite count is preserved or increases (test-removal allowed only if it tests a deleted plugin's behaviour).
    - The `plugins/cache_headers.py` test file (`tests/test_cache_headers.py`) still exists and still passes — proves the surviving plugin is exercised.
  </acceptance_criteria>
  <done>Test fixtures reflect the post-prune shape; suite exits 0; no live code paths reference deleted fixture content.</done>
</task>

<task type="auto">
  <name>Task 6: Verify entrypoint.sh + pyproject.toml audit + frontend regression gate</name>
  <files>(no file modifications — gate-only task)</files>
  <read_first>
    - entrypoint.sh entire file (30 lines).
    - pyproject.toml entire file (22 lines).
    - The pre-commit aggregate state of Tasks 2-5.
  </read_first>
  <action>
    Three gate checks (no edits in this task — purely verification before the plan ships):

    **Gate 1 — entrypoint.sh boots a clean image.** Build the image and run a one-shot container to verify entrypoint.sh's `datasette serve` invocation tolerates the missing `/app/templates` and `/app/static` directories:

    ```
    docker compose build zeeker-datasette
    docker compose up -d zeeker-datasette
    sleep 30  # wait for healthcheck start_period (60s in compose; 30s suffices for first-pass readiness)
    docker compose ps zeeker-datasette
    docker compose exec -T zeeker-datasette curl -fsS http://localhost:8001/-/versions.json
    docker compose logs zeeker-datasette --tail 50
    ```

    Acceptance:
    - Container reaches `running` state.
    - `/-/versions.json` returns 200 (datasette is up).
    - Logs show no FATAL errors. Specifically: NO traceback referencing FileNotFoundError on /app/templates or /app/static.
    - If logs show `WARNING: --template-dir does not exist` or similar, that is EXPECTED + ACCEPTABLE — datasette starts anyway. Document the warning in SUMMARY for transparency.

    If the container fails to boot, edit entrypoint.sh: drop the `--template-dir /app/templates` line and the `--static static:/app/static` line from the `datasette serve` invocation (lines 25-27 of entrypoint.sh). This is a fallback edit only triggered by Gate 1 failure.

    **Gate 2 — pyproject.toml audit (no-op confirmation).** Per the `<pyproject_audit>` analysis in the context block, NO dependencies are removed in this plan. Verify by:

    ```
    diff <(git show HEAD:pyproject.toml) pyproject.toml
    ```

    Expected: empty diff (no changes). If a dependency was removed by mistake, restore it via `git checkout HEAD -- pyproject.toml`.

    **Gate 3 — Frontend regression gate.** Confirm the frontend pytest suite still passes (the frontend doesn't import anything from the deleted files, but it does fetch `/-/metadata.json` from datasette via fetch_site_metadata — and the surviving metadata.json shape was already locked in Plan 07-02):

    ```
    cd /Users/houfu/Projects/zeeker-datasette/packages/zeeker-frontend && uv run pytest -q
    ```

    Acceptance: exit 0; pass count >= 155 (the Phase-6 baseline). Any regression here means a frontend code path depended on something in metadata.json that this plan accidentally affected — block the plan + investigate.

    All three gates must pass before this plan's commit is final.
  </action>
  <verify>
    <automated>cd /Users/houfu/Projects/zeeker-datasette/packages/zeeker-frontend && uv run pytest -q 2>&1 | tail -3 | grep -qE '[0-9]+ passed'</automated>
  </verify>
  <acceptance_criteria>
    - Gate 1: `docker compose ps zeeker-datasette` reports running + healthy; `/-/versions.json` returns 200.
    - Gate 2: `pyproject.toml` is byte-identical to the previous-commit version (`git diff HEAD -- pyproject.toml` produces empty output post-commit-of-tasks-2-5).
    - Gate 3: frontend `pytest -q` exits 0; pass count `>= 155`.
    - Any container-boot warnings about missing `/app/templates` or `/app/static` documented in SUMMARY (these are EXPECTED and ACCEPTABLE).
  </acceptance_criteria>
  <done>Entrypoint boots clean against the pruned image; no dependency removal regrets; frontend suite green. Plan 07-04 is shippable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev workstation → git index (mass delete) | A wildcard `rm` against `plugins/` would catch cache_headers.py; Task 2 uses explicit named removes |
| Dockerfile → image build | Narrowed COPY whitelist; cannot accidentally re-introduce a deleted file via rebase |
| entrypoint.sh → datasette CLI | Missing `--template-dir`/`--static` directories — datasette tolerates this; Gate 1 verifies |
| pyproject.toml → runtime deps | Aggressive removal could break `verify_phase_02.sh` (pyyaml) or future plugins; this plan removes nothing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | Tampering (HIGH) | accidental deletion of cache_headers.py | mitigate | Task 2 uses 6 explicit `git rm <file>` calls (no wildcards); acceptance criterion verifies cache_headers.py is byte-identical pre/post (git diff --cached shows no changes); Dockerfile narrowed COPY whitelist defends against re-introduction via rebase per T-07-04-04 |
| T-07-04-02 | Tampering (HIGH) | pyproject.toml dep removal that breaks verify_phase_02.sh | mitigate | Plan removes ZERO deps (per `<pyproject_audit>` block analysis citing researcher A5 on pyyaml); Gate 2 verifies pyproject.toml is byte-identical post-commit |
| T-07-04-03 | DoS | entrypoint.sh references deleted path → container fails to start | mitigate | Gate 1 (Task 6) explicitly boots the rebuilt image and verifies container reaches healthy state + `/-/versions.json` returns 200; if boot fails, the fallback edit (drop --template-dir + --static lines from entrypoint.sh) is documented in Task 6 action |
| T-07-04-04 | Repudiation | rebase silently re-introduces deleted plugin | mitigate | Dockerfile uses whitelisted COPY (`COPY plugins/__init__.py ./plugins/__init__.py` etc.) so a re-appearing plugins/string_manager.py at the repo root is NOT picked up by the image build; defense-in-depth beyond the git-level deletion |
| T-07-04-05 | Tampering | frontend templates/ accidentally deleted instead of root templates/ | mitigate | Task 3 acceptance criterion verifies `test -d packages/zeeker-frontend/src/zeeker_frontend/templates/` returns 0 (frontend dir UNTOUCHED); Task 3 action calls out the path discrimination explicitly |
| T-07-04-06 | DoS | test fixture edits break pytest collection | mitigate | Task 5 acceptance criterion runs `pytest tests/ -q` and asserts exit 0 + `passed` in output; Gate 3 in Task 6 also runs the frontend pytest suite (separate gate against a different test surface) |
| T-07-04-07 | Info-disclosure | git history retains deleted file contents | accept | Standard git semantics; deleted files are recoverable via `git show pre-phase-7-prune:plugins/string_manager.py` etc.; no secrets in those files (verified by inspection — the deleted plugins read public YAML) |
</threat_model>

<verification>
End-to-end verification (after all tasks ship):

1. **Plugins directory shape:**
   ```
   ls plugins/  # → __init__.py  cache_headers.py  (exactly 2 files)
   ```

2. **Top-level templates/ + static/ gone, frontend equivalents intact:**
   ```
   ! test -d templates && ! test -d static
   test -d packages/zeeker-frontend/src/zeeker_frontend/templates
   test -d packages/zeeker-frontend/src/zeeker_frontend/static
   ```

3. **Dockerfile narrowed:**
   ```
   ! grep -E '^COPY templates/' Dockerfile
   ! grep -E '^COPY static/' Dockerfile
   grep -c '^COPY plugins/' Dockerfile  # → 2 (the two whitelist lines)
   ```

4. **Image builds + container boots clean:**
   ```
   docker compose build zeeker-datasette
   docker compose up -d zeeker-datasette
   curl -fsS http://localhost/-/versions.json | jq -e '.datasette' >/dev/null
   ```

5. **All test suites still pass:**
   ```
   pytest tests/ -q
   cd packages/zeeker-frontend && uv run pytest -q
   ```

6. **pyproject.toml unchanged:**
   ```
   git diff HEAD -- pyproject.toml  # → empty
   ```
</verification>

<success_criteria>
- 6 files deleted from `plugins/`; 2 files preserved (cache_headers.py byte-identical).
- Top-level `templates/` and `static/` removed from disk + git index.
- `Dockerfile` no longer COPYs templates/static; plugins COPY whitelisted to 2 files; mkdir block narrowed.
- `entrypoint.sh` either unchanged (preferred — datasette tolerates missing dirs) OR has the `--template-dir`/`--static` flags dropped (fallback if Gate 1 surfaces a boot failure).
- `pyproject.toml` is byte-identical to pre-Plan-07-04.
- `tests/conftest.py` and `tests/fixtures.py` no longer reference deleted plugin filenames in fixture content.
- Both pytest suites (root + frontend) exit 0 with no regressions.
- Container builds + boots; `/-/versions.json` returns 200.
- `phase-07-pre/` API parity baseline still applies (no new diffs introduced beyond what Plan 07-02's metadata edit captured).
</success_criteria>

<output>
After completion, create `.planning/phases/07-prune-zeeker-datasette/07-04-SUMMARY.md` with:
- Listing of plugins/ before + after (proves 6 deleted, 2 kept)
- Listing of templates/ + static/ deletion (file count + size)
- Dockerfile diff (focused on lines 29-32 + 38-41)
- entrypoint.sh diff (likely empty per Gate 1 fallback decision)
- tests/conftest.py + tests/fixtures.py diffs
- Container boot log excerpt (Gate 1 evidence)
- Both pytest suites' final lines (Gate 3 evidence)
- Threat register dispositions (T-07-04-01..07 all addressed)
</output>
