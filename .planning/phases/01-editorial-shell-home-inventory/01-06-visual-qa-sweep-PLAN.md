---
phase: 01-editorial-shell-home-inventory
plan: 06
type: execute
wave: 6
depends_on: ["01", "02", "03", "04", "05"]
files_modified:
  - scripts/visual_qa.py
autonomous: false
requirements:
  - SC-01-no-regressions
  - SC-01-visual-qa-green
  - SC-01-footer-contrast-check
  - SC-01-fixtures-still-works
  - SC-01-functional-regression
tags:
  - qa
  - regression
  - playwright
must_haves:
  truths:
    - "Visual QA sweep (`scripts/visual_qa.py`) returns HTTP 200 on every configured route across Chromium + WebKit + desktop + mobile"
    - "/fixtures still returns 200 (today's `metadata.get('tables', {}).get(name)` fix intact)"
    - "Footer links are visible at proper contrast on every page (today's `footer a:link` override intact)"
    - "One representative route per production content type is exercised: a *_news table, the judgments table, the legal-guide table"
    - "Core Datasette functionality (filters, facets, FTS, sort direction, pagination, CSV/JSON export) is verified FUNCTIONALLY against a representative news table — not just via HTTP 200 on the base URL"
  artifacts:
    - path: "scripts/visual_qa.py"
      provides: "Extended ROUTES list covering home, developers, status, about, how-to-use, sources, three representative content-type tables"
      contains: "acra_news"
    - path: "tmp/qa/index.html"
      provides: "Contact sheet generated by the sweep — evidence of visual correctness + HTTP 200 on every route"
      contains: "s200"
  key_links:
    - from: "scripts/visual_qa.py ROUTES"
      to: "the four sketch-findings winning patterns (home, database, table-feed, shell)"
      via: "one representative URL per surface"
      pattern: "SG-Government-Newsrooms\\|Zeeker-Judgements\\|Sglawwatch"
---

<objective>
Extend `scripts/visual_qa.py` to include one URL per production content type and run the full sweep to confirm: (a) zero HTTP regressions on any instrumented route, (b) the four sketch-findings patterns render correctly, (c) today's `/fixtures` fix and today's footer fix both survive, (d) Datasette's core interactive features (FTS, pagination, sort direction, CSV/JSON export, facets) remain FUNCTIONALLY intact on a representative news table. This plan ships the script change, a functional-regression task, the sweep run, and a human checkpoint to review the contact sheet before the phase closes.

Purpose: The whole phase introduces many template and CSS changes. HTTP 200 on a base URL is a necessary but INSUFFICIENT signal — a partial can return 200 while breaking FTS, pagination, sort, or export (BLK-06). A dedicated functional-regression task is required to prove those dimensions survived, then the visual sweep covers cross-browser visual correctness, then a human sign-off closes the phase.

Output: Updated `scripts/visual_qa.py` with three new routes, a new Task 2 that runs curl-based functional regression checks against `acra_news` (BLK-06), the sweep run producing `tmp/qa/index.html` with all-green HTTP 200 statuses, plus a human checkpoint confirming no visual regressions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@scripts/visual_qa.py
@.planning/phases/01-editorial-shell-home-inventory/01-02-shared-chrome-SUMMARY.md
@.planning/phases/01-editorial-shell-home-inventory/01-04-database-editorial-rows-SUMMARY.md
@.planning/phases/01-editorial-shell-home-inventory/01-05-table-feed-partials-SUMMARY.md

<interfaces>
The existing `scripts/visual_qa.py` has a `ROUTES: list[tuple[str, str]]` constant (currently 14 routes). Each entry is `(slug_for_filename, url_path)`. The script runs each route through Chromium + WebKit × desktop (1440×900) + mobile (390×844), capturing a PNG and the HTTP status.

Current routes (line 15-30 in existing file):
- `("home", "/")`
- `("about", "/about")`
- `("how-to-use", "/how-to-use")`
- `("sources", "/sources")`
- `("status", "/status")`
- `("developers", "/developers")`
- `("search-all", "/-/search")`
- `("database", "/fixtures")`
- `("table-facetable", "/fixtures/facetable")`
- `("table-searchable-fts", "/fixtures/searchable?_search=dog")`
- `("table-sortable", "/fixtures/sortable")`
- `("table-roadside", "/fixtures/roadside_attractions")`
- `("row-view", "/fixtures/roadside_attractions/1")`
- `("query-sql", "/fixtures?sql=select+pk%2C+state%2C+on_earth+from+facetable+limit+10")`

Need to ADD three production routes (one per content type) from planning_context:
- News feed: `/SG-Government-Newsrooms/acra_news` — tests sketch 004-A feed cards with excerpt
- Judgments: `/Zeeker-Judgements/judgments` — tests judgment card variant with citation line
- Legal guides: `/Sglawwatch/about_singapore_law` — tests guide card variant with collapsed excerpt

The script runs `datasette serve` on `http://localhost:8001` by default (configurable via `--base-url`). The developer is currently running dev servers on 8001 (prod-like DBs) and 8002 (fixtures-only). Production DBs must be attached on the target port for the new routes to return 200.

Playwright + dependencies are already installed (per `pyproject.toml`). Run via `uv run python scripts/visual_qa.py`.

The script's output includes:
- PNGs under `tmp/qa/{browser}/{viewport}/{slug}.png`
- Contact sheet at `tmp/qa/index.html` with colored status badges (green `s200`, red `s404`/`s500`).

**Functional regression $BASE (BLK-06):** The new Task 2 uses `$BASE` as a shell env var, default `http://127.0.0.1:8001`. The executor must set `BASE` in the shell before running the task or rely on the default. Every check in Task 2 is a single shell line whose exit status or captured value must match the stated expectation.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add three production content-type routes to scripts/visual_qa.py ROUTES</name>
  <files>scripts/visual_qa.py</files>
  <read_first>
    - scripts/visual_qa.py (full file; confirm ROUTES constant location + current entries)
  </read_first>
  <action>
    Modify the `ROUTES` list in `scripts/visual_qa.py` (currently at lines 15-30). After the existing `("query-sql", …)` entry, append three new entries covering production content types:

    ```python
    # Production content-type coverage (phase 01 editorial patterns)
    ("table-news-feed", "/SG-Government-Newsrooms/acra_news"),
    ("table-judgments", "/Zeeker-Judgements/judgments"),
    ("table-legal-guides", "/Sglawwatch/about_singapore_law"),
    ```

    Constraints:
    - Preserve all existing entries in order.
    - The new slugs (`table-news-feed`, `table-judgments`, `table-legal-guides`) must be unique against existing slugs — they are.
    - No other part of the script needs to change.

    After editing, confirm the file still parses Python 3.12 (the script uses `from __future__ import annotations`, `playwright.async_api`, and type hints — ensure no syntax error introduced).
  </action>
  <verify>
    <automated>grep -q 'table-news-feed' scripts/visual_qa.py && grep -q 'SG-Government-Newsrooms/acra_news' scripts/visual_qa.py && grep -q 'Zeeker-Judgements/judgments' scripts/visual_qa.py && grep -q 'Sglawwatch/about_singapore_law' scripts/visual_qa.py && python3 -c "import ast, sys; ast.parse(open('scripts/visual_qa.py').read()); sys.exit(0)"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/visual_qa.py` contains literal `"table-news-feed"`
    - `scripts/visual_qa.py` contains literal `"/SG-Government-Newsrooms/acra_news"`
    - `scripts/visual_qa.py` contains literal `"/Zeeker-Judgements/judgments"`
    - `scripts/visual_qa.py` contains literal `"/Sglawwatch/about_singapore_law"`
    - `scripts/visual_qa.py` still parses as valid Python (AST parse succeeds)
    - Existing routes are preserved in order
  </acceptance_criteria>
  <done>Script ROUTES list includes production coverage without deleting existing fixture routes.</done>
</task>

<task type="auto">
  <name>Task 2: Run functional-regression curl checks on a representative news table (BLK-06)</name>
  <files></files>
  <read_first>
    - .planning/phases/01-editorial-shell-home-inventory/01-05-table-feed-partials-SUMMARY.md — confirm which routes were wired
  </read_first>
  <action>
    A dev Datasette server must be running on `$BASE` (default `http://127.0.0.1:8001`) with the `SG-Government-Newsrooms` database attached. Export `BASE` if using a non-default host/port:

    ```bash
    export BASE="${BASE:-http://127.0.0.1:8001}"
    ```

    Then run EACH of the six checks below. Every check is a single shell line. If ANY check fails, stop and report the specific failing check to the developer before proceeding to Task 3.

    **Check 1 — FTS search still works:**
    ```bash
    curl -s "$BASE/SG-Government-Newsrooms/acra_news?_search=budget" -o /dev/null -w '%{http_code}'
    ```
    Expected: prints `200`.

    **Check 2 — Pagination size parameter still works:**
    ```bash
    curl -s "$BASE/SG-Government-Newsrooms/acra_news?_size=10" -o /dev/null -w '%{http_code}'
    ```
    Expected: prints `200`.

    **Check 3 — CSV export still works:**
    ```bash
    curl -s -o /dev/null -w '%{http_code}' "$BASE/SG-Government-Newsrooms/acra_news.csv"
    ```
    Expected: prints `200`.

    **Check 4 — JSON export still works:**
    ```bash
    curl -s -o /dev/null -w '%{http_code}' "$BASE/SG-Government-Newsrooms/acra_news.json?_shape=array"
    ```
    Expected: prints `200`.

    **Check 5 — Sort direction actually changes the output (ascending vs descending produce different HTML):**
    ```bash
    DESC_MD5=$(curl -s "$BASE/SG-Government-Newsrooms/acra_news?_sort_desc=published_date" | md5)
    ASC_MD5=$(curl -s "$BASE/SG-Government-Newsrooms/acra_news?_sort=published_date" | md5)
    [ "$DESC_MD5" != "$ASC_MD5" ] && echo "PASS" || echo "FAIL — sort direction does not change HTML"
    ```
    Expected: prints `PASS`. If `FAIL`, the sort links are not wired — investigate.

    **Check 6 — Facets sidebar or suggested facets still present in rendered HTML:**
    ```bash
    curl -s "$BASE/SG-Government-Newsrooms/acra_news" | grep -q 'class="facets"' \
      || curl -s "$BASE/SG-Government-Newsrooms/acra_news" | grep -q 'facet'
    ```
    Expected: exit code `0` (one of the patterns matched). If non-zero, the facets machinery was lost — investigate.

    All six checks must pass before this task is considered complete. Record the output of each check in the phase summary (or the dev log) for audit.
  </action>
  <verify>
    <automated>BASE="${BASE:-http://127.0.0.1:8001}"; [ "$(curl -s "$BASE/SG-Government-Newsrooms/acra_news?_search=budget" -o /dev/null -w '%{http_code}')" = "200" ] && [ "$(curl -s "$BASE/SG-Government-Newsrooms/acra_news?_size=10" -o /dev/null -w '%{http_code}')" = "200" ] && [ "$(curl -s -o /dev/null -w '%{http_code}' "$BASE/SG-Government-Newsrooms/acra_news.csv")" = "200" ] && [ "$(curl -s -o /dev/null -w '%{http_code}' "$BASE/SG-Government-Newsrooms/acra_news.json?_shape=array")" = "200" ] && [ "$(curl -s "$BASE/SG-Government-Newsrooms/acra_news?_sort_desc=published_date" | md5)" != "$(curl -s "$BASE/SG-Government-Newsrooms/acra_news?_sort=published_date" | md5)" ] && (curl -s "$BASE/SG-Government-Newsrooms/acra_news" | grep -q 'class="facets"' || curl -s "$BASE/SG-Government-Newsrooms/acra_news" | grep -q 'facet')</automated>
  </verify>
  <acceptance_criteria>
    - `curl -s "$BASE/SG-Government-Newsrooms/acra_news?_search=budget" -o /dev/null -w '%{http_code}'` == 200 (FTS search works)
    - `curl -s "$BASE/SG-Government-Newsrooms/acra_news?_size=10" -o /dev/null -w '%{http_code}'` == 200 (pagination size works)
    - `curl -s -o /dev/null -w '%{http_code}' "$BASE/SG-Government-Newsrooms/acra_news.csv"` == 200 (CSV export works)
    - `curl -s -o /dev/null -w '%{http_code}' "$BASE/SG-Government-Newsrooms/acra_news.json?_shape=array"` == 200 (JSON export works)
    - `curl -s "$BASE/SG-Government-Newsrooms/acra_news?_sort_desc=published_date" | md5` != `curl -s "$BASE/SG-Government-Newsrooms/acra_news?_sort=published_date" | md5` (sort direction matters)
    - `curl -s "$BASE/SG-Government-Newsrooms/acra_news" | grep -q 'class="facets"' || curl -s "$BASE/SG-Government-Newsrooms/acra_news" | grep -q 'facet'` (facets present)
  </acceptance_criteria>
  <done>All six functional regression checks pass against a running dev server. If any fail, the phase cannot proceed to the visual sweep.</done>
</task>

<task type="auto">
  <name>Task 3: Run the Playwright sweep and generate the contact sheet</name>
  <files></files>
  <read_first>
    - scripts/visual_qa.py (post-Task 1) — verify new routes present
  </read_first>
  <action>
    Ensure a Datasette dev server is running with BOTH the production databases and the fixtures database attached, listening on `http://localhost:8001`. If the developer has a split setup (prod on 8001, fixtures on 8002), run the sweep twice:

    1. Against prod server (captures production content-type routes):
       ```
       uv run python scripts/visual_qa.py --base-url http://localhost:8001 --out tmp/qa-prod
       ```
    2. Against fixtures server (captures fixtures routes):
       ```
       uv run python scripts/visual_qa.py --base-url http://127.0.0.1:8002 --out tmp/qa-fixtures
       ```

    If a single server can serve both (prod DBs + fixtures mounted), one sweep is sufficient.

    Alternatively — recommended — start a single dev server with all databases attached for QA:
    ```
    datasette serve \\
      /path/to/SG-Government-Newsrooms.db \\
      /path/to/Zeeker-Judgements.db \\
      /path/to/Sglawwatch.db \\
      /path/to/fixtures.db \\
      -m metadata.json --template-dir templates --static static:static \\
      --cors --port 8001
    ```
    (Executor: the exact *.db paths live wherever the developer has them — typically `./data/*.db` or under `tmp/`. Look for them or ask.)

    Expected outcome: each route in ROUTES emits 4 PNGs (chromium×desktop, chromium×mobile, webkit×desktop, webkit×mobile), all with HTTP 200 printed to stdout. Contact sheet at `tmp/qa/index.html` (or `tmp/qa-prod/index.html`).

    Commands printed in the terminal (verify):
    ```
    [chromium/desktop] /SG-Government-Newsrooms/acra_news -> 200
    [chromium/desktop] /Zeeker-Judgements/judgments -> 200
    [chromium/desktop] /Sglawwatch/about_singapore_law -> 200
    ```
    If any route prints `-> 404`, `-> 500`, or `ERR:`, stop and report the failing route for diagnosis before proceeding to the checkpoint.

    **INFO-03 — strengthen acceptance on the contact sheet:** the contact sheet HTML must (a) contain at least `expected-total` occurrences of `class="s200"` — where `expected-total` = `number_of_routes × 2_browsers × 2_viewports` — AND (b) contain ZERO occurrences of `class="s404"` or `class="s500"`. Any red badge is a regression.

    Compute `expected-total` from the current routes list:
    ```bash
    ROUTE_COUNT=$(python3 -c "import ast, sys; tree = ast.parse(open('scripts/visual_qa.py').read()); \
      print(next(len(n.value.elts) for n in ast.walk(tree) \
                 if isinstance(n, ast.Assign) and any(t.id == 'ROUTES' for t in n.targets if isinstance(t, ast.Name))))")
    EXPECTED=$((ROUTE_COUNT * 4))   # 2 browsers × 2 viewports
    echo "expected s200 count: $EXPECTED"
    ```
    (If the ast introspection is brittle, the executor can hand-count the ROUTES list entries and multiply by 4.)
  </action>
  <verify>
    <automated>(test -f tmp/qa/index.html || test -f tmp/qa-prod/index.html) && (! grep -q 'class="s404"' tmp/qa/index.html 2>/dev/null) && (! grep -q 'class="s500"' tmp/qa/index.html 2>/dev/null) && (! grep -q 'class="s404"' tmp/qa-prod/index.html 2>/dev/null) && (! grep -q 'class="s500"' tmp/qa-prod/index.html 2>/dev/null)</automated>
  </verify>
  <acceptance_criteria>
    - At least one `tmp/qa*/index.html` contact sheet exists after the run
    - stdout from the sweep shows `-> 200` for all routes (no 404/500/ERR)
    - `grep -c 'class="s200"' tmp/qa/index.html` (or the prod equivalent) >= expected-total (INFO-03)
    - `grep -c 'class="s404"' tmp/qa/index.html` == 0 AND `grep -c 'class="s500"' tmp/qa/index.html` == 0 — zero red badges (INFO-03)
  </acceptance_criteria>
  <done>Full sweep completes; no route returns non-200; contact sheet generated with the expected number of green s200 badges and zero red badges.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The phase's four editorial patterns (shell, home, database-rows, table-feed-cards) are rendered by the production templates, verified FUNCTIONALLY by Task 2's curl checks (FTS, pagination, sort, export, facets), and swept visually by `scripts/visual_qa.py` in both Chromium and WebKit × desktop + mobile. Contact sheet is at `tmp/qa/index.html` (or `tmp/qa-prod/index.html`).
  </what-built>
  <how-to-verify>
    1. Open the contact sheet file directly in a browser: `open tmp/qa/index.html` (or `tmp/qa-prod/index.html`).
    2. Confirm every cell shows a green `200` status badge. Any red badge = regression; stop and report.
    3. Inspect the `home`, `database` (both fixtures and prod), `table-news-feed`, `table-judgments`, `table-legal-guides`, and `table-facetable` rows side-by-side:
       - Home: dark nav with ochre logo, warm hero with italic-accent "rendered" in petrol, petrol stat band below, `№ 01 · Databases` label, card grid with rotating accent borders (petrol/ochre/terracotta in sequence), dark CTA at end.
       - Database (`/fixtures` and any prod DB): dark nav + breadcrumb + warm hero + petrol stat band + sticky `.db-toolbar` filter strip + editorial rows with display-serif titles and right-aligned row counts; FTS badge visible where applicable. Hero H1 italicizes the LAST word only.
       - Table feed (`/SG-Government-Newsrooms/acra_news`): cards stacked vertically, each with date (mono) + category pill (colored by type) + display-serif title + 2-line excerpt + SHA ID + Source ↗ link. Datasette's filter form, facets, FTS search, sort, pagination, and export panel still render around the card list.
       - Judgment feed: citation mono line above each card, case-name title, court pill.
       - Legal-guide feed: cards without excerpt blocks (content_length=0 path).
       - Fixtures tables without a `_table-*` partial (facetable, sortable): still render Datasette's default HTML table — correct (no regression).
    4. Footer on every page: 4 columns, link color visible at full contrast (NOT near-invisible rgba(255,255,244,0.8)).
    5. Mobile viewport (390×844) screenshots: hero H1 scales to text-4xl, stat band collapses to 2 columns, editorial rows collapse to idx+name+count, `.how-grid` collapses to 1 column.
    6. On a feed page, open the full-size PNG and verify the left Datasette facets sidebar AND the filter form above the cards are both still present.
    7. Confirm Task 2's six functional checks all printed `200` / `PASS` (FTS, pagination, CSV, JSON, sort-direction, facets).
  </how-to-verify>
  <resume-signal>Type "approved" to close the phase, or describe the specific regression(s) for a gap-closure plan.</resume-signal>
</task>

</tasks>

<verification>
1. `scripts/visual_qa.py` ROUTES list has exactly three new production-coverage entries added.
2. Task 2's six functional regression checks all pass against the dev server (BLK-06).
3. Sweep run completed without any non-200 HTTP status.
4. Contact sheet's `class="s200"` count meets or exceeds the route-count × 4 expectation, and contains ZERO `class="s404"` / `class="s500"` occurrences.
5. Human reviewer has inspected the contact sheet and approved.
6. `/fixtures` returns 200 in the sweep (regression check for today's database.html bug fix).
7. Footer contact-sheet cells visibly show paper-colored footer with links at secondary-text color on every page.
</verification>

<success_criteria>
- Updated script contains production routes.
- Task 2 functional checks all pass (BLK-06).
- Sweep produces contact sheet with all-green 200 statuses and zero red badges.
- Human checkpoint approved.
- No regression of today's two bug fixes (fixtures + footer).
</success_criteria>

<output>
Create `.planning/phases/01-editorial-shell-home-inventory/01-06-visual-qa-sweep-SUMMARY.md` documenting:
- Exact `datasette serve` command used (which DBs attached on which port).
- Output of each of Task 2's six functional regression checks (actual values, not just pass/fail).
- Contact sheet output path.
- `grep -c 'class="s200"'` count on the contact sheet, plus confirmation that `s404` / `s500` counts are zero.
- Any routes that returned non-200 and the diagnosis / resolution.
- Human-reviewer sign-off line (e.g. "Approved by {user} 2026-04-XX").
</output>
</content>
